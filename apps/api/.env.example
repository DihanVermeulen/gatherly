# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=gatherly
DB_USER=postgres
DB_PASSWORD=postgres

# Server Configuration
PORT=5001
FRONTEND_URL=http://localhost:3001

# JWT Authentication
# NOTE: Rotate JWT_SECRET and REFRESH_SECRET after Phase 28 (role removal) to invalidate all pre-existing tokens.
JWT_SECRET=your-jwt-secret-here-change-in-production
REFRESH_SECRET=your-refresh-secret-here-change-in-production
