---
phase: 29-phase-27-still-is-not-working
plan: 03
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - apps/gatherly-mobile/app/event-details.tsx
autonomous: true

must_haves:
  truths:
    - "Participant-only users (participantId in session, no full account) see a visible login/register option on the event-details screen"
    - "Tapping the login/register option navigates to the sign-in screen"
    - "Full account users do NOT see the login/register banner"
  artifacts:
    - path: "apps/gatherly-mobile/app/event-details.tsx"
      provides: "Login/register banner for participant-only sessions"
      contains: "participantId"
  key_links:
    - from: "apps/gatherly-mobile/app/event-details.tsx"
      to: "user.participantId"
      via: "useSession() hook"
      pattern: "user\\.participantId"
---

<objective>
Add a login/register banner to the event-details screen that is visible only to participant-only users (those signed in via magic link without a full account). This allows them to upgrade to a full account.

Purpose: The CONTEXT decision requires "Event details screen must have a visible login/sign-up option so participant-only users can upgrade to a full account." Without this, participant-only users have no path to creating a full account from within the app.
Output: Event-details screen with conditional login/register banner for participant-only sessions.
</objective>

<execution_context>
@C:\Users\dihan\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\dihan\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-phase-27-still-is-not-working/29-CONTEXT.md
@apps/gatherly-mobile/app/event-details.tsx
@apps/gatherly-mobile/app/api/auth.ts
@apps/gatherly-mobile/app/contexts/AuthContext.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add login/register banner to event-details for participant-only sessions</name>
  <files>apps/gatherly-mobile/app/event-details.tsx</files>
  <action>
In `apps/gatherly-mobile/app/event-details.tsx`, add a banner component that appears at the top of the event details content (below the hero block, above the participant list) when the user has a participant-only session.

**Detection logic:**
The `user` object from `useSession()` has `participantId?: number`. A participant-only session has `participantId` set AND the user was signed in via magic link (no full account). Check: `user?.participantId !== undefined`.

Full-account users have `user.id` set as their real user ID and `user.participantId` is undefined.

**Banner UI:**
- A View with light blue/teal background (e.g., `bg-primary-50` or inline style `{ backgroundColor: '#f0fdfa' }` — teal-50)
- Rounded corners (rounded-xl), padding (p-4), margin (mx-4, mb-4)
- Left-aligned text:
  - Bold text: "Create an account" (text-typography-900, font-semibold)
  - Body text: "Sign up to manage your events and wishlists across devices" (text-typography-500, text-sm)
- Right side or below text: "Sign Up" button (small/md size, teal/primary action)
- On press: `router.push("/register" as never)` — navigate to the register screen

**Important considerations:**
- Import `useRouter` from expo-router (already imported in the file)
- The banner should NOT appear for full-account users (when `user?.participantId === undefined`)
- Place the banner INSIDE the ScrollView, after the hero block but before the main content
- Use the existing GlueStack components already imported in the file (Text, Button, ButtonText, Pressable)
- Add import for `HStack` from @/components/ui/hstack if not already imported (for horizontal layout of text + button)

**Do NOT change any other functionality in event-details.tsx.** This is an additive change only.
  </action>
  <verify>
Grep for "participantId" in `apps/gatherly-mobile/app/event-details.tsx` — should find the detection logic.
Grep for "Create an account" in `apps/gatherly-mobile/app/event-details.tsx` — should find the banner text.
Grep for "register" in `apps/gatherly-mobile/app/event-details.tsx` — should find the navigation to register screen.
  </verify>
  <done>
Participant-only users see a "Create an account" banner on the event-details screen with a Sign Up button that navigates to the register screen. Full-account users do not see the banner.
  </done>
</task>

</tasks>

<verification>
1. A user signed in via magic link (participant-only session with participantId set) sees the "Create an account" banner on event-details
2. A full-account user does NOT see the banner
3. Tapping "Sign Up" navigates to the register screen
4. The banner does not break any existing event-details functionality
</verification>

<success_criteria>
- Participant-only sessions show a login/register banner on event-details
- Full-account sessions do not show the banner
- Banner has a clear call-to-action that navigates to the register screen
</success_criteria>

<output>
After completion, create `.planning/phases/29-phase-27-still-is-not-working/29-03-SUMMARY.md`
</output>
