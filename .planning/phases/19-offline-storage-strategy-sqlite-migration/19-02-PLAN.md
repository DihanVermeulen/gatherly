---
phase: 19-offline-storage-strategy
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - apps/gatherly-mobile/app/contexts/EventsContext.tsx
  - apps/gatherly-mobile/app/contexts/AuthContext.tsx
  - apps/gatherly-mobile/package.json
autonomous: true

must_haves:
  truths:
    - "Events are cached to SQLite after every successful API fetch"
    - "Cached events load from SQLite on app cold start before API responds"
    - "AsyncStorage import is completely gone from the codebase"
    - "Auth tokens continue to work (SecureStore keys unchanged)"
    - "Cache is cleared on user logout"
  artifacts:
    - path: "apps/gatherly-mobile/app/contexts/EventsContext.tsx"
      provides: "Events context using SQLite for caching instead of AsyncStorage"
      contains: "useDatabase"
    - path: "apps/gatherly-mobile/app/contexts/AuthContext.tsx"
      provides: "Auth context using SecureStorage abstraction + cache clearing on logout"
      contains: "clearCache"
  key_links:
    - from: "apps/gatherly-mobile/app/contexts/EventsContext.tsx"
      to: "apps/gatherly-mobile/lib/cache.ts"
      via: "cacheEvents and loadCachedEvents calls"
      pattern: "cacheEvents|loadCachedEvents"
    - from: "apps/gatherly-mobile/app/contexts/EventsContext.tsx"
      to: "apps/gatherly-mobile/contexts/DatabaseContext.tsx"
      via: "useDatabase hook"
      pattern: "useDatabase"
    - from: "apps/gatherly-mobile/app/contexts/AuthContext.tsx"
      to: "apps/gatherly-mobile/lib/secureStorage.ts"
      via: "SecureStorage import"
      pattern: "SecureStorage"
---

<objective>
Migrate the EventsContext from AsyncStorage to SQLite for caching, update AuthContext to use the SecureStorage abstraction and clear cache on logout, and remove the AsyncStorage dependency entirely.

Purpose: This is the core migration. The active EventsContext (app/contexts/EventsContext.tsx) currently uses AsyncStorage for caching events. It needs to use the SQLite cache layer from Plan 01 instead. AuthContext should use the SecureStorage abstraction for consistency and clear the SQLite cache on logout.

Output: EventsContext uses SQLite, AuthContext uses SecureStorage, AsyncStorage package removed.
</objective>

<execution_context>
@C:\Users\dihan\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\dihan\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-offline-storage-strategy-sqlite-migration/19-01-SUMMARY.md
@apps/gatherly-mobile/app/contexts/EventsContext.tsx
@apps/gatherly-mobile/app/contexts/AuthContext.tsx
@apps/gatherly-mobile/lib/database.ts
@apps/gatherly-mobile/lib/cache.ts
@apps/gatherly-mobile/lib/secureStorage.ts
@apps/gatherly-mobile/contexts/DatabaseContext.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate EventsContext from AsyncStorage to SQLite</name>
  <files>
    apps/gatherly-mobile/app/contexts/EventsContext.tsx
  </files>
  <action>
Rewrite the caching logic in `apps/gatherly-mobile/app/contexts/EventsContext.tsx`:

1. Remove the `import AsyncStorage from "@react-native-async-storage/async-storage";` line entirely.

2. Add imports:
   - `import { useDatabase } from "@/contexts/DatabaseContext";`
   - `import { cacheEvents, loadCachedEvents } from "@/lib/cache";`

3. Inside `EventsProvider`, get the database instance:
   ```typescript
   const db = useDatabase();
   ```

4. Update the "Load cached events on mount" useEffect:
   - BEFORE: `const saved = await AsyncStorage.getItem(STORAGE_KEY); ... JSON.parse(saved) ...`
   - AFTER: `const events = await loadCachedEvents(db);` then dispatch SET_EVENTS with the result
   - Remove the STORAGE_KEY constant (no longer needed)

5. Update the "Check API and fetch" useEffect:
   - BEFORE: `await AsyncStorage.setItem(STORAGE_KEY, JSON.stringify({ events }));`
   - AFTER: `await cacheEvents(db, events);`
   - Add `db` to the useEffect dependency array

6. Update the `refreshEvents` function:
   - BEFORE: `await AsyncStorage.setItem(STORAGE_KEY, JSON.stringify({ events }));`
   - AFTER: `await cacheEvents(db, events);`

7. Keep everything else the same — the reducer, the context shape, the useEvents hook. This is a drop-in replacement of the storage backend only.

IMPORTANT: The `db` comes from `useDatabase()` which is guaranteed non-null because DatabaseProvider blocks rendering until DB is ready (Plan 01). No null checks needed.
  </action>
  <verify>
    - `cd apps/gatherly-mobile && npx tsc --noEmit --pretty 2>&1 | head -30` compiles clean
    - `grep -r "AsyncStorage" apps/gatherly-mobile/app/contexts/EventsContext.tsx` returns nothing
    - `grep "useDatabase\|cacheEvents\|loadCachedEvents" apps/gatherly-mobile/app/contexts/EventsContext.tsx` shows all three imports used
  </verify>
  <done>
    - EventsContext.tsx uses SQLite (via useDatabase + cache functions) instead of AsyncStorage
    - No AsyncStorage references remain in EventsContext.tsx
    - Events are cached to SQLite after API fetch and loaded from SQLite on cold start
  </done>
</task>

<task type="auto">
  <name>Task 2: Update AuthContext to use SecureStorage abstraction + clear cache on logout + remove AsyncStorage package</name>
  <files>
    apps/gatherly-mobile/app/contexts/AuthContext.tsx
    apps/gatherly-mobile/package.json
  </files>
  <action>
1. Update `apps/gatherly-mobile/app/contexts/AuthContext.tsx`:
   - Add import: `import { SecureStorage } from "@/lib/secureStorage";`
   - Add import: `import { clearCache } from "@/lib/cache";`
   - Add import: `import { useDatabase } from "@/contexts/DatabaseContext";`
   - NOTE: AuthContext is used inside SessionProvider which is OUTSIDE DatabaseProvider in _layout.tsx. This means useDatabase() will NOT work here. Instead:
     - Import `initDatabase` from `@/lib/database` directly
     - In signOut, call `const db = await initDatabase(); await clearCache(db);` to clear the SQLite cache
     - This works because initDatabase() returns the singleton and doesn't re-create the DB
   - Optionally replace direct SecureStore calls with SecureStorage abstraction. The current code uses `SecureStore.setItemAsync(ACCESS_TOKEN_KEY, ...)` with key constants. Replace with `SecureStorage.setAuthToken(...)`, `SecureStorage.getAuthToken()`, etc. Remove the local ACCESS_TOKEN_KEY and USER_KEY constants.
   - In the `signOut` function, AFTER clearing SecureStore items, add: `const db = await initDatabase(); await clearCache(db);` to wipe cached events/wishlists on logout. Wrap in try/catch so logout doesn't fail if DB has issues.

2. Remove AsyncStorage package:
   - Run: `cd apps/gatherly-mobile && npm uninstall @react-native-async-storage/async-storage`
   - Verify no remaining imports: `grep -r "AsyncStorage\|async-storage" apps/gatherly-mobile/app/ apps/gatherly-mobile/contexts/ apps/gatherly-mobile/hooks/ apps/gatherly-mobile/lib/ --include="*.ts" --include="*.tsx"`
   - If any remaining references are found, fix them (there should be none based on codebase analysis — only EventsContext used it)

3. The `contexts/GiftsContext.tsx` file uses `localStorage` (web-only pattern). This context does not appear to be used in the app (not imported in _layout.tsx or any screens). Leave it as-is — it's dead code and not an AsyncStorage concern.
  </action>
  <verify>
    - `cd apps/gatherly-mobile && npx tsc --noEmit --pretty 2>&1 | head -30` compiles clean
    - `grep -r "AsyncStorage\|async-storage" apps/gatherly-mobile/app/ apps/gatherly-mobile/contexts/ apps/gatherly-mobile/hooks/ apps/gatherly-mobile/lib/ --include="*.ts" --include="*.tsx"` returns nothing
    - `grep "async-storage" apps/gatherly-mobile/package.json` returns nothing (package removed)
    - `grep "clearCache" apps/gatherly-mobile/app/contexts/AuthContext.tsx` shows cache clearing in signOut
  </verify>
  <done>
    - AuthContext uses SecureStorage abstraction for all token operations
    - Logout clears SQLite cache (events + wishlists)
    - @react-native-async-storage/async-storage removed from package.json
    - Zero AsyncStorage references remain in app source code
  </done>
</task>

</tasks>

<verification>
- `grep -rn "AsyncStorage\|async-storage" apps/gatherly-mobile/app/ apps/gatherly-mobile/contexts/ apps/gatherly-mobile/hooks/ apps/gatherly-mobile/lib/ --include="*.ts" --include="*.tsx"` returns zero results
- `grep "async-storage" apps/gatherly-mobile/package.json` returns nothing
- App compiles without type errors
- EventsContext loads from SQLite on cold start, caches to SQLite after API fetch
- Logging out clears the SQLite cache
</verification>

<success_criteria>
- AsyncStorage is completely removed (package and all imports)
- Events are cached in SQLite, loaded from SQLite on cold start
- Auth tokens continue using SecureStore (same keys, sessions preserved)
- Logout clears cached data from SQLite
</success_criteria>

<output>
After completion, create `.planning/phases/19-offline-storage-strategy-sqlite-migration/19-02-SUMMARY.md`
</output>
