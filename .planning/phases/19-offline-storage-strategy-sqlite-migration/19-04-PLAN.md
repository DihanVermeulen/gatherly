---
phase: 19-offline-storage-strategy-sqlite-migration
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/gatherly-mobile/app/_layout.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "After sign-out, the Events screen shows no events from the previous session"
    - "After signing in with a different account, only that account's events are visible"
    - "EventsContext loads fresh from SQLite (empty after clearCache) on each new login"
  artifacts:
    - path: "apps/gatherly-mobile/app/_layout.tsx"
      provides: "EventsProvider scoped to authenticated guard so it mounts/unmounts with session"
      contains: "EventsProvider inside Stack.Protected guard={!!session}"
  key_links:
    - from: "apps/gatherly-mobile/app/_layout.tsx"
      to: "apps/gatherly-mobile/app/contexts/EventsContext.tsx"
      via: "EventsProvider remounts on session change (inside guard)"
      pattern: "Stack\\.Protected.*guard=\\{!!session\\}[\\s\\S]*?EventsProvider"
---

<objective>
Scope EventsProvider inside the authenticated guard in _layout.tsx so it mounts fresh on each login and unmounts on sign-out, naturally resetting all in-memory event state without any cross-context wiring.

Purpose: EventsProvider currently mounts once outside the auth guard and its useReducer state holds the previous user's events in memory across sign-out/sign-in cycles. SQLite is cleared correctly by clearCache() in signOut, but the UI reads in-memory state and shows the prior user's events until the API fetch completes.

Output: _layout.tsx restructured so EventsProvider wraps only the authenticated Stack.Protected block. Each new login gets a fresh EventsProvider instance with initialState (events: [], loading: false, error: null), which then loads from the now-empty SQLite cache.
</objective>

<execution_context>
@C:\Users\dihan\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\dihan\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/gatherly-mobile/app/_layout.tsx
@apps/gatherly-mobile/app/contexts/EventsContext.tsx
@apps/gatherly-mobile/app/contexts/AuthContext.tsx
@apps/gatherly-mobile/components/OfflineBanner.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move EventsProvider inside the authenticated Stack.Protected guard in _layout.tsx</name>
  <files>apps/gatherly-mobile/app/_layout.tsx</files>
  <action>
Restructure RootLayoutNav in _layout.tsx to move EventsProvider inside the authenticated guard block. The goal is for EventsProvider to mount when a session exists and unmount when the user signs out, so each new login starts with a blank EventsProvider state.

Current structure (simplified):
```
<GluestackUIProvider>
  <EventsProvider>           ← mounts once, never remounts
    <GestureHandlerRootView>
      <SafeAreaView>
        <OfflineBanner />
        <ThemeProvider>
          <Stack>
            <Stack.Protected guard={!!session}>   ← auth guard
              ... authenticated screens ...
            </Stack.Protected>
            <Stack.Protected guard={!session}>
              ... sign-in, register ...
            </Stack.Protected>
            <Stack.Screen name="join" ... />
          </Stack>
        </ThemeProvider>
      </SafeAreaView>
    </GestureHandlerRootView>
  </EventsProvider>
</GluestackUIProvider>
```

Required structure:
```
<GluestackUIProvider>
  <GestureHandlerRootView style={{ flex: 1 }}>
    <View className="h-full w-full bg-background-0">
      <SafeAreaView className="h-full w-full max-w-7xl mx-auto bg-background-0" edges={["top"]}>
        <>
          <OfflineBanner />
          <ThemeProvider value={...}>
            <Stack>
              <Stack.Protected guard={!!session}>
                <EventsProvider>              ← scoped inside authenticated guard
                  <Stack.Screen name="(tabs)" options={{ headerShown: false }} />
                  <Stack.Screen name="edit-event" options={{ headerShown: false }} />
                  <Stack.Screen name="manage-exclusions" options={{ headerShown: false }} />
                  <Stack.Screen name="event-details" options={{ headerShown: false }} />
                  <Stack.Screen name="my-wishlist" options={{ headerShown: false }} />
                  <Stack.Screen name="edit-wishlist-item" options={{ headerShown: false }} />
                  <Stack.Screen name="modal" options={{ presentation: "modal" }} />
                </EventsProvider>
              </Stack.Protected>
              <Stack.Protected guard={!session}>
                <Stack.Screen name="sign-in" options={{ headerShown: false }} />
                <Stack.Screen name="register" options={{ headerShown: false }} />
              </Stack.Protected>
              <Stack.Screen name="join" options={{ headerShown: false, gestureEnabled: false }} />
            </Stack>
          </ThemeProvider>
        </>
      </SafeAreaView>
    </View>
  </GestureHandlerRootView>
</GluestackUIProvider>
```

Key constraints:
- OfflineBanner stays OUTSIDE EventsProvider — it only depends on useNetworkStatus (confirmed: no EventsContext usage)
- The join screen stays OUTSIDE EventsProvider — it is a public route and does not call useEvents()
- EventsProvider must remain INSIDE DatabaseProvider (DatabaseProvider is in RootLayout, wrapping RootLayoutNav, so useDatabase() is available to EventsProvider wherever it sits within RootLayoutNav)
- Do NOT move DatabaseProvider — it is already correctly positioned in RootLayout outside RootLayoutNav
- Do NOT modify EventsContext.tsx or AuthContext.tsx — the fix is layout-only
- Keep all existing Stack.Screen entries exactly as they are (options, names, guard assignments)
  </action>
  <verify>
1. TypeScript compiles without errors: `cd /c/repos/gatherly && pnpm check-types 2>&1 | grep -E "(error|Error)" | head -20`
2. Confirm EventsProvider is inside the authenticated guard: `grep -n "EventsProvider\|Stack.Protected\|guard=" apps/gatherly-mobile/app/_layout.tsx`
3. Confirm OfflineBanner is outside EventsProvider (appears before EventsProvider in the tree): verify grep output shows OfflineBanner before EventsProvider
  </verify>
  <done>
_layout.tsx restructured with EventsProvider nested inside Stack.Protected guard={!!session}. TypeScript reports no new errors in _layout.tsx. grep confirms EventsProvider is a child of the authenticated Stack.Protected block, not a sibling wrapping the entire Stack. OfflineBanner remains outside EventsProvider.
  </done>
</task>

</tasks>

<verification>
Manual sign-out/sign-in flow:
1. Sign in as User A — events load normally
2. Sign out — Events screen should be empty (EventsProvider unmounted and remounted with initialState)
3. Sign in as User B — only User B's events appear, no User A events visible at any point

TypeScript check: `cd /c/repos/gatherly && pnpm check-types`
</verification>

<success_criteria>
- Signing out clears in-memory event state — no previous user's events visible after logout
- Signing in with a different account shows only that account's events
- OfflineBanner continues to appear/disappear correctly based on network status
- No TypeScript errors introduced in _layout.tsx
- UAT Test 6 passes: "Sign out clears cached data"
</success_criteria>

<output>
After completion, create `.planning/phases/19-offline-storage-strategy-sqlite-migration/19-04-SUMMARY.md`
</output>
