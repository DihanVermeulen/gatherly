---
phase: 14-edit-event-screen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/gatherly-mobile/app/api/invites.ts
  - apps/gatherly-mobile/app/api/index.ts
  - apps/gatherly-mobile/app/manage-exclusions.tsx
  - apps/gatherly-mobile/app/_layout.tsx
autonomous: true

must_haves:
  truths:
    - "Invites API module exists and can create/list/delete invites"
    - "Manage Exclusions screen renders with tap-to-select-pair UI"
    - "manage-exclusions route is registered in the root Stack navigator"
  artifacts:
    - path: "apps/gatherly-mobile/app/api/invites.ts"
      provides: "Invites API client"
      exports: ["invitesApi", "Invite"]
    - path: "apps/gatherly-mobile/app/manage-exclusions.tsx"
      provides: "Manage Exclusions screen"
      min_lines: 80
  key_links:
    - from: "apps/gatherly-mobile/app/api/invites.ts"
      to: "apps/gatherly-mobile/app/api/client.ts"
      via: "import apiClient"
      pattern: "import.*client"
    - from: "apps/gatherly-mobile/app/manage-exclusions.tsx"
      to: "apps/gatherly-mobile/app/contexts/EventsContext"
      via: "useEvents hook"
      pattern: "useEvents"
    - from: "apps/gatherly-mobile/app/_layout.tsx"
      to: "manage-exclusions"
      via: "Stack.Screen registration"
      pattern: 'name="manage-exclusions"'
---

<objective>
Create the supporting infrastructure for the Edit Event screen: the invites API module, the Manage Exclusions sub-screen, and route registration. Also install react-qr-code.

Purpose: Plan 02 (edit-event.tsx overhaul) depends on these files existing. Splitting them out keeps Plan 02 focused on a single file.
Output: `api/invites.ts`, `manage-exclusions.tsx`, updated `_layout.tsx` and `api/index.ts`, react-qr-code installed.
</objective>

<execution_context>
@C:\Users\dihan\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\dihan\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-edit-event-screen/14-CONTEXT.md
@.planning/phases/14-edit-event-screen/14-RESEARCH.md
@apps/gatherly-mobile/app/api/events.ts
@apps/gatherly-mobile/app/api/index.ts
@apps/gatherly-mobile/app/api/client.ts
@apps/gatherly-mobile/app/_layout.tsx
@apps/gatherly-mobile/app/event-details.tsx
@apps/gatherly-mobile/screen-templates/Edit.png
@apps/gatherly/src/pages/events/invites.tsx
@apps/gatherly/src/pages/events/edit.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install react-qr-code and create invites API module</name>
  <files>
    apps/gatherly-mobile/app/api/invites.ts
    apps/gatherly-mobile/app/api/index.ts
  </files>
  <action>
1. Install react-qr-code in the mobile app:
   ```bash
   cd apps/gatherly-mobile && npm install react-qr-code
   ```
   (Use `npm install` NOT pnpm — pnpm has path length issues in this mobile app per project decisions.)

2. Create `apps/gatherly-mobile/app/api/invites.ts` following the exact pattern from `api/events.ts`:
   - Import `apiClient` from `./client`
   - Export `Invite` type:
     ```typescript
     export type Invite = {
       id: number;
       event_id: number;
       email: string | null;
       invite_url: string;
       magic_link_url: string;
       status: 'pending' | 'accepted' | 'expired';
       participant_name?: string;
       created_at: string;
       expires_at: string;
     };
     ```
   - Export `invitesApi` object with:
     - `create(eventId: string, email?: string)` — POST `/api/events/${eventId}/invites` body `{ email }`, returns `Invite`
     - `list(eventId: string)` — GET `/api/events/${eventId}/invites`, returns `{ invites: Invite[] }`
     - `delete(eventId: string, inviteId: number)` — DELETE `/api/events/${eventId}/invites/${inviteId}`
   - All methods: `.then(r => r.data)` pattern matching existing API modules

3. Update `apps/gatherly-mobile/app/api/index.ts` — add re-exports:
   ```typescript
   export { invitesApi } from "./invites";
   export type { Invite } from "./invites";
   ```
  </action>
  <verify>
    - `ls apps/gatherly-mobile/node_modules/react-qr-code/` confirms package installed
    - File `apps/gatherly-mobile/app/api/invites.ts` exists with create/list/delete methods
    - File `apps/gatherly-mobile/app/api/index.ts` re-exports invitesApi
  </verify>
  <done>Invites API module exists with typed create/list/delete methods; react-qr-code is installed</done>
</task>

<task type="auto">
  <name>Task 2: Create Manage Exclusions screen and register route</name>
  <files>
    apps/gatherly-mobile/app/manage-exclusions.tsx
    apps/gatherly-mobile/app/_layout.tsx
  </files>
  <action>
1. **Before implementing:** Read `apps/api/src/routes/events.ts` and verify the PUT `/api/events/:id` endpoint accepts and persists a `couples` field in the request body. Search for how `couples` is handled in the update route. If the PUT endpoint does NOT handle `couples`, note it and use a workaround (e.g., direct couple API if one exists, or add a TODO comment). Do NOT silently assume it works.

2. Create `apps/gatherly-mobile/app/manage-exclusions.tsx`:
   - Uses Expo Router: `useLocalSearchParams<{ id: string }>()` and `useRouter()`
   - Gets event from EventsContext via `import { useEvents } from './contexts/EventsContext'` (relative path — `manage-exclusions.tsx` lives at `app/manage-exclusions.tsx` and `EventsContext.tsx` lives at `app/contexts/EventsContext.tsx`)
   - Uses `eventsApi` from `./api/events` for saving couples

   **State:**
   - `couples: string[][]` — initialized from `event.couples`
   - `firstSelected: string | null` — tracks first person tapped in pair selection
   - `saving: boolean` — loading state when saving

   **UI layout (match Edit.png style / GlueStack components):**
   - Header: back arrow (ArrowLeft from lucide-react-native) + "Manage Exclusions" title
   - Info text: "Tap two participants to create an exclusion pair. Excluded pairs will not be assigned to buy for each other."
   - Participant grid: render `event.people` as tappable chips/cards
     - Default state: rounded pill with avatar initials + name
     - Selected state (firstSelected === name): highlighted border (teal/primary), filled background
     - When second person tapped: add `[firstSelected, secondPerson]` to couples, clear `firstSelected`
     - Cannot select same person twice
   - Existing couples list: below the grid, show each couple as a row `"Person A & Person B"` with a trash/X icon to remove
   - Save logic: on back navigation OR explicit save button, call `eventsApi.update(id, { couples })` to persist the updated couples array (full array replace, per research)
   - Use GlueStack components: Text, Pressable, Avatar/AvatarFallbackText, Icon (Trash2, ArrowLeft from lucide-react-native), VStack, HStack, Divider

   **Important patterns:**
   - Couple deletion: filter the couple out of the array, send full updated array
   - Couple display uses participant NAMES (string[][]), not IDs
   - On mount, deep-copy couples from event to local state to avoid mutating context directly
   - On save, call `eventsApi.update(id, { couples: localCouples })` then `refreshEvents()` from EventsContext

3. Register the route in `apps/gatherly-mobile/app/_layout.tsx` (the **root** layout file, same file where `edit-event` is registered — NOT the tabs layout):
   - Inside the authenticated `Stack.Protected` block, add:
     ```tsx
     <Stack.Screen name="manage-exclusions" options={{ headerShown: false }} />
     ```
   - Place it after the existing `edit-event` Screen registration
  </action>
  <verify>
    - File `apps/gatherly-mobile/app/manage-exclusions.tsx` exists
    - Grep `app/_layout.tsx` for `manage-exclusions` returns a match (confirm it is the ROOT layout, not tabs layout)
    - The screen imports `useEvents` from `'./contexts/EventsContext'` (relative path, not bare `contexts/EventsContext`)
    - The screen imports from `expo-router` (NOT `@react-navigation/native`)
    - Confirmed that PUT `/api/events/:id` handles `couples` field (or documented workaround if it does not)
  </verify>
  <done>Manage Exclusions screen exists with tap-to-select-pair UI, couple list with delete, and save via API; route is registered in root Stack layout (app/_layout.tsx)</done>
</task>

</tasks>

<verification>
- `npm ls react-qr-code --prefix apps/gatherly-mobile` shows the package
- `apps/gatherly-mobile/app/api/invites.ts` exports `invitesApi` and `Invite` type
- `apps/gatherly-mobile/app/api/index.ts` re-exports invitesApi
- `apps/gatherly-mobile/app/manage-exclusions.tsx` uses Expo Router, renders participant chips, manages couples
- `apps/gatherly-mobile/app/_layout.tsx` (root layout) has `manage-exclusions` Stack.Screen
</verification>

<success_criteria>
- react-qr-code installed in apps/gatherly-mobile
- Invites API module with create/list/delete matching project patterns
- Manage Exclusions screen with tap-to-select-pair, couple list, delete, save via API
- Route registered in root layout Stack (app/_layout.tsx)
</success_criteria>

<output>
After completion, create `.planning/phases/14-edit-event-screen/14-01-SUMMARY.md`
</output>
