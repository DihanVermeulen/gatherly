---
phase: 02-wishlist-core
plan: 05
type: execute
wave: 5
depends_on: ["02-04"]
files_modified: []

autonomous: false

must_haves:
  truths:
    - "User visually confirms the wishlist page matches the gift-registry.html design"
    - "User confirms all CRUD operations work end-to-end"
    - "User confirms image compression produces acceptable quality"
  artifacts: []
  key_links: []
---

<objective>
Visual and functional verification of the complete wishlist feature.

Purpose: Human verification that the wishlist page looks correct, CRUD operations work end-to-end, and the user experience matches the design intent before marking Phase 2 complete.

Output: User approval or list of issues to address.
</objective>

<execution_context>
@C:\Users\dihan\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\dihan\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-wishlist-core/02-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete wishlist CRUD feature including:
- Backend API with ownership authorization
- Frontend API client and validation
- Image compression (800px, 80% quality)
- EventsContext state management
- Bottom drawer form for add/edit
- Personal carousel with swipe-to-delete
- Registry view grouped by participant
- Route at /events/:eventId/wishlist/:participantId
  </what-built>
  <how-to-verify>
1. Start both apps: `pnpm dev` from repo root
2. Create or use an existing event with 3+ participants
3. Navigate to the wishlist page for one participant
4. **Test Create:** Tap "Add" → fill form with name + image + description + URL + priority → Save → item appears in carousel
5. **Test Image:** Upload a large photo (5MB+) → verify it compresses (no huge delay) → preview shows correctly
6. **Test Edit:** Tap Edit on a card → verify form pre-fills → change name → Save → card updates
7. **Test Delete:** Swipe left on a card → delete button appears → tap delete → card removed
8. **Test Registry:** Scroll down to see other participants' wishlists grouped by name
9. **Visual check:** Compare layout with `apps/gatherly/docs/screen-templates/gift-registry.html` (open in browser for reference)
10. **Dark mode:** Toggle dark mode class on HTML element and verify colors match
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Human visual and functional verification of all Phase 2 success criteria.
</verification>

<success_criteria>
User approves the wishlist feature as matching design intent and functioning correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-wishlist-core/02-05-SUMMARY.md`
</output>
