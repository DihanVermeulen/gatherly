---
phase: 03-claiming-system
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - apps/gatherly/src/api/wishlists.ts
  - apps/gatherly/src/hooks/useWishlistMutations.ts
  - apps/gatherly/src/components/wishlist/WishlistRegistryItem.tsx
  - apps/gatherly/src/pages/events/wishlist.tsx
autonomous: true

must_haves:
  truths:
    - "Participant can tap 'I'll buy this' on an unclaimed item and see instant optimistic feedback"
    - "Participant can unclaim an item they previously claimed"
    - "Claimed-by-others items show grey 'Claimed' badge with no action available"
    - "Own wishlist items never show claim buttons (cannot claim own items)"
    - "If claim fails (409 conflict), UI rolls back to unclaimed state"
    - "Optimistic updates provide instant feedback without waiting for server"
  artifacts:
    - path: "apps/gatherly/src/hooks/useWishlistMutations.ts"
      provides: "useClaimWishlistItem and useUnclaimWishlistItem hooks"
      exports: ["useClaimWishlistItem", "useUnclaimWishlistItem"]
    - path: "apps/gatherly/src/api/wishlists.ts"
      provides: "claim() and unclaim() API functions"
      contains: "claim"
    - path: "apps/gatherly/src/components/wishlist/WishlistRegistryItem.tsx"
      provides: "Three-state claim button component"
      contains: "claimedByMe"
    - path: "apps/gatherly/src/pages/events/wishlist.tsx"
      provides: "Wired registry with claim/unclaim handlers"
      contains: "useClaimWishlistItem"
  key_links:
    - from: "apps/gatherly/src/pages/events/wishlist.tsx"
      to: "apps/gatherly/src/hooks/useWishlistMutations.ts"
      via: "hook invocation"
      pattern: "useClaimWishlistItem"
    - from: "apps/gatherly/src/hooks/useWishlistMutations.ts"
      to: "apps/gatherly/src/api/wishlists.ts"
      via: "mutationFn"
      pattern: "wishlistsApi\\.claim"
    - from: "apps/gatherly/src/components/wishlist/WishlistRegistryItem.tsx"
      to: "mutation state"
      via: "props"
      pattern: "claimedByMe|isPending"
---

<objective>
Wire frontend claiming UI with optimistic mutations, three-state button, and rollback on conflict.

Purpose: Participants need instant visual feedback when claiming/unclaiming gifts (CLAIM-01, CLAIM-02), with automatic rollback if the server rejects the claim (CLAIM-03). The button must show three distinct states: available, claimed-by-me, claimed-by-other.
Output: Working claim/unclaim flow from button tap through optimistic cache update to server confirmation.
</objective>

<execution_context>
@C:\Users\dihan\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\dihan\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-claiming-system/03-RESEARCH.md
@.planning/phases/03-claiming-system/03-01-SUMMARY.md
@apps/gatherly/src/hooks/useEventMutations.ts
@apps/gatherly/src/api/wishlists.ts
@apps/gatherly/src/components/wishlist/WishlistRegistryItem.tsx
@apps/gatherly/src/pages/events/wishlist.tsx
@apps/gatherly/src/api/events.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add claim/unclaim API functions and mutation hooks</name>
  <files>apps/gatherly/src/api/wishlists.ts, apps/gatherly/src/hooks/useWishlistMutations.ts</files>
  <action>
**wishlists.ts API client — add two new methods:**

```typescript
claim: async (eventId: string, wishlistId: number): Promise<{ success: boolean }> => {
  const response = await apiClient.post(`/api/events/${eventId}/wishlists/${wishlistId}/claim`);
  return response.data;
},

unclaim: async (eventId: string, wishlistId: number): Promise<{ success: boolean }> => {
  await apiClient.delete(`/api/events/${eventId}/wishlists/${wishlistId}/claim`);
  return { success: true };
},
```

**useWishlistMutations.ts — NEW FILE. Create two hooks following the useEventMutations.ts pattern exactly:**

`useClaimWishlistItem()`:
- `mutationKey: ["wishlist", "claim"]`
- `mutationFn: ({ eventId, wishlistId }) => wishlistsApi.claim(eventId, wishlistId)`
- `onMutate`: Cancel queries with key `["events"]`, snapshot previous, optimistically update the matching wishlist item in the events cache: set `isClaimed: true, claimedByMe: true`. Navigate into the events array, find the event by eventId, find the wishlist item by wishlistId within `event.wishlists`, and update it. Return `{ previous }`.
- `onError`: Rollback to previous snapshot.
- `onSettled`: Invalidate `["events"]` queries only when `queryClient.isMutating({ mutationKey: ["wishlist", "claim"] }) === 1` (prevents thrashing during concurrent mutations).

`useUnclaimWishlistItem()`:
- `mutationKey: ["wishlist", "unclaim"]`
- `mutationFn: ({ eventId, wishlistId }) => wishlistsApi.unclaim(eventId, wishlistId)`
- `onMutate`: Same cancel/snapshot pattern. Set `isClaimed: false, claimedByMe: false` on the matching item.
- `onError`: Rollback.
- `onSettled`: Invalidate `["events"]` with same `isMutating` guard using key `["wishlist", "unclaim"]`.

Type the mutation variables as `{ eventId: string; wishlistId: number }`.
Type the context as `{ previous: Event[] | undefined }`.
Import `Event` from `../api/events` and `wishlistsApi` from `../api/wishlists`.
  </action>
  <verify>
Run `cd C:/repos/gatherly && pnpm check-types` — no TypeScript errors.
Verify useWishlistMutations.ts exports both hooks.
  </verify>
  <done>
wishlists API client has claim() and unclaim() methods.
useClaimWishlistItem and useUnclaimWishlistItem hooks exist with optimistic updates and rollback.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update WishlistRegistryItem to three-state button and wire wishlist page</name>
  <files>apps/gatherly/src/components/wishlist/WishlistRegistryItem.tsx, apps/gatherly/src/pages/events/wishlist.tsx</files>
  <action>
**WishlistRegistryItem.tsx — Rewrite to support three states:**

Update the props type:
```typescript
type WishlistRegistryItemProps = {
  item: WishlistItem;
  onClaim?: () => void;
  onUnclaim?: () => void;
  isPending?: boolean;
};
```

Remove the `isClaimed` prop — read `item.isClaimed` and `item.claimedByMe` directly from the item.

Three rendering states:
1. `item.isClaimed && item.claimedByMe` — **Claimed by me**: Show the item normally (not greyed out) with a green "You're buying this" badge (CheckCircle icon) and an "Unclaim" text button below it. The unclaim button calls `onUnclaim`. If `isPending`, show a Loader2 spinner instead of the unclaim text and disable the button.
2. `item.isClaimed && !item.claimedByMe` — **Claimed by someone else**: Current greyed-out design with "Claimed" badge. No action button. Keep existing styling.
3. `!item.isClaimed` — **Available**: Current unclaimed design with "I'll buy this" button calling `onClaim`. If `isPending`, show Loader2 spinner inside the button and disable it.

Import `Loader2` from `lucide-react`.

**wishlist.tsx — Wire the mutation hooks:**

1. Import `useClaimWishlistItem` and `useUnclaimWishlistItem` from `hooks/useWishlistMutations`.
2. Call both hooks at the top of `WishlistPage` component: `const claimMutation = useClaimWishlistItem()` and `const unclaimMutation = useUnclaimWishlistItem()`.
3. In the registry section where `WishlistRegistryItem` is rendered (around line 441), replace the current props:
   - Remove `isClaimed={!!item.claimedBy}` (no longer a prop)
   - Remove `onClaim={undefined}`
   - Add: `onClaim={() => claimMutation.mutate({ eventId: eventId!, wishlistId: item.id })}`
   - Add: `onUnclaim={() => unclaimMutation.mutate({ eventId: eventId!, wishlistId: item.id })}`
   - Add: `isPending={claimMutation.isPending || unclaimMutation.isPending}`

Note: The `isPending` applies globally — could be refined per-item, but for simplicity checking the mutation's global pending state is fine. The optimistic update will handle the visual state immediately anyway.
  </action>
  <verify>
Run `cd C:/repos/gatherly && pnpm check-types` — no TypeScript errors.
Run `cd C:/repos/gatherly && pnpm build` — build succeeds.
Grep for `onClaim={undefined}` in wishlist.tsx — should return zero results (replaced with real handler).
  </verify>
  <done>
WishlistRegistryItem renders three distinct states: available, claimed-by-me, claimed-by-other.
Wishlist page wires claim/unclaim mutations with optimistic updates.
Build passes with no errors.
  </done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds
- `pnpm check-types` passes
- No `onClaim={undefined}` stub remaining
- WishlistRegistryItem handles all three states
- Optimistic cache updates in both hooks with rollback on error
</verification>

<success_criteria>
Tapping "I'll buy this" immediately shows "You're buying this" (optimistic).
Tapping "Unclaim" immediately reverts to available state (optimistic).
409 conflict from server rolls back the optimistic update.
Claimed-by-others items show grey badge with no action.
</success_criteria>

<output>
After completion, create `.planning/phases/03-claiming-system/03-02-SUMMARY.md`
</output>
