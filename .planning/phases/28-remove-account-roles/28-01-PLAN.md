---
phase: 28-remove-account-roles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/db/migrations/013-remove-role-from-users.sql
  - apps/api/src/services/tokenService.ts
  - apps/api/src/middleware/auth.ts
  - apps/api/src/middleware/requireOrganizer.ts
  - apps/api/src/routes/auth.ts
  - apps/api/src/routes/events.ts
  - apps/api/src/routes/magicLink.ts
  - apps/api/src/routes/users.ts
  - apps/gatherly-mobile/app/api/auth.ts
  - apps/gatherly-mobile/app/api/users.ts
  - apps/gatherly-mobile/app/event-details.tsx
  - apps/gatherly-mobile/app/polls.tsx
  - apps/gatherly-mobile/app/rsvp.tsx
  - apps/gatherly/src/api/auth.ts
  - apps/gatherly/src/pages/events/details.tsx
  - apps/gatherly/src/pages/events/index.tsx
autonomous: true

must_haves:
  truths:
    - "A newly registered user can create events immediately (no role restriction)"
    - "Magic-link-only sessions are correctly identified by participantId presence, not role"
    - "The users table has no role column"
    - "JWT tokens contain no role field"
    - "All existing tokens are invalidated (JWT_SECRET bumped)"
  artifacts:
    - path: "apps/api/src/db/migrations/013-remove-role-from-users.sql"
      provides: "Migration to drop role column"
      contains: "DROP COLUMN"
    - path: "apps/api/src/services/tokenService.ts"
      provides: "TokenPayload without role"
    - path: "apps/api/src/middleware/auth.ts"
      provides: "Request user type without role"
  key_links:
    - from: "apps/api/src/routes/events.ts"
      to: "req.user.participantId"
      via: "participant gate check"
      pattern: "participantId !== undefined"
    - from: "apps/gatherly-mobile/app/event-details.tsx"
      to: "user?.participantId"
      via: "isOrganizer derivation"
      pattern: "participantId === undefined"
---

<objective>
Remove the `role` field from the entire codebase: database column, JWT payload, TypeScript types, and all conditional checks. Replace `role`-based gates with `participantId`-based checks. Bump JWT_SECRET to force re-login.

Purpose: The role field is vestigial after Phase 27 introduced participantId as the correct session discriminant. Removing it simplifies auth logic and lets all registered users create events immediately.
Output: Migration SQL, updated backend services/routes/middleware, updated mobile and legacy web clients -- all role-free.
</objective>

<execution_context>
@C:\Users\dihan\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\dihan\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-remove-account-roles/28-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend role removal (migration + services + middleware + routes)</name>
  <files>
    apps/api/src/db/migrations/013-remove-role-from-users.sql
    apps/api/src/services/tokenService.ts
    apps/api/src/middleware/auth.ts
    apps/api/src/middleware/requireOrganizer.ts
    apps/api/src/routes/auth.ts
    apps/api/src/routes/events.ts
    apps/api/src/routes/magicLink.ts
    apps/api/src/routes/users.ts
  </files>
  <action>
    Follow the RESEARCH.md inventory exactly. Order: types first, then routes.

    1. Create migration `013-remove-role-from-users.sql`:
       ```sql
       ALTER TABLE users DROP COLUMN IF EXISTS role;
       ```

    2. `tokenService.ts`: Remove `role` from the `TokenPayload` interface (keep userId, email, participantId?, eventId?). Remove `role: "participant"` from the `generateParticipantTokens` payload object.

    3. `middleware/auth.ts`: Remove `role` from the Request user type declaration. Remove `role: payload.role` from both user assignment blocks (around lines 50 and 103).

    4. `middleware/requireOrganizer.ts`: Remove the commented-out role-check block and update the docstring. Keep the middleware functional -- it should still check that `!req.user.participantId` (which is the existing working check).

    5. `routes/auth.ts`:
       - Registration: Remove `role` from INSERT column list and VALUES. Remove `role` param from `generateTokens()` calls.
       - Login: Remove `role` from SELECT query. Remove `role` param from `generateTokens()` calls.
       - Refresh: Remove `role` from SELECT query. Remove `role` from `generateTokens()` calls and response body.
       - Remove `role: "participant"` from participant response body (line ~241).

    6. `routes/events.ts` (line 56-74): Replace `if (user.role === "participant")` with `if (user.participantId !== undefined)`. Keep the body of that branch unchanged.

    7. `routes/magicLink.ts`:
       - Remove `role` from SELECT query (line ~230).
       - Remove `role: matchedUser.role` from `generateTokens()` call (line ~254).
       - Remove `role: matchedUser.role` from response body (line ~271).
       - Remove `role: "participant"` from participant response body (line ~302).

    8. `routes/users.ts`:
       - Remove `role` from SELECT in GET /me (line ~19).
       - Remove `role: u.role` from response body in GET /me (line ~37).
       - Remove `role` from RETURNING in PUT /me (line ~60).
       - Remove `role: u.role` from response body in PUT /me (line ~73).

    9. Bump JWT_SECRET: Add a comment in `.env.example` noting the secret was rotated for Phase 28. The actual secret rotation is a manual step (user changes their .env value).

    After all edits, run `grep -rn "\.role" apps/api/src/ --include="*.ts" | grep -v node_modules | grep -v ".roles"` to confirm zero remaining `role` references in backend code (excluding unrelated words like "roles" in comments if any).
  </action>
  <verify>
    Run from repo root:
    - `cd apps/api && npx tsc --noEmit` -- zero TypeScript errors
    - `grep -rn "user\.role\|\.role ==\|role:" apps/api/src/ --include="*.ts" | grep -v node_modules | grep -v "// "` -- should return zero matches (no role references in active code)
  </verify>
  <done>
    All backend files compile without errors. No references to `role` remain in API source. Migration file exists. TokenPayload interface has only userId, email, participantId?, eventId?.
  </done>
</task>

<task type="auto">
  <name>Task 2: Client role removal (mobile + legacy web)</name>
  <files>
    apps/gatherly-mobile/app/api/auth.ts
    apps/gatherly-mobile/app/api/users.ts
    apps/gatherly-mobile/app/event-details.tsx
    apps/gatherly-mobile/app/polls.tsx
    apps/gatherly-mobile/app/rsvp.tsx
    apps/gatherly/src/api/auth.ts
    apps/gatherly/src/pages/events/details.tsx
    apps/gatherly/src/pages/events/index.tsx
  </files>
  <action>
    Follow the RESEARCH.md inventory exactly.

    **Mobile app (apps/gatherly-mobile/app):**

    1. `api/auth.ts`:
       - Remove `role` from the response type (line ~85).
       - Remove `role: "participant"` from participant shape type (line ~95).
       - Remove `role` from destructuring (line ~126-132) -- ensure the remaining destructured fields are still returned correctly.

    2. `api/users.ts`:
       - Remove `role: string` from `UserProfile` interface (line ~7).
       - Remove `role: string` from `updateMe` return type (line ~18).

    3. `event-details.tsx` (line ~369): Replace `user?.role === "organizer"` with `user?.participantId === undefined` for the isOrganizer check.

    4. `polls.tsx` (line ~27): Replace `user?.role === "organizer"` with `user?.participantId === undefined`.

    5. `rsvp.tsx` (line ~26): Replace `user?.role === "organizer"` with `user?.participantId === undefined`.

    **Legacy web app (apps/gatherly/src):**

    6. `api/auth.ts`:
       - Remove `role` from User type (line ~7).
       - Remove `role` from login/register response handling (line ~23).
       - Remove hardcoded `role: "participant"` in magic link flow (line ~74).

    7. `pages/events/details.tsx`: Replace three `user?.role === "participant"` checks (lines ~34, 54, 71) with `user?.participantId !== undefined`.

    8. `pages/events/index.tsx` (line ~33): Replace `user?.role === "participant"` with `user?.participantId !== undefined`.

    After all edits, run grep across both client apps to confirm zero remaining `role` references.
  </action>
  <verify>
    Run from repo root:
    - `grep -rn "\.role\b" apps/gatherly-mobile/app/ --include="*.ts" --include="*.tsx" | grep -v node_modules` -- zero matches
    - `grep -rn "\.role\b" apps/gatherly/src/ --include="*.ts" --include="*.tsx" | grep -v node_modules` -- zero matches
    - `cd apps/gatherly && npx tsc --noEmit` -- zero TypeScript errors (if tsconfig exists)
  </verify>
  <done>
    All client code compiles. No references to `role` remain in mobile or legacy web source. isOrganizer checks use participantId-based logic. User types have no role field.
  </done>
</task>

</tasks>

<verification>
Full codebase sweep for any remaining role references:
```bash
grep -rn "user\.role\|\.role ==\|role:" apps/api/src/ apps/gatherly-mobile/app/ apps/gatherly/src/ --include="*.ts" --include="*.tsx" | grep -v node_modules | grep -v "\.roles"
```
Expected: zero matches.

TypeScript compilation across all apps:
```bash
cd apps/api && npx tsc --noEmit
```

Migration file exists:
```bash
cat apps/api/src/db/migrations/013-remove-role-from-users.sql
```
</verification>

<success_criteria>
1. Zero references to `role` in API, mobile, or legacy web TypeScript source
2. TokenPayload has no role field
3. Users table migration drops role column
4. events.ts participant gate uses `participantId !== undefined`
5. Mobile isOrganizer checks use `participantId === undefined`
6. All apps compile without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/28-remove-account-roles/28-01-SUMMARY.md`
</output>
