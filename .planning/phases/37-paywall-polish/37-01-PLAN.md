---
phase: 37-paywall-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/gatherly-mobile/app/polls.tsx
  - apps/gatherly-mobile/app/potluck-setup.tsx
  - apps/gatherly-mobile/app/edit-event.tsx
  - apps/gatherly-mobile/app/modules-config.tsx
  - apps/gatherly-mobile/app/magic-link/[token].tsx
autonomous: true

must_haves:
  truths:
    - "A magic-link participant who triggers any paywall sees 'Ask your organiser to upgrade this event' copy with no upgrade CTA"
    - "If createPoll returns trial_limit_reached 403, the polls screen opens PaywallModal instead of showing a generic error"
    - "If createPotluckCategory returns trial_limit_reached 403, the potluck setup screen opens PaywallModal instead of showing a toast"
    - "If magic-link redemption fails with participant_cap_reached 403, the magic-link screen shows 'This event is full' with an explanation"
  artifacts:
    - path: "apps/gatherly-mobile/app/polls.tsx"
      provides: "isParticipant derivation + trial_limit_reached catch"
      contains: "isParticipant"
    - path: "apps/gatherly-mobile/app/potluck-setup.tsx"
      provides: "onTrialLimitReached prop on CategoryRow + isParticipant derivation"
      contains: "onTrialLimitReached"
    - path: "apps/gatherly-mobile/app/edit-event.tsx"
      provides: "isParticipant derivation passed to PaywallModal"
      contains: "isParticipant"
    - path: "apps/gatherly-mobile/app/modules-config.tsx"
      provides: "useSession import + isParticipant derivation"
      contains: "useSession"
    - path: "apps/gatherly-mobile/app/magic-link/[token].tsx"
      provides: "event-full state + participant_cap_reached 403 handling + render branch"
      contains: "event-full"
  key_links:
    - from: "polls.tsx catch block"
      to: "PaywallModal"
      via: "setShowPaywall(true) on trial_limit_reached"
      pattern: "trial_limit_reached.*setShow"
    - from: "potluck-setup.tsx CategoryRow"
      to: "PotluckSetupScreen PaywallModal"
      via: "onTrialLimitReached callback prop"
      pattern: "onTrialLimitReached"
    - from: "magic-link/[token].tsx catch"
      to: "event-full render branch"
      via: "setState('event-full') on 403 participant_cap_reached"
      pattern: "event-full"
---

<objective>
Close three broken/partial paywall integration chains from the v2.3 audit: (1) derive isParticipant from useSession() at all four PaywallModal call sites so magic-link participants see "Ask your organiser" copy, (2) intercept trial_limit_reached 403 errors in polls and potluck-setup to open PaywallModal instead of showing generic errors, (3) handle participant_cap_reached 403 on magic-link redemption with a specific "This event is full" screen.

Purpose: Without these fixes, the participant paywall variant is unreachable, API 403s are silently swallowed, and magic-link users hitting the cap see a confusing generic error.
Output: Five modified files with correct error handling and participant detection.
</objective>

<execution_context>
@C:\Users\dihan\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\dihan\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-paywall-polish/37-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive isParticipant at all four PaywallModal call sites and intercept trial_limit_reached 403</name>
  <files>
    apps/gatherly-mobile/app/polls.tsx
    apps/gatherly-mobile/app/potluck-setup.tsx
    apps/gatherly-mobile/app/edit-event.tsx
    apps/gatherly-mobile/app/modules-config.tsx
  </files>
  <action>
    **All four files — isParticipant derivation:**

    The pattern is: `const isParticipant = user?.participantId !== undefined;` — then pass `isParticipant={isParticipant}` to the PaywallModal component.

    1. **polls.tsx** (line ~31 area already has `const isOrganizer = user?.participantId === undefined`):
       - Add `const isParticipant = !isOrganizer;` right after the existing isOrganizer line
       - In the `handleCreatePoll` catch block (line ~98), change from bare `catch` to `catch (err: unknown)`. Inspect `(err as { response?: { data?: { error?: string } } })?.response?.data?.error`. If `=== 'trial_limit_reached'`, call `setShowCreateModal(false); setShowPaywall(true);` instead of `setCreateError(...)`. Else keep the existing generic error.
       - At the PaywallModal JSX (line ~472), change `isParticipant={false}` to `isParticipant={isParticipant}`

    2. **potluck-setup.tsx**:
       - Find the `CategoryRow` component props type (around line ~100). Add `onTrialLimitReached?: () => void` to its props interface/destructuring.
       - In `CategoryRow`'s `handleNameBlur` catch block (line ~200), change from bare `catch` to `catch (err: unknown)`. Inspect error. If `trial_limit_reached`, call `onTrialLimitReached?.()` instead of `showToast(...)`. Else keep existing toast.
       - In the parent `PotluckSetupScreen`, where `CategoryRow` is rendered, pass `onTrialLimitReached={() => setShowPaywall(true)}` prop.
       - At line ~506 area, `isOrganizer` already exists. Add `const isParticipant = !isOrganizer;` after it.
       - At the PaywallModal JSX (line ~831), change `isParticipant={false}` to `isParticipant={isParticipant}`

    3. **edit-event.tsx** (line ~110 already has `const { user } = useSession()`):
       - Add `const isParticipant = user?.participantId !== undefined;` near line 110 after the existing user destructure
       - At the PaywallModal JSX (line ~1038), change `isParticipant={false}` to `isParticipant={isParticipant}`

    4. **modules-config.tsx**:
       - Add import: `import { useSession } from "./contexts/AuthContext";` (verify exact relative path matches other files in same directory)
       - Inside the component function (around line ~125), add `const { user } = useSession(); const isParticipant = user?.participantId !== undefined;`
       - At the PaywallModal JSX (line ~379), change `isParticipant={false}` to `isParticipant={isParticipant}`

    **Important:** Do NOT change `isOrganizer` logic — it already works. Only ADD the `isParticipant` derivation and thread it to PaywallModal.
  </action>
  <verify>
    Run `cd apps/gatherly-mobile && npx tsc --noEmit` — zero type errors.
    Grep: `grep -rn "isParticipant={false}" apps/gatherly-mobile/app/` — should return zero matches (all four hardcoded values replaced).
    Grep: `grep -rn "isParticipant={isParticipant}" apps/gatherly-mobile/app/` — should return 4 matches (one per file).
    Grep: `grep -n "trial_limit_reached" apps/gatherly-mobile/app/polls.tsx apps/gatherly-mobile/app/potluck-setup.tsx` — should return matches in both files.
  </verify>
  <done>
    All four PaywallModal call sites pass isParticipant derived from useSession(). The polls.tsx handleCreatePoll catch opens PaywallModal on trial_limit_reached. The potluck-setup.tsx CategoryRow bubbles trial_limit_reached up to parent via onTrialLimitReached callback which opens PaywallModal.
  </done>
</task>

<task type="auto">
  <name>Task 2: Handle participant_cap_reached 403 on magic-link redemption screen</name>
  <files>
    apps/gatherly-mobile/app/magic-link/[token].tsx
  </files>
  <action>
    1. **Extend MagicLinkState type** (line ~17): Add `| "event-full"` to the union type.

    2. **Update handleJoin catch** (line ~148): Change from:
       ```
       } catch (err: unknown) {
         const status = ...;
         if (status === 401 || status === 400) { setState("invalid"); }
         else { setState("error"); }
       }
       ```
       To also extract `errorCode` from `(err as { response?: { data?: { error?: string } } })?.response?.data?.error`. Add an `else if (status === 403 && errorCode === 'participant_cap_reached')` branch that calls `setState("event-full")`. Keep existing branches unchanged.

    3. **Update handleNameSubmit catch** (line ~182): Change bare `catch` to `catch (err: unknown)`. Add the same 403/participant_cap_reached check before falling through to `setState("error")`.

    4. **Add "event-full" render branch** in the `renderContent` function/switch. Use the same visual pattern as the `"invalid"` state (AlertTriangle icon if available, or the same icon used by "invalid" / "error" states). Render:
       - Icon: Use the same error/warning icon style as "invalid" state, but in amber (#f59e0b)
       - Heading: "This event is full"
       - Body text: "The organiser has reached the maximum number of participants for this event."
       - Button: "Go Back" that calls `router.back()` — there is nothing to retry since the cap is a hard limit until the organiser upgrades
       - Use the same layout/spacing as the existing "invalid" and "error" render branches for consistency

    **Important:** Do NOT change any other state transitions. Only add the new "event-full" variant.
  </action>
  <verify>
    Run `cd apps/gatherly-mobile && npx tsc --noEmit` — zero type errors.
    Grep: `grep -n "event-full" apps/gatherly-mobile/app/magic-link/\[token\].tsx` — should return matches for: type definition, setState call(s), and render branch.
    Grep: `grep -n "participant_cap_reached" apps/gatherly-mobile/app/magic-link/\[token\].tsx` — should return 2 matches (one in handleJoin, one in handleNameSubmit).
  </verify>
  <done>
    The magic-link screen handles participant_cap_reached 403 in both handleJoin and handleNameSubmit, showing a dedicated "This event is full" screen with "Go Back" button instead of a generic error.
  </done>
</task>

</tasks>

<verification>
1. `cd apps/gatherly-mobile && npx tsc --noEmit` passes with zero errors
2. `grep -rn "isParticipant={false}" apps/gatherly-mobile/` returns zero matches
3. `grep -rn "trial_limit_reached" apps/gatherly-mobile/app/polls.tsx apps/gatherly-mobile/app/potluck-setup.tsx` returns matches in both
4. `grep -rn "participant_cap_reached" apps/gatherly-mobile/app/magic-link/` returns 2 matches
5. `grep -rn "event-full" apps/gatherly-mobile/app/magic-link/` returns 3+ matches (type, setState, render)
</verification>

<success_criteria>
- All four PaywallModal call sites derive isParticipant from useSession() — no hardcoded false remains
- polls.tsx and potluck-setup.tsx intercept trial_limit_reached 403 and open PaywallModal
- magic-link/[token].tsx handles participant_cap_reached 403 with a dedicated "event-full" screen
- TypeScript compiles cleanly with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/37-paywall-polish/37-01-SUMMARY.md`
</output>
