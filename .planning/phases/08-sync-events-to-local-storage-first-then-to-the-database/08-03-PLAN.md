---
phase: 08-sync-events
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - apps/gatherly/src/components/SyncIndicator.tsx
  - apps/gatherly/src/layouts/MainLayout.tsx
autonomous: false

must_haves:
  truths:
    - "User sees a visual indicator when changes are syncing to the server"
    - "User sees an offline badge when network is unavailable"
    - "User sees a success state when all changes are synced"
    - "User can retry failed sync operations from the error state"
    - "Sync indicator is visible on all pages via the main layout"
  artifacts:
    - path: "apps/gatherly/src/components/SyncIndicator.tsx"
      provides: "Visual sync status badge component"
      exports: ["SyncIndicator"]
    - path: "apps/gatherly/src/layouts/MainLayout.tsx"
      provides: "Main layout with SyncIndicator integrated"
  key_links:
    - from: "apps/gatherly/src/components/SyncIndicator.tsx"
      to: "apps/gatherly/src/hooks/useSyncStatus.ts"
      via: "useSyncStatus() hook call"
      pattern: "useSyncStatus"
    - from: "apps/gatherly/src/layouts/MainLayout.tsx"
      to: "apps/gatherly/src/components/SyncIndicator.tsx"
      via: "SyncIndicator rendered in layout"
      pattern: "<SyncIndicator"
---

<objective>
Create a sync status indicator component and integrate it into the main layout so users always know the state of their data: synced, syncing, offline, or error with retry.

Purpose: Offline-first apps MUST provide clear visual feedback about data sync state. Without this, users cannot trust that their changes are saved. This is the trust layer for the offline-first architecture.

Output: SyncIndicator component rendered in MainLayout, visible on all pages.
</objective>

<execution_context>
@C:\Users\dihan\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\dihan\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-sync-events-to-local-storage-first-then-to-the-database/08-RESEARCH.md
@.planning/phases/08-sync-events-to-local-storage-first-then-to-the-database/08-02-SUMMARY.md
@apps/gatherly/src/hooks/useSyncStatus.ts
@apps/gatherly/src/layouts/MainLayout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SyncIndicator component and wire into MainLayout</name>
  <files>
    apps/gatherly/src/components/SyncIndicator.tsx
    apps/gatherly/src/layouts/MainLayout.tsx
  </files>
  <action>
1. Create `apps/gatherly/src/components/SyncIndicator.tsx`:
   - Import `useSyncStatus, SyncStatus` from `../hooks/useSyncStatus`
   - Import icons from `lucide-react`: `Cloud, CloudOff, Loader2, AlertTriangle, Check`
   - The component renders a small floating badge/pill at the bottom of the screen (above any bottom nav if present), showing one of four states:

   **Synced state** (status === 'synced'):
   - Show a subtle green pill with Check icon and text "All synced"
   - Auto-hide after 3 seconds using a `useState` + `useEffect` with timeout
   - Only show briefly after a mutation completes (transition from 'syncing' to 'synced')
   - When hidden, render nothing (null)

   **Syncing state** (status === 'syncing'):
   - Show a blue pill with Loader2 icon (animate-spin) and text "Syncing {mutatingCount} change(s)..."
   - Always visible while syncing

   **Offline state** (status === 'offline'):
   - Show a yellow/amber pill with CloudOff icon and text "Offline - changes saved locally"
   - Always visible while offline

   **Error state** (status === 'error'):
   - Show a red pill with AlertTriangle icon and text "Sync failed"
   - Include a "Retry" button that calls `retryFailedMutations()`
   - Always visible until retry succeeds or user dismisses

   Styling:
   - Position: `fixed bottom-20 left-1/2 -translate-x-1/2 z-50` (above potential bottom nav)
   - Pill shape: `rounded-full px-4 py-2 shadow-lg`
   - Use Tailwind dark mode classes for all states
   - Smooth transitions: `transition-all duration-300`
   - Text: `text-sm font-medium`
   - Each state has appropriate bg/text colors:
     - synced: `bg-green-100 text-green-700 dark:bg-green-900/50 dark:text-green-300`
     - syncing: `bg-blue-100 text-blue-700 dark:bg-blue-900/50 dark:text-blue-300`
     - offline: `bg-amber-100 text-amber-700 dark:bg-amber-900/50 dark:text-amber-300`
     - error: `bg-red-100 text-red-700 dark:bg-red-900/50 dark:text-red-300`

2. Update `apps/gatherly/src/layouts/MainLayout.tsx`:
   - Read the file first to understand current structure
   - Add `import { SyncIndicator } from "components/SyncIndicator"`
   - Render `<SyncIndicator />` inside the layout, after the main content outlet but before any closing wrapper divs
   - It should be a sibling to the Outlet, not wrapping it
   - Since SyncIndicator uses fixed positioning, placement in DOM is flexible - just needs to be inside the provider tree
  </action>
  <verify>
    - `pnpm --filter gatherly build` compiles without errors
    - SyncIndicator.tsx exports SyncIndicator component
    - MainLayout.tsx imports and renders SyncIndicator
    - Grep confirms: `<SyncIndicator` appears in MainLayout.tsx
  </verify>
  <done>SyncIndicator component exists with four visual states (synced, syncing, offline, error). It is rendered in MainLayout and visible on all pages. Error state includes retry button.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete offline-first sync architecture for events:
    1. TanStack Query v5 with persistent cache (survives page reload)
    2. Optimistic mutations for event create/update/delete (instant UI feedback)
    3. Automatic offline queuing (mutations pause when offline, resume when online)
    4. Migration from old localStorage format
    5. Visual sync status indicator (synced/syncing/offline/error with retry)
  </what-built>
  <how-to-verify>
    1. Start the API: `cd apps/api && pnpm dev`
    2. Start the frontend: `cd apps/gatherly && pnpm dev`
    3. Open http://localhost:3000/events in browser

    **Test 1 - Basic sync (online):**
    - Create a new event
    - Verify it appears immediately in the list
    - Verify the sync indicator briefly shows "Syncing..." then "All synced"
    - Refresh the page - event should persist (loaded from cache + API)

    **Test 2 - Offline behavior:**
    - Open browser DevTools > Network tab > check "Offline" checkbox
    - Create a new event
    - Verify it appears immediately (optimistic update)
    - Verify sync indicator shows "Offline - changes saved locally"
    - Uncheck "Offline" in DevTools
    - Verify sync indicator shows "Syncing..." then "All synced"
    - Refresh page - event should be there (was synced to server)

    **Test 3 - Page reload persistence:**
    - With API running, create an event
    - Close the browser tab completely
    - Reopen http://localhost:3000/events
    - Event should appear immediately from cache before API fetch completes

    **Test 4 - Backward compatibility:**
    - Navigate to an event edit page (/events/edit/:id)
    - Verify participants, couples, assignments still work
    - Navigate to wishlist page - verify it loads
    - Navigate to gifts page - verify it loads
  </how-to-verify>
  <resume-signal>Type "approved" if all four tests pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `pnpm --filter gatherly build` compiles
2. SyncIndicator renders correctly for all four states
3. Sync indicator is visible on all pages via MainLayout
4. Events persist across page reload via TanStack Query cache
5. Offline mutations queue and replay on reconnection
6. All existing pages work without modification (backward compatibility)
</verification>

<success_criteria>
- User sees sync status indicator on all pages
- Online: changes sync immediately with brief "synced" confirmation
- Offline: changes are queued locally with clear "offline" indicator
- Error: failed syncs show retry option
- All existing event pages (list, edit, gifts, wishlist, invites) work without modification
</success_criteria>

<output>
After completion, create `.planning/phases/08-sync-events-to-local-storage-first-then-to-the-database/08-03-SUMMARY.md`
</output>
