---
phase: 09-magic-link-access
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - apps/api/src/middleware/requireOrganizer.ts
  - apps/api/src/routes/events.ts
  - apps/api/src/routes/invites.ts
  - apps/api/src/routes/gifts.ts
autonomous: true

must_haves:
  truths:
    - "Participant-role users receive 403 when attempting to create, update, or delete events"
    - "Participant-role users receive 403 when attempting to manage participants, couples, or assignments"
    - "Participant-role users receive 403 when attempting to manage invites"
    - "Participant-role users CAN read events (GET /events, GET /events/:id)"
    - "Participant-role users CAN read and claim/unclaim gifts"
    - "Organizer-role users are unaffected by the new middleware"
  artifacts:
    - path: "apps/api/src/middleware/requireOrganizer.ts"
      provides: "Middleware that returns 403 if req.user.role !== 'organizer'"
      exports: ["requireOrganizer"]
    - path: "apps/api/src/routes/events.ts"
      provides: "Event mutation routes protected by requireOrganizer"
      contains: "requireOrganizer"
    - path: "apps/api/src/routes/invites.ts"
      provides: "Invite management routes protected by requireOrganizer"
      contains: "requireOrganizer"
  key_links:
    - from: "apps/api/src/middleware/requireOrganizer.ts"
      to: "apps/api/src/middleware/auth.ts"
      via: "Reads req.user.role set by authenticateJWT"
      pattern: "req\\.user.*role.*organizer"
    - from: "apps/api/src/routes/events.ts"
      to: "apps/api/src/middleware/requireOrganizer.ts"
      via: "Applied after authenticateJWT on mutation routes"
      pattern: "authenticateJWT.*requireOrganizer"
---

<objective>
Add requireOrganizer middleware and protect all admin-only API routes.

Purpose: Participants authenticated via magic links must not be able to create/edit/delete events, manage participants/couples/assignments, or manage invites. This plan adds server-side enforcement so that even if the frontend UI is bypassed, the API rejects unauthorized mutations.

Output: requireOrganizer middleware, protected event mutation routes, protected invite routes
</objective>

<execution_context>
@C:\Users\dihan\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\dihan\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-magic-link-access-for-invited-members-with-restricted-permissions/09-RESEARCH.md
@.planning/phases/09-magic-link-access-for-invited-members-with-restricted-permissions/09-01-SUMMARY.md
@apps/api/src/middleware/auth.ts
@apps/api/src/routes/events.ts
@apps/api/src/routes/invites.ts
@apps/api/src/routes/gifts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create requireOrganizer middleware</name>
  <files>
    apps/api/src/middleware/requireOrganizer.ts
  </files>
  <action>
    Create `apps/api/src/middleware/requireOrganizer.ts`:

    ```typescript
    import { Request, Response, NextFunction } from 'express';

    /**
     * Middleware that requires the authenticated user to have the 'organizer' role.
     * Must be used AFTER authenticateJWT middleware (req.user must be set).
     * Returns 403 if user is not an organizer.
     */
    export function requireOrganizer(
      req: Request,
      res: Response,
      next: NextFunction
    ): void {
      if (!req.user) {
        res.status(401).json({ error: 'Authentication required' });
        return;
      }

      if (req.user.role !== 'organizer') {
        res.status(403).json({ error: 'Organizer access required' });
        return;
      }

      next();
    }
    ```

    This middleware is intentionally simple. It checks req.user (set by authenticateJWT) and rejects non-organizers with 403. It does NOT check specific resource ownership - that is a future concern.
  </action>
  <verify>
    - File exists at `apps/api/src/middleware/requireOrganizer.ts`
    - Exports `requireOrganizer` function
    - Returns 403 with `{ error: 'Organizer access required' }` for non-organizer role
    - Returns 401 if req.user is not set
    - `pnpm check-types` passes
  </verify>
  <done>
    requireOrganizer middleware created, returns 403 for participant-role users, 401 for unauthenticated
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply requireOrganizer to event and invite mutation routes</name>
  <files>
    apps/api/src/routes/events.ts
    apps/api/src/routes/invites.ts
    apps/api/src/routes/gifts.ts
  </files>
  <action>
    1. In `apps/api/src/routes/events.ts`:

    a. Add import: `import { requireOrganizer } from "../middleware/requireOrganizer";`

    b. Add `requireOrganizer` as middleware AFTER `authenticateJWT` on these routes:
    - POST / (create event): `authenticateJWT, requireOrganizer, asyncHandler(...)`
    - PUT /:id (update event): `authenticateJWT, requireOrganizer, asyncHandler(...)`
    - DELETE /:id (delete event): `authenticateJWT, requireOrganizer, asyncHandler(...)`
    - POST /:id/participants: `authenticateJWT, requireOrganizer, async (...)`
    - DELETE /:id/participants/:name: `authenticateJWT, requireOrganizer, async (...)`
    - POST /:id/couples: `authenticateJWT, requireOrganizer, async (...)`
    - DELETE /:id/couples/:coupleId: `authenticateJWT, requireOrganizer, async (...)`
    - POST /:id/generate: `authenticateJWT, requireOrganizer, async (...)`
    - GET /:id/codes: `authenticateJWT, requireOrganizer, async (...)`

    c. Do NOT add requireOrganizer to:
    - GET / (list events) - uses optionalAuth, accessible to all
    - GET /:id (event details) - uses optionalAuth, accessible to all

    2. In `apps/api/src/routes/invites.ts`:

    a. Add import: `import { requireOrganizer } from "../middleware/requireOrganizer";`

    b. Add `requireOrganizer` after `authenticateJWT` on:
    - POST /events/:eventId/invites (create invite): `authenticateJWT, requireOrganizer, asyncHandler(...)`
    - GET /events/:eventId/invites (list invites): `authenticateJWT, requireOrganizer, asyncHandler(...)`
    - DELETE /events/:eventId/invites/:inviteId (revoke invite): `authenticateJWT, requireOrganizer, asyncHandler(...)`

    c. Do NOT add requireOrganizer to:
    - POST /invites/validate (public, rate limited)
    - POST /invites/:code/accept (public, rate limited)

    3. In `apps/api/src/routes/gifts.ts`:

    Do NOT add requireOrganizer to gift routes. Participants should be able to:
    - GET /:id/gifts (view gifts) - already uses optionalAuth
    - POST /:id/gifts (add gift) - authenticated participants can add
    - POST /:id/gifts/:giftId/claim (claim) - authenticated participants can claim
    - DELETE /:id/gifts/:giftId/claim (unclaim) - authenticated participants can unclaim

    Leave gift routes unchanged. The existing authenticateJWT on mutations is sufficient.
  </action>
  <verify>
    - `pnpm check-types` passes
    - Event creation routes (POST, PUT, DELETE) have both authenticateJWT and requireOrganizer
    - Participant management routes have requireOrganizer
    - Couple management routes have requireOrganizer
    - Assignment generation and codes routes have requireOrganizer
    - Invite management routes (create, list, delete) have requireOrganizer
    - Event read routes (GET /, GET /:id) do NOT have requireOrganizer
    - Gift routes do NOT have requireOrganizer
    - Public invite routes (validate, accept) do NOT have requireOrganizer
  </verify>
  <done>
    All event mutation routes, participant/couple/assignment management routes, and invite management routes are protected by requireOrganizer. Read routes and gift routes remain accessible to authenticated participants.
  </done>
</task>

</tasks>

<verification>
- requireOrganizer middleware exists and returns 403 for non-organizer role
- POST/PUT/DELETE /events/* routes require organizer role
- POST/DELETE participants, couples routes require organizer role
- POST generate, GET codes require organizer role
- POST/GET/DELETE invites management routes require organizer role
- GET /events and GET /events/:id remain accessible to all authenticated users
- Gift CRUD and claim/unclaim remain accessible to all authenticated users
- `pnpm check-types` passes
</verification>

<success_criteria>
- Participant-role JWT holders get 403 on all event mutation endpoints
- Organizer-role JWT holders are unaffected
- Gift operations remain open to all authenticated users
- Event read operations remain open to all
</success_criteria>

<output>
After completion, create `.planning/phases/09-magic-link-access-for-invited-members-with-restricted-permissions/09-03-SUMMARY.md`
</output>
