---
phase: 21-nextjs-website
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - apps/web/app/page.tsx
  - apps/web/components/video-modal.tsx
autonomous: true

must_haves:
  truths:
    - "Visiting http://localhost:3001 shows the hero section with 'Events Made Effortless' heading and teal italic styling"
    - "The hero has two CTA buttons: 'Start Planning Now' and 'Watch Demo'"
    - "Clicking 'Watch Demo' opens a full-screen video modal overlay"
    - "The hero right side shows a plant image composition and floating event card"
    - "A 3-column feature grid shows Gift Exchange, Potluck Planner, Photo Gallery with teal icons"
    - "A dark teal mobile CTA section shows App Store and Google Play buttons with a phone mockup"
  artifacts:
    - path: "apps/web/app/page.tsx"
      provides: "Home page matching Home.png template"
      min_lines: 120
    - path: "apps/web/components/video-modal.tsx"
      provides: "Reusable video modal component used by Watch Demo button"
  key_links:
    - from: "apps/web/app/page.tsx"
      to: "apps/web/components/video-modal.tsx"
      via: "import and render with triggerLabel prop"
      pattern: "VideoModal"
---

<objective>
Build the Home page (apps/web/app/page.tsx) matching Home.png pixel-for-pixel: hero section, 3-column feature grid, dark teal mobile CTA, and the VideoModal component used by the "Watch Demo" button.

Purpose: The home page is the primary marketing surface — it must match the template exactly for layout, color, typography, and composition.
Output: Complete Home page and VideoModal component. All sections visible at http://localhost:3001.
</objective>

<execution_context>
@C:\Users\dihan\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\dihan\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-nextjs-website/21-RESEARCH.md
@.planning/phases/21-nextjs-website/21-CONTEXT.md
@apps/web/screen-templates/Home.png
@.planning/phases/21-nextjs-website/21-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: VideoModal component</name>
  <files>apps/web/components/video-modal.tsx</files>
  <action>
Create the VideoModal client component. It renders a trigger button and a full-screen modal overlay with an iframe/video player. The modal closes on backdrop click or Escape key.

```typescript
'use client'
import { useState, useEffect } from 'react'
import { X } from 'lucide-react'

interface VideoModalProps {
  triggerLabel: string
  triggerClassName?: string
}

export function VideoModal({ triggerLabel, triggerClassName }: VideoModalProps) {
  const [open, setOpen] = useState(false)
  // TODO: replace with real demo video URL
  const videoUrl = ''

  useEffect(() => {
    const handleKey = (e: KeyboardEvent) => {
      if (e.key === 'Escape') setOpen(false)
    }
    if (open) document.addEventListener('keydown', handleKey)
    return () => document.removeEventListener('keydown', handleKey)
  }, [open])

  return (
    <>
      <button
        onClick={() => setOpen(true)}
        className={triggerClassName}
      >
        {triggerLabel}
      </button>

      {open && (
        <div
          className="fixed inset-0 bg-black/70 z-50 flex items-center justify-center p-4"
          onClick={() => setOpen(false)}
        >
          <div
            className="relative w-full max-w-3xl aspect-video bg-black rounded-xl overflow-hidden shadow-2xl"
            onClick={e => e.stopPropagation()}
          >
            <button
              onClick={() => setOpen(false)}
              className="absolute top-3 right-3 z-10 bg-black/50 hover:bg-black/70 text-white rounded-full p-1.5 transition-colors"
              aria-label="Close video"
            >
              <X size={18} />
            </button>
            {videoUrl ? (
              <iframe
                src={videoUrl}
                className="w-full h-full"
                allowFullScreen
                allow="autoplay; encrypted-media"
              />
            ) : (
              <div className="w-full h-full flex items-center justify-center">
                <p className="text-white text-center text-lg">Demo video coming soon</p>
              </div>
            )}
          </div>
        </div>
      )}
    </>
  )
}
```
  </action>
  <verify>No TypeScript errors in this file (`pnpm check-types` from apps/web/).</verify>
  <done>VideoModal component exists as a named export, renders a trigger button, opens a full-screen modal on click, closes on backdrop click and Escape key.</done>
</task>

<task type="auto">
  <name>Task 2: Home page matching Home.png</name>
  <files>apps/web/app/page.tsx</files>
  <action>
Build the complete Home page. Match Home.png exactly for every section. Replace the placeholder page.tsx created in Plan 01.

Reference Home.png carefully — the template shows:
1. **Hero section** (white bg): Left column — "SIMPLIFY YOUR SOCIAL LIFE" label in small teal caps, "Events Made" in large bold black + "Effortless" in teal italic, body text, two CTA buttons ("Start Planning Now" teal + arrow, "Watch Demo" ghost). Right column — tall plant photo + floating card "Upcoming: Rooftop BBQ Party" with teal toggle.
2. **Feature grid** (white bg): "Everything you need for the perfect host" heading, subtitle, 3-col cards: Gift Exchange / Potluck Planner / Photo Gallery — each with small teal icon, title, description, 2-3 bullet points with teal checkmarks.
3. **Mobile CTA section** (dark brand-800 teal bg): Left — "Experience Gatherly on the go" heading, description, black App Store + Google Play buttons. Right — phone mockup showing events list.
4. Home page metadata title: "Gatherly — Events Made Effortless" (handled by root layout default).

For the plant hero image: use an `<img>` tag pointing to a placeholder image service (e.g., `https://images.unsplash.com/photo-1558618666-fcd25c85cd64?w=400&h=500&fit=crop` — tropical plant). Add a TODO comment for the real asset.

For the phone mockup in mobile CTA: use a styled div with a dark rounded border to represent the phone frame, containing a simplified event list (3 items with brand colors).

For app store buttons: both `href="#"` (app not yet published).

```typescript
import type { Metadata } from 'next'
import { ArrowRight, Check, Gift, UtensilsCrossed, Camera } from 'lucide-react'
import { VideoModal } from '@/components/video-modal'

export const metadata: Metadata = {
  title: 'Gatherly — Events Made Effortless',
  description: 'Plan, manage, and enjoy your gatherings without the stress. From intimate dinners to large celebrations.',
}

const features = [
  {
    icon: Gift,
    title: 'Gift Exchange',
    description: 'Organise Secret Santa with automated drawing and built-in wishlists. Set budgets and delivery dates effortlessly.',
    bullets: ['Anonymous matching', 'Gifting tracking'],
  },
  {
    icon: UtensilsCrossed,
    title: 'Potluck Planner',
    description: "Coordinate who brings what so you never have two potato salads again. Real-time lists for appetisers, mains, and drinks.",
    bullets: ['Dietary labels/API', 'RSVP food commitments'],
  },
  {
    icon: Camera,
    title: 'Photo Gallery',
    description: 'Collect and share every memory in a private, high-resolution collaborative album. No more texting photos individually.',
    bullets: ['Bulk download/print', 'Shared commenting'],
  },
]

export default function HomePage() {
  return (
    <>
      {/* Hero */}
      <section className="max-w-6xl mx-auto px-6 pt-20 pb-24 flex flex-col md:flex-row items-center gap-12">
        {/* Left */}
        <div className="flex-1 max-w-xl">
          <p className="text-xs font-semibold tracking-widest text-brand-500 uppercase mb-4">
            Simplify your social life
          </p>
          <h1 className="text-5xl md:text-6xl font-extrabold leading-tight text-gray-900 mb-6">
            Events Made{' '}
            <span className="text-brand-500 italic font-extrabold">Effortless</span>
          </h1>
          <p className="text-lg text-gray-500 mb-8 leading-relaxed">
            Plan, manage, and enjoy your gatherings without the stress. From intimate dinners to
            large celebrations, Gatherly handles the details so you can focus on the memories.
          </p>
          <div className="flex flex-wrap gap-4">
            <a
              href="#"
              className="inline-flex items-center gap-2 bg-brand-500 hover:bg-brand-600 text-white font-semibold px-6 py-3 rounded-lg transition-colors"
            >
              Start Planning Now <ArrowRight size={16} />
            </a>
            <VideoModal
              triggerLabel="Watch Demo"
              triggerClassName="inline-flex items-center gap-2 border border-gray-300 hover:border-gray-400 text-gray-700 font-semibold px-6 py-3 rounded-lg transition-colors"
            />
          </div>
        </div>

        {/* Right — plant + floating card */}
        <div className="flex-1 relative flex justify-center items-center">
          {/* TODO: replace with real hero plant asset */}
          <img
            src="https://images.unsplash.com/photo-1558618666-fcd25c85cd64?w=440&h=520&fit=crop"
            alt="Tropical plant"
            className="rounded-2xl object-cover w-[320px] h-[400px] md:w-[400px] md:h-[480px]"
          />
          {/* Floating event card */}
          <div className="absolute bottom-6 left-4 md:left-0 bg-white rounded-xl shadow-xl p-4 w-56 border border-gray-100">
            <p className="text-xs text-gray-400 uppercase tracking-wide mb-1">Upcoming</p>
            <p className="font-semibold text-gray-900 text-sm">Rooftop BBQ Party</p>
            <div className="mt-2 flex items-center justify-between">
              <span className="text-xs text-gray-400">6 guests</span>
              <button className="w-9 h-5 bg-brand-500 rounded-full relative">
                <span className="absolute right-0.5 top-0.5 w-4 h-4 bg-white rounded-full shadow-sm" />
              </button>
            </div>
          </div>
        </div>
      </section>

      {/* Feature grid */}
      <section className="bg-gray-50 py-20">
        <div className="max-w-6xl mx-auto px-6">
          <div className="text-center mb-14">
            <h2 className="text-3xl md:text-4xl font-bold text-gray-900 mb-4">
              Everything you need for the perfect host
            </h2>
            <p className="text-gray-500 max-w-xl mx-auto">
              Our specialised modules make organising specific event types a breeze. No more messy
              spreadsheets or lost group chats.
            </p>
          </div>
          <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
            {features.map((f) => {
              const Icon = f.icon
              return (
                <div key={f.title} className="bg-white rounded-xl p-6 border border-gray-100 shadow-sm">
                  <div className="w-10 h-10 bg-brand-500/10 rounded-lg flex items-center justify-center mb-4">
                    <Icon size={20} className="text-brand-500" />
                  </div>
                  <h3 className="font-semibold text-gray-900 mb-2">{f.title}</h3>
                  <p className="text-sm text-gray-500 mb-4 leading-relaxed">{f.description}</p>
                  <ul className="space-y-1.5">
                    {f.bullets.map((b) => (
                      <li key={b} className="flex items-center gap-2 text-sm text-gray-600">
                        <Check size={14} className="text-brand-500 shrink-0" />
                        {b}
                      </li>
                    ))}
                  </ul>
                </div>
              )
            })}
          </div>
        </div>
      </section>

      {/* Mobile CTA section */}
      <section className="bg-brand-800 py-20">
        <div className="max-w-6xl mx-auto px-6 flex flex-col md:flex-row items-center gap-12">
          {/* Left text */}
          <div className="flex-1 text-white">
            <h2 className="text-3xl md:text-4xl font-bold mb-4 leading-tight">
              Experience Gatherly on the go
            </h2>
            <p className="text-brand-400 mb-8 leading-relaxed">
              Take the stress out of event planning with our mobile app. Real-time notifications,
              quick RSVPs, and mobile photo uploads. Available now for iOS and Android.
            </p>
            <div className="flex flex-wrap gap-4">
              {/* App Store button */}
              <a
                href="#"
                className="flex items-center gap-3 bg-black hover:bg-gray-900 text-white px-5 py-3 rounded-xl transition-colors"
              >
                <svg viewBox="0 0 24 24" fill="currentColor" className="w-6 h-6"><path d="M18.71 19.5c-.83 1.24-1.71 2.45-3.05 2.47-1.34.03-1.77-.79-3.29-.79-1.53 0-2 .77-3.27.82-1.31.05-2.3-1.32-3.14-2.53C4.25 17 2.94 12.45 4.7 9.39c.87-1.52 2.43-2.48 4.12-2.51 1.28-.02 2.5.87 3.29.87.78 0 2.26-1.07 3.8-.91.65.03 2.47.26 3.64 1.98-.09.06-2.17 1.28-2.15 3.81.03 3.02 2.65 4.03 2.68 4.04-.03.07-.42 1.44-1.38 2.83M13 3.5c.73-.83 1.94-1.46 2.94-1.5.13 1.17-.34 2.35-1.04 3.19-.69.85-1.83 1.51-2.95 1.42-.15-1.15.41-2.35 1.05-3.11z"/></svg>
                <div>
                  <p className="text-xs opacity-70">Download on the</p>
                  <p className="text-sm font-semibold">App Store</p>
                </div>
              </a>
              {/* Google Play button */}
              <a
                href="#"
                className="flex items-center gap-3 bg-black hover:bg-gray-900 text-white px-5 py-3 rounded-xl transition-colors"
              >
                <svg viewBox="0 0 24 24" fill="currentColor" className="w-6 h-6"><path d="M3.18 23.76c.37.2.8.22 1.2.06l12.16-7.03-2.66-2.67-10.7 9.64zm-1.14-1.02A1.99 1.99 0 012 21.22V2.78c0-.66.36-1.23.87-1.55L14.12 12 2.04 22.74zM20.98 10.5l-2.63-1.52-2.98 2.97 2.98 2.98 2.65-1.53c.76-.44.76-1.47-.02-1.9zM4.38.18L16.54 7.2 13.88 9.87 3.18.24c.37-.17.82-.17 1.2-.06z"/></svg>
                <div>
                  <p className="text-xs opacity-70">Get it on</p>
                  <p className="text-sm font-semibold">Google Play</p>
                </div>
              </a>
            </div>
          </div>

          {/* Right — phone mockup */}
          <div className="flex-1 flex justify-center">
            <div className="w-56 h-96 bg-gray-900 rounded-[2.5rem] border-4 border-gray-700 shadow-2xl overflow-hidden p-3 flex flex-col">
              <div className="text-white text-xs font-semibold mb-3 px-1">My Events</div>
              {[
                { name: "Anna's Birthday", date: 'Tomorrow • 12 guests', color: 'bg-brand-500' },
                { name: 'Team Offsite Dinner', date: 'Friday • 8 guests', color: 'bg-purple-500' },
                { name: 'Annual Holiday Bash', date: 'Dec 20 • 24 guests', color: 'bg-orange-400' },
              ].map((ev) => (
                <div key={ev.name} className="flex items-center gap-3 bg-gray-800 rounded-xl p-3 mb-2">
                  <div className={`w-8 h-8 ${ev.color} rounded-lg shrink-0`} />
                  <div>
                    <p className="text-white text-xs font-medium">{ev.name}</p>
                    <p className="text-gray-400 text-xs">{ev.date}</p>
                  </div>
                </div>
              ))}
              <div className="mt-auto flex justify-end">
                <div className="w-10 h-10 bg-brand-500 rounded-full flex items-center justify-center text-white text-lg font-bold shadow-lg">
                  +
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>
    </>
  )
}
```
  </action>
  <verify>
Visit http://localhost:3001:
- Hero renders with "Events Made Effortless" heading, teal italic "Effortless"
- "Start Planning Now" and "Watch Demo" buttons present
- Clicking "Watch Demo" opens modal overlay
- Feature grid shows 3 cards (Gift Exchange, Potluck Planner, Photo Gallery)
- Dark teal mobile CTA section renders with App Store + Play buttons and phone mockup
Run `pnpm check-types` from apps/web/ — exits 0.
  </verify>
  <done>Home page matches Home.png layout. All 3 sections render. VideoModal opens on Watch Demo click. TypeScript clean.</done>
</task>

</tasks>

<verification>
After both tasks:
1. http://localhost:3001 shows complete Home page matching Home.png
2. Watch Demo opens video modal
3. 3 feature cards visible with teal icons and bullet points
4. Dark teal mobile CTA section with both app store buttons
5. `pnpm check-types` exits 0
</verification>

<success_criteria>
- Home page matches Home.png for all sections (hero, feature grid, mobile CTA)
- VideoModal component is a reusable named export
- All CTAs use href="#" placeholder (app not yet published)
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/21-nextjs-website/21-02-SUMMARY.md` following the summary template.
</output>
