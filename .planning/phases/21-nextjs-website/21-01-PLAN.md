---
phase: 21-nextjs-website
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/next.config.ts
  - apps/web/postcss.config.mjs
  - apps/web/tsconfig.json
  - apps/web/eslint.config.mjs
  - apps/web/app/globals.css
  - apps/web/app/layout.tsx
  - apps/web/components/nav.tsx
  - apps/web/components/footer.tsx
autonomous: true

must_haves:
  truths:
    - "Running `pnpm dev` in apps/web starts a Next.js dev server on port 3001"
    - "Every page rendered by apps/web shows the persistent Nav and Footer"
    - "The Nav contains links to Features, How it Works, Pricing, Download and a Get Started button"
    - "The brand teal color (#1DB8A0) is available as a Tailwind utility class (bg-brand-500)"
    - "The AASA and assetlinks.json files exist at apps/web/public/.well-known/"
  artifacts:
    - path: "apps/web/package.json"
      provides: "Next.js 15 workspace package with dev script on port 3001"
    - path: "apps/web/app/globals.css"
      provides: "Tailwind v4 CSS-first config with brand color tokens"
    - path: "apps/web/app/layout.tsx"
      provides: "Root layout wrapping all pages with Nav + Footer"
    - path: "apps/web/components/nav.tsx"
      provides: "Persistent nav bar matching Home.png header"
    - path: "apps/web/components/footer.tsx"
      provides: "4-column footer matching Home.png footer"
    - path: "apps/web/public/.well-known/apple-app-site-association"
      provides: "AASA file for iOS Universal Links"
    - path: "apps/web/public/.well-known/assetlinks.json"
      provides: "Android App Links verification file"
  key_links:
    - from: "apps/web/app/layout.tsx"
      to: "apps/web/components/nav.tsx"
      via: "import and render"
      pattern: "import.*Nav.*from.*components/nav"
    - from: "apps/web/app/globals.css"
      to: "tailwindcss"
      via: "@import"
      pattern: "@import \"tailwindcss\""
    - from: "apps/web/app/globals.css"
      to: "brand color tokens"
      via: "@theme block"
      pattern: "--color-brand-500"
---

<objective>
Scaffold the apps/web Next.js package: all config files, shared layout, Nav, Footer, Tailwind v4 setup, and .well-known deep link files. This is the foundation all other Phase 21 plans build on.

Purpose: Without this plan, no page can be built — every other plan depends on the root layout, Tailwind tokens, and monorepo wiring being in place.
Output: A bootable Next.js 15 app at apps/web/ on port 3001 with Nav + Footer visible on every route, Tailwind v4 with brand colors, and .well-known files in place.
</objective>

<execution_context>
@C:\Users\dihan\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\dihan\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-nextjs-website/21-RESEARCH.md
@.planning/phases/21-nextjs-website/21-CONTEXT.md
@apps/web/screen-templates/Home.png
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold package config files</name>
  <files>
    apps/web/package.json
    apps/web/next.config.ts
    apps/web/postcss.config.mjs
    apps/web/tsconfig.json
    apps/web/eslint.config.mjs
  </files>
  <action>
Create `apps/web/` directory and all config files from scratch. Do NOT run `create-next-app` — the monorepo already exists.

**apps/web/package.json** — exact content:
```json
{
  "name": "web",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3001",
    "build": "next build",
    "start": "next start -p 3001",
    "lint": "eslint .",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "next": "latest",
    "react": "19.1.0",
    "react-dom": "19.1.0",
    "lucide-react": "latest"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@tailwindcss/postcss": "latest",
    "@types/node": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "tailwindcss": "latest",
    "typescript": "latest"
  }
}
```

**apps/web/next.config.ts** — AASA must be served as application/json:
```typescript
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  async headers() {
    return [
      {
        source: '/.well-known/apple-app-site-association',
        headers: [{ key: 'Content-Type', value: 'application/json' }],
      },
    ]
  },
}

export default nextConfig
```

**apps/web/postcss.config.mjs** — Tailwind v4 PostCSS plugin:
```javascript
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
}
```

**apps/web/tsconfig.json** — extends monorepo nextjs.json:
```json
{
  "extends": "@repo/typescript-config/nextjs.json",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": { "@/*": ["./*"] }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

**apps/web/eslint.config.mjs** — use monorepo Next.js preset:
```javascript
import { config } from '@repo/eslint-config/next'
export default config
```

After creating files, run from the repo root:
```bash
pnpm install
```

This registers the `web` workspace package and installs all dependencies including `next`, `tailwindcss`, and `@tailwindcss/postcss`.
  </action>
  <verify>
From repo root: `ls apps/web/package.json apps/web/next.config.ts apps/web/tsconfig.json` — all files present.
Run: `pnpm install` — exits 0 with no errors.
Run: `ls apps/web/node_modules/.pnpm` or `ls apps/web/node_modules/next` — next package resolved.
  </verify>
  <done>All 5 config files exist, pnpm install resolves the web workspace without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Tailwind v4 globals, root layout, Nav, Footer, .well-known files</name>
  <files>
    apps/web/app/globals.css
    apps/web/app/layout.tsx
    apps/web/components/nav.tsx
    apps/web/components/footer.tsx
    apps/web/public/.well-known/apple-app-site-association
    apps/web/public/.well-known/assetlinks.json
  </files>
  <action>
Create the app directory structure and all shared UI files.

**apps/web/app/globals.css** — Tailwind v4 CSS-first, brand tokens from Home.png visual:
```css
@import "tailwindcss";

@theme {
  /* Brand teal extracted from templates (~#1DB8A0) */
  --color-brand-400: #25d0b4;
  --color-brand-500: #1db8a0;
  --color-brand-600: #189e88;
  --color-brand-800: #0f766e;

  /* Typography — Inter via next/font/google set via CSS variable */
  --font-sans: var(--font-inter), ui-sans-serif, system-ui, sans-serif;
}
```

**apps/web/app/layout.tsx** — root layout, Inter font, persistent Nav + Footer:
```typescript
import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Nav } from '@/components/nav'
import { Footer } from '@/components/footer'

const inter = Inter({ subsets: ['latin'], variable: '--font-inter' })

export const metadata: Metadata = {
  title: { template: '%s | Gatherly', default: 'Gatherly — Events Made Effortless' },
  description: 'Plan, manage, and enjoy your gatherings without the stress.',
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className={inter.variable}>
      <body className="font-sans antialiased">
        <Nav />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  )
}
```

**apps/web/components/nav.tsx** — persistent nav matching Home.png header exactly:

The nav from Home.png has: Gatherly logo (teal "A" icon + "Gatherly" wordmark) on the left, nav links (Features, How it Works, Pricing, Download) in the center, "Get Started" teal button on the right. White background, subtle bottom border.

```typescript
import Link from 'next/link'

export function Nav() {
  return (
    <header className="sticky top-0 z-50 bg-white border-b border-gray-100">
      <div className="max-w-6xl mx-auto px-6 h-16 flex items-center justify-between">
        {/* Logo */}
        <Link href="/" className="flex items-center gap-2 font-bold text-gray-900">
          <div className="w-8 h-8 bg-brand-500 rounded-lg flex items-center justify-center text-white text-sm font-bold">
            A
          </div>
          <span>Gatherly</span>
        </Link>

        {/* Nav links */}
        <nav className="hidden md:flex items-center gap-8 text-sm text-gray-600">
          <Link href="/features" className="hover:text-gray-900 transition-colors">Features</Link>
          <Link href="/how-it-works" className="hover:text-gray-900 transition-colors">How it Works</Link>
          <Link href="/pricing" className="hover:text-gray-900 transition-colors">Pricing</Link>
          <Link href="/download" className="hover:text-gray-900 transition-colors">Download</Link>
        </nav>

        {/* CTA */}
        <Link
          href="#"
          className="bg-brand-500 hover:bg-brand-600 text-white text-sm font-semibold px-4 py-2 rounded-lg transition-colors"
        >
          Get Started
        </Link>
      </div>
    </header>
  )
}
```

**apps/web/components/footer.tsx** — 4-column footer matching Home.png footer (Gatherly tagline, Product, Company, Support columns, copyright):

```typescript
import Link from 'next/link'

export function Footer() {
  return (
    <footer className="bg-white border-t border-gray-100 py-12 mt-16">
      <div className="max-w-6xl mx-auto px-6">
        <div className="grid grid-cols-1 md:grid-cols-4 gap-8 mb-8">
          {/* Brand column */}
          <div>
            <div className="flex items-center gap-2 font-bold text-gray-900 mb-3">
              <div className="w-7 h-7 bg-brand-500 rounded-md flex items-center justify-center text-white text-xs font-bold">
                A
              </div>
              <span>Gatherly</span>
            </div>
            <p className="text-sm text-gray-500 leading-relaxed">
              Gatherly is dedicated to bringing people together. Our mission is to make every event
              stress-free and memorable.
            </p>
          </div>

          {/* Product */}
          <div>
            <h3 className="text-sm font-semibold text-gray-900 mb-3">Product</h3>
            <ul className="space-y-2 text-sm text-gray-500">
              <li><Link href="/features" className="hover:text-gray-900 transition-colors">Features</Link></li>
              <li><Link href="/how-it-works" className="hover:text-gray-900 transition-colors">Integrations</Link></li>
              <li><Link href="/pricing" className="hover:text-gray-900 transition-colors">Pricing</Link></li>
              <li><Link href="#" className="hover:text-gray-900 transition-colors">Photo App</Link></li>
            </ul>
          </div>

          {/* Company */}
          <div>
            <h3 className="text-sm font-semibold text-gray-900 mb-3">Company</h3>
            <ul className="space-y-2 text-sm text-gray-500">
              <li><Link href="#" className="hover:text-gray-900 transition-colors">About Us</Link></li>
              <li><Link href="#" className="hover:text-gray-900 transition-colors">Careers</Link></li>
              <li><Link href="#" className="hover:text-gray-900 transition-colors">Blog</Link></li>
              <li><Link href="#" className="hover:text-gray-900 transition-colors">Contact</Link></li>
            </ul>
          </div>

          {/* Support */}
          <div>
            <h3 className="text-sm font-semibold text-gray-900 mb-3">Support</h3>
            <ul className="space-y-2 text-sm text-gray-500">
              <li><Link href="#" className="hover:text-gray-900 transition-colors">Help Center</Link></li>
              <li><Link href="#" className="hover:text-gray-900 transition-colors">Terms of Service</Link></li>
              <li><Link href="#" className="hover:text-gray-900 transition-colors">Privacy Policy</Link></li>
              <li><Link href="#" className="hover:text-gray-900 transition-colors">Status</Link></li>
            </ul>
          </div>
        </div>

        <div className="border-t border-gray-100 pt-6 text-center text-xs text-gray-400">
          © 2026 Gatherly Technologies Inc. All rights reserved.
        </div>
      </div>
    </footer>
  )
}
```

**apps/web/public/.well-known/apple-app-site-association** — copy exact content from apps/gatherly/public/.well-known/apple-app-site-association:
```json
{
  "applinks": {
    "details": [
      {
        "appID": "TEAMID.com.gatherly.gatherly",
        "paths": ["/magic-link/*"]
      }
    ]
  }
}
```

**apps/web/public/.well-known/assetlinks.json** — copy exact content from apps/gatherly/public/.well-known/assetlinks.json:
```json
[
  {
    "relation": ["delegate_permission/common.handle_all_urls"],
    "target": {
      "namespace": "android_app",
      "package_name": "com.gatherly.gatherly",
      "sha256_cert_fingerprints": [
        "PLACEHOLDER:SHA256:FINGERPRINT:FROM:EAS:CREDENTIALS"
      ]
    }
  }
]
```

Also create a minimal placeholder home page so the dev server doesn't 404:

**apps/web/app/page.tsx** — temporary placeholder (replaced by Plan 02):
```typescript
export default function HomePage() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <p className="text-gray-500">Home page — coming in Plan 02</p>
    </div>
  )
}
```
  </action>
  <verify>
Run from apps/web/: `pnpm dev` — server starts on port 3001 with no errors.
Visit http://localhost:3001 — page loads with Nav (Gatherly logo + links + Get Started button) and Footer (4 columns).
Check http://localhost:3001/.well-known/apple-app-site-association — returns JSON content (not 404, not binary download).
Run `pnpm check-types` from apps/web/ — exits 0.
  </verify>
  <done>Dev server runs on port 3001. Nav and Footer visible on home route. Brand teal bg-brand-500 class resolves. AASA served as JSON. TypeScript clean.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `ls apps/web/package.json apps/web/next.config.ts apps/web/tsconfig.json apps/web/postcss.config.mjs apps/web/eslint.config.mjs` — all present
2. `ls apps/web/app/globals.css apps/web/app/layout.tsx apps/web/components/nav.tsx apps/web/components/footer.tsx` — all present
3. `ls apps/web/public/.well-known/apple-app-site-association apps/web/public/.well-known/assetlinks.json` — both present
4. Dev server starts on port 3001 without error
5. Nav links render: Features, How it Works, Pricing, Download, Get Started
</verification>

<success_criteria>
- apps/web is a valid pnpm workspace package recognized by Turborepo
- Next.js 15 dev server runs on port 3001
- Root layout renders Nav + Footer on every route
- Tailwind v4 brand tokens available (bg-brand-500, text-brand-500, etc.)
- .well-known files in place for Universal Links / App Links
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/21-nextjs-website/21-01-SUMMARY.md` following the summary template.
</output>
