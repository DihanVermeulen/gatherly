---
phase: 38-checkout-and-payment-success-screens
plan: 02
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - apps/gatherly-mobile/app/payment-success.tsx
autonomous: false

must_haves:
  truths:
    - "Payment Success screen shows green checkmark, event name, amount paid, payment method, and transaction ID"
    - "Go to Event Dashboard button refreshes events and navigates to event-details"
    - "Event planTier is premium after returning to dashboard"
  artifacts:
    - path: "apps/gatherly-mobile/app/payment-success.tsx"
      provides: "Payment success confirmation screen"
      min_lines: 80
  key_links:
    - from: "apps/gatherly-mobile/app/payment-success.tsx"
      to: "refreshEvents"
      via: "useEvents context"
      pattern: "refreshEvents"
    - from: "apps/gatherly-mobile/app/payment-success.tsx"
      to: "/event-details"
      via: "router.replace after refreshEvents"
      pattern: "router\\.replace.*event-details"
---

<objective>
Create the Payment Success screen matching Success.png template.

Purpose: After a successful checkout, the user sees a confirmation screen with payment details and can return to their (now premium) event dashboard.
Output: Working payment-success.tsx that reads params from checkout, displays confirmation, and navigates back to event-details with refreshed data.
</objective>

<execution_context>
@C:\Users\dihan\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\dihan\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/gatherly-mobile/screen-templates/Success.png
@apps/gatherly-mobile/app/api/events.ts
@apps/gatherly-mobile/app/contexts/EventsContext.tsx
@apps/gatherly-mobile/constants/upgrades.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Payment Success screen</name>
  <files>apps/gatherly-mobile/app/payment-success.tsx</files>
  <action>
    Create `app/payment-success.tsx` matching Success.png template.

    **Params:** Read from `useLocalSearchParams<{ eventId: string; last4: string; txId: string }>()`. Defaults: eventId="" last4="1234" txId="#TH-0000-00".

    **Layout:** SafeAreaView edges={["top", "bottom"]} with white background. NO AppHeader. Use ScrollView for content with centered alignment.

    **Top section:**
    - Back arrow (ChevronLeft from lucide) in top-left as a Pressable — onPress calls router.back()
    - "GATHERLY" centered text: fontSize 13, fontWeight "700", letterSpacing 3, color "#6b7280", textTransform "uppercase"

    **Hero section (centered):**
    - Large teal circle (width/height 80, borderRadius 40, backgroundColor "#0d9488") with white CheckCircle icon (from lucide, size 44, color "#ffffff") centered inside
    - "Payment Successful!" — fontSize 24, fontWeight "700", color "#111827", marginTop 20
    - "Your Standard Plan for the **{eventName}** is now active." — fontSize 14, color "#6b7280", textAlign "center". Bold the event name. Load event name via `eventsApi.getById(eventId)` in useEffect (fallback: "your event").

    **Active Plan card (white card with shadow):**
    - Container: backgroundColor "#ffffff", borderRadius 16, padding 20, marginTop 24, marginHorizontal 16, shadow (shadowColor "#000", shadowOffset {width:0,height:2}, shadowOpacity 0.08, shadowRadius 8, elevation 3)
    - "ACTIVE PLAN" badge: small View with backgroundColor "#dcfce7", borderRadius 6, paddingHorizontal 8, paddingVertical 3. Text: fontSize 10, fontWeight "700", color "#16a34a", textTransform "uppercase"
    - Event name bold + "Standard Plan" subtitle below the badge
    - Event cover photo thumbnail (small rounded image, 44x44, borderRadius 8) to the right of the name — use coverPhotoUrl from getById if available, otherwise a teal placeholder
    - Three detail rows with horizontal separator lines between them:
      - "Amount Paid" (left, gray) — PREMIUM_PRICE_DISPLAY (right, bold) (import from constants/upgrades)
      - "Payment Method" (left, gray) — CreditCard icon (size 14) + " Visa .... {last4}" (right)
      - "Transaction ID" (left, gray) — txId (right)

    **Footer section (centered):**
    - "A confirmation email has been sent to your registered address." — fontSize 12, color "#9ca3af", fontStyle "italic", textAlign "center", marginTop 20

    - "Go to Event Dashboard" button: Pressable, backgroundColor "#0d9488", borderRadius 12, paddingVertical 16, marginTop 24, marginHorizontal 16. Text: white, bold, fontSize 16, centered. Show ActivityIndicator (white, small) when loading.
      - onPress handler:
        ```typescript
        async function handleGoToDashboard() {
          setLoading(true);
          try {
            await refreshEvents();
            router.replace(`/event-details?id=${eventId}` as never);
          } catch {
            router.replace(`/event-details?id=${eventId}` as never);
          }
        }
        ```
      - Import `refreshEvents` from `useEvents()` context (EventsContext).

    - "View Receipt" secondary link: Pressable, marginTop 12. Text: fontSize 14, fontWeight "600", color "#6b7280", textAlign "center". onPress: `Alert.alert("Coming Soon", "Receipt downloads will be available soon.")`

    - "Need help with your plan?" — fontSize 12, color "#9ca3af", textAlign "center", marginTop 16, marginBottom 20
  </action>
  <verify>
    - File `apps/gatherly-mobile/app/payment-success.tsx` exists
    - grep "refreshEvents" apps/gatherly-mobile/app/payment-success.tsx
    - grep "router.replace.*event-details" apps/gatherly-mobile/app/payment-success.tsx
    - grep "PREMIUM_PRICE_DISPLAY" apps/gatherly-mobile/app/payment-success.tsx
    - grep "Payment Successful" apps/gatherly-mobile/app/payment-success.tsx
  </verify>
  <done>Payment Success screen renders matching Success.png with checkmark hero, event name, Active Plan card (amount, payment method, transaction ID), and "Go to Event Dashboard" button that refreshes events and navigates to event-details.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete checkout and payment success flow: PaywallModal/Pricing -> Checkout -> Payment Success -> Event Dashboard</what-built>
  <how-to-verify>
    1. Open any free-tier event in the app
    2. Trigger PaywallModal (e.g., try to enable a premium module in Modules Config)
    3. Tap "Upgrade" — should navigate to Checkout screen
    4. Verify Checkout screen matches Checkout.png: order summary, Express Pay buttons, card form
    5. Tap Apple Pay or Google Pay — should show "Coming Soon" alert
    6. Fill in all 4 card fields with any values (e.g., "Test User", "4242424242424242", "12/28", "123")
    7. Tap "Pay and Activate Event" — should navigate to Payment Success screen
    8. Verify Success screen matches Success.png: green checkmark, "Payment Successful!", Active Plan card with amount/method/txId
    9. Tap "Go to Event Dashboard" — should navigate to event-details
    10. Verify the event now shows as premium (no more paywall banners for premium features)
    11. Also test: Pricing screen -> Upgrade -> same checkout flow
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Full flow works: PaywallModal -> Checkout -> fill card -> submit -> Success -> Go to Dashboard -> event is premium
- Full flow works: Pricing screen -> Checkout -> same path
- Express Pay buttons show "Coming Soon" alert
- View Receipt shows "Coming Soon" alert
- Event planTier is updated to premium after flow completes
- Cannot navigate back to Checkout from Success screen (router.replace used)
</verification>

<success_criteria>
- Payment Success screen matches Success.png template
- "Go to Event Dashboard" calls refreshEvents() then navigates to event-details
- Event is premium after completing the flow
- No regressions in existing PaywallModal or Pricing screen functionality
</success_criteria>

<output>
After completion, create `.planning/phases/38-checkout-and-payment-success-screens/38-02-SUMMARY.md`
</output>
