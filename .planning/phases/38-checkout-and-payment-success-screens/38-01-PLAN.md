---
phase: 38-checkout-and-payment-success-screens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/gatherly-mobile/constants/upgrades.ts
  - apps/gatherly-mobile/app/api/events.ts
  - apps/gatherly-mobile/app/_layout.tsx
  - apps/gatherly-mobile/app/checkout.tsx
  - apps/gatherly-mobile/components/PaywallModal.tsx
  - apps/gatherly-mobile/app/pricing.tsx
autonomous: true

must_haves:
  truths:
    - "Tapping Upgrade in PaywallModal navigates to the Checkout screen"
    - "Tapping Upgrade on the Pricing screen navigates to the Checkout screen"
    - "Checkout screen shows order summary, express pay row, and card form matching Checkout.png"
    - "Submitting checkout form with any non-empty card details triggers upgrade and navigates to payment-success"
  artifacts:
    - path: "apps/gatherly-mobile/app/checkout.tsx"
      provides: "Checkout screen with card form"
      min_lines: 100
    - path: "apps/gatherly-mobile/constants/upgrades.ts"
      provides: "PREMIUM_PRICE_DISPLAY constant"
      contains: "PREMIUM_PRICE_DISPLAY"
    - path: "apps/gatherly-mobile/app/api/events.ts"
      provides: "upgradeEvent API method"
      contains: "upgradeEvent"
  key_links:
    - from: "apps/gatherly-mobile/components/PaywallModal.tsx"
      to: "/checkout"
      via: "router.push with eventId param"
      pattern: "router\\.push.*checkout"
    - from: "apps/gatherly-mobile/app/pricing.tsx"
      to: "/checkout"
      via: "router.push with eventId param"
      pattern: "router\\.push.*checkout"
    - from: "apps/gatherly-mobile/app/checkout.tsx"
      to: "/payment-success"
      via: "router.replace after upgradeEvent call"
      pattern: "router\\.replace.*payment-success"
---

<objective>
Create the Checkout screen, wire upgrade CTAs, and add supporting infrastructure (API method, constant, layout registration).

Purpose: Tapping "Upgrade" anywhere in the app navigates to a card payment form (Checkout.png template) instead of opening an external browser.
Output: Working checkout flow from PaywallModal/Pricing -> Checkout screen -> navigates to payment-success on submit.
</objective>

<execution_context>
@C:\Users\dihan\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\dihan\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/gatherly-mobile/screen-templates/Checkout.png
@apps/gatherly-mobile/app/_layout.tsx
@apps/gatherly-mobile/app/api/events.ts
@apps/gatherly-mobile/constants/upgrades.ts
@apps/gatherly-mobile/components/PaywallModal.tsx
@apps/gatherly-mobile/app/pricing.tsx
@apps/gatherly-mobile/components/AppHeader.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add infrastructure — API method, constant, layout registration</name>
  <files>
    apps/gatherly-mobile/constants/upgrades.ts
    apps/gatherly-mobile/app/api/events.ts
    apps/gatherly-mobile/app/_layout.tsx
  </files>
  <action>
    1. In `constants/upgrades.ts`, add below the existing UPGRADE_REQUEST_URL:
       ```
       export const PREMIUM_PRICE_DISPLAY = "$49.00";
       ```

    2. In `app/api/events.ts`, add an `upgradeEvent` method to the `eventsApi` object (before the closing brace):
       ```typescript
       upgradeEvent: async (id: string): Promise<TEvent> => {
         const response = await apiClient.patch(`/api/events/${id}/upgrade`);
         return response.data;
       },
       ```

    3. In `app/_layout.tsx`, add two Stack.Screen entries inside the `Stack.Protected guard={!!session}` block, after the existing `pricing` screen entry:
       ```tsx
       <Stack.Screen name="checkout" options={{ headerShown: false }} />
       <Stack.Screen name="payment-success" options={{ headerShown: false }} />
       ```
  </action>
  <verify>
    - grep "PREMIUM_PRICE_DISPLAY" apps/gatherly-mobile/constants/upgrades.ts
    - grep "upgradeEvent" apps/gatherly-mobile/app/api/events.ts
    - grep "checkout" apps/gatherly-mobile/app/_layout.tsx
    - grep "payment-success" apps/gatherly-mobile/app/_layout.tsx
  </verify>
  <done>PREMIUM_PRICE_DISPLAY exported, upgradeEvent method exists on eventsApi, both screens registered in layout</done>
</task>

<task type="auto">
  <name>Task 2: Create Checkout screen and wire upgrade CTAs</name>
  <files>
    apps/gatherly-mobile/app/checkout.tsx
    apps/gatherly-mobile/components/PaywallModal.tsx
    apps/gatherly-mobile/app/pricing.tsx
  </files>
  <action>
    **Create `app/checkout.tsx`** matching Checkout.png template:

    - Read eventId from `useLocalSearchParams<{ eventId: string }>()` with default `""`
    - Use `KeyboardAvoidingView` (behavior: 'padding' on iOS, 'height' on Android) wrapping a `ScrollView`
    - Use `SafeAreaView` edges={["bottom"]} as outer wrapper with white bg
    - Use `AppHeader` with title="Checkout" and `onBack={() => router.back()}`

    **Order Summary section:**
    - Load event name via `eventsApi.getById(eventId)` in useEffect (store in local state, fallback to "Your Event")
    - Gray subtitle with event name, bold "Standard Plan" text, teal "$49.00" price (use PREMIUM_PRICE_DISPLAY from constants/upgrades.ts)
    - Right side: small rounded image placeholder (use event cover photo if available from getById, otherwise a teal placeholder View with a Gift icon or just a colored rectangle)

    **Express Pay section:**
    - "Express Pay" label
    - Two buttons side by side: "Apple Pay" (black bg, white text) and "Google Pay" (white bg with border, dark text)
    - "OR PAY WITH CARD" divider text (gray, centered, with horizontal lines on each side)
    - All express pay buttons call `Alert.alert("Coming Soon", "Express Pay will be available soon.")`

    **Payment Method section:**
    - "Payment Method" header with green ShieldCheck icon + "SECURE PAYMENT" badge (green bg, white text) right-aligned
    - Four TextInput fields with labels above each:
      - CARDHOLDER NAME (placeholder "John Doe")
      - CARD NUMBER (placeholder "0000 0000 0000 0000", keyboardType="number-pad", CreditCard icon from lucide on right side of input)
      - Two-column row: EXPIRY DATE (placeholder "MM/YY") | CVV (placeholder "123", secureTextEntry=true)
    - Use plain React Native TextInput (not GlueStack Input). Style: borderWidth 1, borderColor "#e5e7eb", borderRadius 10, padding 14, fontSize 15, backgroundColor "#ffffff"

    **Submit button:**
    - Teal Pressable: "Pay and Activate Event", backgroundColor "#0d9488", pressed state "#0f766e"
    - Disabled when ANY field is empty (cardholderName, cardNumber, expiry, cvv all trimmed must be non-empty)
    - Disabled state: opacity 0.5
    - Show ActivityIndicator when loading

    **On submit:**
    ```typescript
    setLoading(true);
    try {
      await eventsApi.upgradeEvent(eventId);
      const txId = `#TH-${Math.floor(1000 + Math.random() * 9000)}-${Math.floor(10 + Math.random() * 90)}`;
      const last4 = cardNumber.replace(/\s/g, "").slice(-4) || "1234";
      router.replace(`/payment-success?eventId=${eventId}&last4=${last4}&txId=${encodeURIComponent(txId)}` as never);
    } catch {
      Alert.alert("Payment Failed", "Please try again.");
      setLoading(false);
    }
    ```
    Use `router.replace` (NOT push) so user cannot go back to checkout after payment.

    **Footer:**
    - Lock icon + "Secure one-time payment powered by Stripe" gray text
    - "By completing this purchase, you agree to Gatherly's Terms of Service and Privacy Policy." — smaller gray text. Terms/Privacy are plain text (no links needed).

    **Update `components/PaywallModal.tsx`:**
    - Add `import { useRouter } from "expo-router";` at top
    - Destructure `eventId` from props (it's already in the interface but not destructured)
    - Replace the `handleUpgrade` function body:
      ```typescript
      function handleUpgrade() {
        onClose();
        router.push(`/checkout?eventId=${eventId ?? ""}` as never);
      }
      ```
    - Remove the `expo-web-browser` import (no longer used in this file — but check if the "Need help choosing?" link at the bottom still uses it. If so, keep the import.)
    - The "Need help choosing?" Pressable at the bottom still opens WebBrowser, so KEEP the expo-web-browser import.

    **Update `app/pricing.tsx`:**
    - Replace `handleRequestAccess` function: instead of Alert.alert + WebBrowser, use:
      ```typescript
      function handleUpgrade() {
        router.push(`/checkout?eventId=${eventId}` as never);
      }
      ```
    - Update the Pressable onPress to call `handleUpgrade` instead of `handleRequestAccess`
    - The `expo-web-browser` import can be removed IF the "Need help choosing?" link at the bottom is also updated. Check: the bottom link still calls WebBrowser.openBrowserAsync — keep the import.
    - The Pressable CTA remains disabled when `!eventId` (existing behavior, preserved).
  </action>
  <verify>
    - File `apps/gatherly-mobile/app/checkout.tsx` exists and has TextInput fields for card details
    - grep "router.push.*checkout" apps/gatherly-mobile/components/PaywallModal.tsx
    - grep "router.push.*checkout" apps/gatherly-mobile/app/pricing.tsx
    - grep "router.replace.*payment-success" apps/gatherly-mobile/app/checkout.tsx
    - grep "upgradeEvent" apps/gatherly-mobile/app/checkout.tsx
  </verify>
  <done>Checkout screen renders matching Checkout.png with order summary, express pay (decorative), card form (4 fields), and submit button. PaywallModal and Pricing screen CTAs navigate to /checkout?eventId=X. Submitting with any non-empty card details calls upgradeEvent and navigates to payment-success.</done>
</task>

</tasks>

<verification>
- Open PaywallModal on any event -> tap Upgrade -> navigates to Checkout screen (not external browser)
- Open Pricing screen with eventId -> tap Upgrade -> navigates to Checkout screen
- Checkout shows: order summary with event name + $49.00, Express Pay buttons (alert "Coming Soon"), card form with 4 fields
- Fill all 4 card fields with any values -> "Pay and Activate Event" button becomes active
- Submit -> calls PATCH /api/events/:id/upgrade -> navigates to payment-success with last4 + txId params
- Express Pay buttons show "Coming Soon" alert
- Back button on Checkout works (router.back)
</verification>

<success_criteria>
- Checkout screen matches Checkout.png template layout
- All upgrade CTAs (PaywallModal, Pricing) route to /checkout
- Card form accepts any non-empty input and triggers upgrade
- Navigation uses router.replace to payment-success (no back to checkout)
</success_criteria>

<output>
After completion, create `.planning/phases/38-checkout-and-payment-success-screens/38-01-SUMMARY.md`
</output>
