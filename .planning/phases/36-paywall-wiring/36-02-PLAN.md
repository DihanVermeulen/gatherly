---
phase: 36-paywall-wiring
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - apps/gatherly-mobile/app/potluck-setup.tsx
  - apps/gatherly-mobile/app/edit-event.tsx
  - apps/gatherly-mobile/app/polls.tsx
autonomous: true

must_haves:
  truths:
    - "In Potluck Setup, free-tier organizers see the normal setup screen (not a full-screen gate) with a 'X of 3 categories' counter visible when 2+ categories exist"
    - "In Potluck Setup, tapping 'Add New Category' when at 3 categories on free tier opens PaywallModal instead of creating a category"
    - "In Edit Event, a participant count badge 'X/20 participants' appears when 15+ participants exist"
    - "In Edit Event, the invite button is disabled and opens PaywallModal when free-tier event has 20 participants"
    - "In Polls, a counter 'X of 1 polls used' is always visible on free-tier events for organizers"
    - "In Polls, tapping + to create a poll when 1 already exists on free tier opens PaywallModal instead of the create modal"
  artifacts:
    - path: "apps/gatherly-mobile/app/potluck-setup.tsx"
      provides: "Potluck setup with category counter and PaywallModal cap enforcement"
    - path: "apps/gatherly-mobile/app/edit-event.tsx"
      provides: "Edit event with participant badge and cap enforcement"
    - path: "apps/gatherly-mobile/app/polls.tsx"
      provides: "Polls screen with poll counter and cap enforcement"
  key_links:
    - from: "apps/gatherly-mobile/app/potluck-setup.tsx"
      to: "apps/gatherly-mobile/components/PaywallModal.tsx"
      via: "import and render PaywallModal"
      pattern: "PaywallModal"
    - from: "apps/gatherly-mobile/app/edit-event.tsx"
      to: "apps/gatherly-mobile/components/PaywallModal.tsx"
      via: "import and render PaywallModal"
      pattern: "PaywallModal"
    - from: "apps/gatherly-mobile/app/polls.tsx"
      to: "apps/gatherly-mobile/components/PaywallModal.tsx"
      via: "import and render PaywallModal"
      pattern: "PaywallModal"
---

<objective>
Wire PaywallBanner into the remaining three screens — Potluck Setup, Edit Event, and Polls — adding tier counters, cap badges, and PaywallModal triggers that replace all remaining bespoke locked states.

Purpose: Complete the paywall wiring so every feature with a tier limit shows consistent locked UX through the shared PaywallModal.
Output: Updated potluck-setup.tsx, edit-event.tsx, polls.tsx with counters, badges, and PaywallModal enforcement.
</objective>

<execution_context>
@C:\Users\dihan\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\dihan\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-paywall-wiring/36-CONTEXT.md
@.planning/phases/36-paywall-wiring/36-RESEARCH.md
@.planning/phases/36-paywall-wiring/36-01-SUMMARY.md
@apps/gatherly-mobile/app/potluck-setup.tsx
@apps/gatherly-mobile/app/edit-event.tsx
@apps/gatherly-mobile/app/polls.tsx
@apps/gatherly-mobile/components/PaywallModal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Potluck Setup — remove gate, add counter + PaywallModal</name>
  <files>apps/gatherly-mobile/app/potluck-setup.tsx</files>
  <action>
**Remove the full-screen free-tier gate:**
- Delete the entire `if (!loading && isFree) { return <SafeAreaView>...</SafeAreaView>; }` block (approximately lines 616-683). Free-tier organizers should now see the normal setup screen.

**Add PaywallModal state:**
- Import `PaywallModal` from `@/components/PaywallModal` and `PaywallFeature` type from `@/components/PaywallBanner`.
- Add state: `const [showPaywall, setShowPaywall] = useState(false);`

**Add category counter:**
- Below the "Configure Items" subtitle text (the `<Text>` with "Define what guests should bring..."), add a counter row that is **only visible when `isFree && categories.length >= 2`**:
  ```
  <View style={{ flexDirection: 'row', alignItems: 'center', backgroundColor: '#fffbeb', borderRadius: 10, paddingHorizontal: 12, paddingVertical: 8, marginBottom: 16 }}>
    <Text style={{ fontSize: 13, color: '#92400e', fontWeight: '600' }}>
      {categories.length} of 3 categories
    </Text>
    <Text style={{ fontSize: 13, color: '#92400e', marginLeft: 4 }}>
      · Upgrade for unlimited
    </Text>
  </View>
  ```
- The counter should count all categories (including temp ones) since they represent the user's intent.

**Cap enforcement on "Add New Category":**
- Modify `handleAddCategory`: at the top, add a guard — if `isFree && categories.length >= 3`, call `setShowPaywall(true)` and return early (do not create a temp category).
- The existing "Add New Category" button press calls `handleAddCategory`, so no button changes needed.

**Render PaywallModal:**
- At the bottom of the component (before closing `</SafeAreaView>`), render:
  ```
  <PaywallModal isOpen={showPaywall} onClose={() => setShowPaywall(false)} feature="potluck_trial" eventId={id} isParticipant={false} />
  ```
- isParticipant is always false since potluck-setup is organizer-only.
  </action>
  <verify>
- `grep -c "if (!loading && isFree)" apps/gatherly-mobile/app/potluck-setup.tsx` returns 0 (gate removed)
- `grep "of 3 categories" apps/gatherly-mobile/app/potluck-setup.tsx` confirms counter exists
- `grep "PaywallModal" apps/gatherly-mobile/app/potluck-setup.tsx` confirms modal is rendered
  </verify>
  <done>
- Free-tier organizers see the normal potluck setup screen (not the old full-screen gate)
- Counter "X of 3 categories . Upgrade for unlimited" visible when 2+ categories exist on free tier
- Adding a 4th category on free tier opens PaywallModal instead
  </done>
</task>

<task type="auto">
  <name>Task 2: Edit Event participant badge + Polls counter + PaywallModal</name>
  <files>
    apps/gatherly-mobile/app/edit-event.tsx
    apps/gatherly-mobile/app/polls.tsx
  </files>
  <action>
**edit-event.tsx — participant cap badge + PaywallModal:**

1. **Add imports:** Import `PaywallModal` from `@/components/PaywallModal`. Import `Lock` from `lucide-react-native` if not already present.

2. **Add state:** `const [showPaywall, setShowPaywall] = useState(false);`

3. **Add isFree derivation:** `const isFree = (event?.planTier ?? 'free') === 'free';` — add near the existing derived values (around line 138-141).

4. **Add PARTICIPANT_CAP constant:** `const PARTICIPANT_CAP = 20;` at the top of the file or inside the component.

5. **Participant count badge:** In the Guest List section header row (around line 443-458), between the "Guest List" title and the "Manage" button, add a badge that is **only visible when `isFree && participants.length >= 15`**:
   ```
   {isFree && participants.length >= 15 && (
     <View style={{ backgroundColor: '#fffbeb', borderRadius: 10, paddingHorizontal: 8, paddingVertical: 3, marginRight: 'auto', marginLeft: 8 }}>
       <Text style={{ fontSize: 11, fontWeight: '700', color: '#92400e' }}>
         {participants.length}/{PARTICIPANT_CAP} participants
       </Text>
     </View>
   )}
   ```

6. **Cap enforcement on invite button:** Modify the invite button (the `UserPlus` Pressable around line 529-542). When `isFree && participants.length >= PARTICIPANT_CAP`:
   - Change `onPress` to call `setShowPaywall(true)` instead of `handleCreateInvite`
   - Keep the button visually the same but route to PaywallModal
   - Wrap the existing condition: `onPress={isFree && participants.length >= PARTICIPANT_CAP ? () => setShowPaywall(true) : handleCreateInvite}`

7. **Render PaywallModal:** At the bottom of the component (before the closing `</SafeAreaView>`), after the invite modal:
   ```
   <PaywallModal isOpen={showPaywall} onClose={() => setShowPaywall(false)} feature="participant_cap" eventId={id} isParticipant={false} />
   ```

**polls.tsx — poll counter + PaywallModal:**

1. **Add imports:** Import `PaywallModal` from `@/components/PaywallModal`. Import `useEvents` from `./contexts/EventsContext`.

2. **Add isFree derivation:** Inside the component, add:
   ```
   const { state: { events } } = useEvents();
   const event = events.find((e) => e.id === id) ?? null;
   const isFree = (event?.planTier ?? 'free') === 'free';
   ```

3. **Add state:** `const [showPaywall, setShowPaywall] = useState(false);`

4. **Add poll counter:** After the `<ScrollView>` opening and before the loading/empty/list ternary (around line 166), add a counter that is **visible when `isFree && isOrganizer`** (always visible on free tier, regardless of poll count):
   ```
   {isFree && isOrganizer && !loading && (
     <View style={{ flexDirection: 'row', alignItems: 'center', backgroundColor: '#fffbeb', borderRadius: 10, paddingHorizontal: 12, paddingVertical: 8, marginBottom: 12 }}>
       <Text style={{ fontSize: 13, color: '#92400e', fontWeight: '600' }}>
         {polls.length} of 1 polls used
       </Text>
       <Text style={{ fontSize: 13, color: '#92400e', marginLeft: 4 }}>
         · Upgrade for unlimited
       </Text>
     </View>
   )}
   ```

5. **Cap enforcement on + button:** Modify the header `rightAction` (around line 148-156). When `isFree && polls.length >= 1`, the + button should open PaywallModal instead of the create modal:
   ```
   rightAction={
     isOrganizer
       ? {
           icon: <Plus size={20} color="white" />,
           onPress: () => {
             if (isFree && polls.length >= 1) {
               setShowPaywall(true);
             } else {
               setShowCreateModal(true);
             }
           },
           style: { backgroundColor: "#0d9488" },
         }
       : undefined
   }
   ```

6. **Render PaywallModal:** At the bottom of the component (before closing `</SafeAreaView>`), after the Create Poll Modal:
   ```
   <PaywallModal isOpen={showPaywall} onClose={() => setShowPaywall(false)} feature="polls_trial" eventId={id} isParticipant={false} />
   ```
  </action>
  <verify>
- `grep "PARTICIPANT_CAP" apps/gatherly-mobile/app/edit-event.tsx` confirms constant exists
- `grep "participants.length >= 15" apps/gatherly-mobile/app/edit-event.tsx` confirms badge threshold
- `grep "PaywallModal" apps/gatherly-mobile/app/edit-event.tsx` confirms modal is rendered
- `grep "of 1 polls used" apps/gatherly-mobile/app/polls.tsx` confirms counter text
- `grep "PaywallModal" apps/gatherly-mobile/app/polls.tsx` confirms modal is rendered
- `grep "useEvents" apps/gatherly-mobile/app/polls.tsx` confirms import added
- No TypeScript errors: `cd apps/gatherly-mobile && npx tsc --noEmit --pretty 2>&1 | head -30`
  </verify>
  <done>
- Edit Event: "X/20 participants" badge visible at 15+ participants on free tier; invite button opens PaywallModal at cap
- Polls: "X of 1 polls used . Upgrade for unlimited" counter always visible on free tier for organizers; + button opens PaywallModal when 1 poll exists
  </done>
</task>

</tasks>

<verification>
1. potluck-setup.tsx: no full-screen free-tier gate, category counter present, PaywallModal rendered
2. edit-event.tsx: participant badge with >=15 threshold, cap enforcement at 20, PaywallModal rendered
3. polls.tsx: poll counter always visible on free tier for organizers, + button cap enforcement, PaywallModal rendered, useEvents imported
4. All three screens import PaywallModal from the shared component (created in 36-01)
5. TypeScript compiles without errors
</verification>

<success_criteria>
- WIRE-02 satisfied: Potluck Setup shows normal screen (not gate) with category counter + PaywallModal at cap
- WIRE-04 satisfied: Edit Event shows participant badge at 15+ and PaywallModal at 20-participant cap
- WIRE-05 satisfied: Polls shows always-visible counter on free tier and PaywallModal when poll limit hit
- All bespoke locked states replaced with shared PaywallModal
</success_criteria>

<output>
After completion, create `.planning/phases/36-paywall-wiring/36-02-SUMMARY.md`
</output>
