---
phase: 36-paywall-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/gatherly-mobile/components/PaywallBanner.tsx
  - apps/gatherly-mobile/components/PaywallModal.tsx
  - apps/gatherly-mobile/app/modules-config.tsx
  - apps/gatherly-mobile/app/event-details.tsx
autonomous: true

must_haves:
  truths:
    - "In Module Config, premium modules show a lock icon in the top-right and NO toggle; tapping the card opens a full-screen PaywallBanner modal"
    - "In Module Config, coming-soon modules show the same grey tint as locked modules but with a 'Coming soon' badge instead of a lock icon"
    - "The old bespoke upgrade modal in Module Config is completely removed"
    - "In Event Details, premium module cards are hidden entirely on free-tier events for all users"
    - "PaywallBanner CTA opens external URL directly via expo-web-browser without an Alert.alert confirmation dialog"
  artifacts:
    - path: "apps/gatherly-mobile/components/PaywallModal.tsx"
      provides: "Full-screen modal wrapper around PaywallBanner"
      exports: ["PaywallModal"]
    - path: "apps/gatherly-mobile/components/PaywallBanner.tsx"
      provides: "Updated banner with direct WebBrowser.openBrowserAsync (no Alert.alert)"
    - path: "apps/gatherly-mobile/app/modules-config.tsx"
      provides: "Rewired module config with lock treatment and PaywallModal"
    - path: "apps/gatherly-mobile/app/event-details.tsx"
      provides: "Event hub that hides premium modules on free events"
  key_links:
    - from: "apps/gatherly-mobile/components/PaywallModal.tsx"
      to: "apps/gatherly-mobile/components/PaywallBanner.tsx"
      via: "imports and renders PaywallBanner inside Modal"
      pattern: "import.*PaywallBanner"
    - from: "apps/gatherly-mobile/app/modules-config.tsx"
      to: "apps/gatherly-mobile/components/PaywallModal.tsx"
      via: "showPaywall state triggers PaywallModal"
      pattern: "PaywallModal"
---

<objective>
Wire PaywallBanner into Module Config and Event Details screens, replacing all bespoke upgrade UX. Create the shared PaywallModal wrapper (full-screen GlueStack Modal around PaywallBanner) that all screens will use.

Purpose: Centralise upgrade UX through a single modal component; remove old inline upgrade modals and replace with consistent PaywallBanner treatment.
Output: PaywallModal component, updated modules-config.tsx, updated event-details.tsx, updated PaywallBanner (Alert.alert removed).
</objective>

<execution_context>
@C:\Users\dihan\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\dihan\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-paywall-wiring/36-CONTEXT.md
@.planning/phases/36-paywall-wiring/36-RESEARCH.md
@apps/gatherly-mobile/components/PaywallBanner.tsx
@apps/gatherly-mobile/app/modules-config.tsx
@apps/gatherly-mobile/app/event-details.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PaywallModal + fix PaywallBanner CTA</name>
  <files>
    apps/gatherly-mobile/components/PaywallModal.tsx
    apps/gatherly-mobile/components/PaywallBanner.tsx
  </files>
  <action>
**PaywallBanner.tsx — remove Alert.alert confirmation:**
- In `handleUpgradeCta()`, replace the `Alert.alert(...)` with a direct call: `WebBrowser.openBrowserAsync(UPGRADE_REQUEST_URL)`. No confirmation dialog. The function should just call `WebBrowser.openBrowserAsync(UPGRADE_REQUEST_URL)` directly.

**PaywallModal.tsx — create new file:**
- Import `Modal, ModalBackdrop, ModalContent` from `@/components/ui/modal` (GlueStack)
- Import `PaywallBanner` and its `PaywallFeature` type from `@/components/PaywallBanner`
- Props interface: `{ isOpen: boolean; onClose: () => void; feature: PaywallFeature; eventId?: string; isParticipant: boolean }`
- Render a GlueStack `<Modal isOpen={isOpen} onClose={onClose} size="full">` wrapping `<ModalBackdrop />` and `<ModalContent style={{ flex: 1 }}>` (flex:1 ensures full height since GlueStack size="full" only controls width)
- Inside ModalContent: render a close button (X icon from lucide, top-right, absolute positioned, z-10) and `<PaywallBanner feature={feature} eventId={eventId} isParticipant={isParticipant} />`
- Centre the PaywallBanner vertically within the modal content using flex justifyContent center
- onClose keeps user on current screen (no router navigation)
- Export `PaywallModal` as named export
  </action>
  <verify>
- `grep -r "Alert.alert" apps/gatherly-mobile/components/PaywallBanner.tsx` returns no results (Alert.alert removed)
- `grep "PaywallModal" apps/gatherly-mobile/components/PaywallModal.tsx` confirms export exists
- `grep "size=\"full\"" apps/gatherly-mobile/components/PaywallModal.tsx` confirms full-screen modal
  </verify>
  <done>
- PaywallBanner CTA calls WebBrowser.openBrowserAsync directly without Alert.alert
- PaywallModal renders as a full-screen modal wrapping PaywallBanner with a close button
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire Module Config + Event Details</name>
  <files>
    apps/gatherly-mobile/app/modules-config.tsx
    apps/gatherly-mobile/app/event-details.tsx
  </files>
  <action>
**modules-config.tsx — replace bespoke upgrade modal with PaywallModal:**

1. **Remove old upgrade modal:** Delete the entire `showUpgradeModal` state, the `<Modal isOpen={showUpgradeModal}...>` block (lines ~367-422), and all `setShowUpgradeModal` calls.

2. **Add PaywallModal state:** Add `const [paywallFeature, setPaywallFeature] = useState<PaywallFeature | null>(null);` (import PaywallFeature from PaywallBanner). The modal is open when `paywallFeature !== null`.

3. **Import PaywallModal** from `@/components/PaywallModal`.

4. **Update handleToggle:** When `isPremium && isFree`, instead of `setShowUpgradeModal(true)`, call `setPaywallFeature({ type: 'module_locked', moduleName: mod.label })` — but handleToggle receives `type` not `label`, so look up the label from MODULE_DEFS. Actually, update handleToggle to also accept `label: string` parameter, or derive the label inside handleToggle by finding the ModuleDef from MODULE_DEFS. Simplest: change `handleToggle` to accept the full ModuleDef, then use `mod.label`. Update call sites accordingly.

5. **Replace the free-tier upgrade banner CTA:** The teal "Unlock Premium Modules" banner's "Upgrade" button should call `setPaywallFeature({ type: 'module_locked', moduleName: 'Premium Modules' })`.

6. **Lock treatment for module cards:**
   - For locked modules (`isLocked && !mod.comingSoon`): **hide the Switch entirely** — do not render it at all (not greyed out, completely gone). Instead, show a small Lock icon (lucide `Lock`, size 16, color `#94a3b8`) in that space.
   - For coming-soon modules (`mod.comingSoon`): **also hide the Switch** and show the existing "Coming soon" badge text instead. Apply same grey tint.
   - Apply a light grey background tint to the entire card row for both locked and coming-soon modules: `backgroundColor: 'rgba(0,0,0,0.03)'` on the outer View.
   - Make locked (non-coming-soon) module cards tappable — wrap the card row content in a Pressable that calls `setPaywallFeature({ type: 'module_locked', moduleName: mod.label })`.

7. **Render PaywallModal** at the bottom of the component: `<PaywallModal isOpen={paywallFeature !== null} onClose={() => setPaywallFeature(null)} feature={paywallFeature!} eventId={id} isParticipant={false} />` (this screen is organizer-only, so isParticipant is always false).

8. **Also handle the API error path:** In the catch block of handleToggle where `err?.response?.data?.error === "upgrade_required"`, set paywallFeature similarly.

**event-details.tsx — hide premium modules on free events:**

1. Add `isFree` derivation: `const isFree = (event?.planTier ?? 'free') === 'free';` (after the `event` variable is derived).

2. Define a constant set: `const PREMIUM_MODULE_TYPES = new Set(['polls', 'rsvp', 'potluck', 'white_elephant', 'photo_gallery', 'expense_splitter']);`

3. In the module cards rendering section (inside `CATEGORY_ORDER.map`), filter the entries: when `isFree`, exclude any entry whose `type` is in `PREMIUM_MODULE_TYPES`. Replace `const entries = MODULE_CATALOG.filter((m) => m.category === category);` with:
   ```
   const entries = MODULE_CATALOG.filter(
     (m) => m.category === category && !(isFree && PREMIUM_MODULE_TYPES.has(m.type))
   );
   ```

4. If after filtering `entries` is empty, return null for that category (this is already handled by the existing `if (!entries?.length) return null` check — but that check doesn't exist in event-details. Add it: `if (entries.length === 0) return null;`).

No PaywallModal needed in event-details — premium modules are simply hidden.
  </action>
  <verify>
- `grep -c "showUpgradeModal" apps/gatherly-mobile/app/modules-config.tsx` returns 0 (old modal fully removed)
- `grep "PaywallModal" apps/gatherly-mobile/app/modules-config.tsx` confirms PaywallModal is rendered
- `grep "PREMIUM_MODULE_TYPES" apps/gatherly-mobile/app/event-details.tsx` confirms filtering is in place
- No TypeScript errors: `cd apps/gatherly-mobile && npx tsc --noEmit --pretty 2>&1 | head -30`
  </verify>
  <done>
- Module Config: locked modules show lock icon, no toggle; tapping opens PaywallModal. Coming-soon modules show grey tint + badge, no toggle. Old bespoke modal is gone.
- Event Details: premium module cards are hidden on free events for all users.
  </done>
</task>

</tasks>

<verification>
1. In modules-config.tsx, grep for "showUpgradeModal" — must return 0 matches (bespoke modal removed)
2. In modules-config.tsx, grep for "PaywallModal" — must be present
3. In event-details.tsx, grep for "PREMIUM_MODULE_TYPES" — must be present
4. In PaywallBanner.tsx, grep for "Alert.alert" — must return 0 matches
5. PaywallModal.tsx exists and exports PaywallModal with size="full"
6. TypeScript compiles without errors
</verification>

<success_criteria>
- WIRE-01 satisfied: Module Config premium modules show lock icon + no toggle; tapping opens PaywallModal (replaces bespoke modal)
- WIRE-03 satisfied: Event Details hides premium module cards on free events (per CONTEXT.md override of roadmap spec)
- PaywallModal component exists as shared full-screen modal for all screens
- PaywallBanner CTA opens external URL directly (no Alert.alert)
</success_criteria>

<output>
After completion, create `.planning/phases/36-paywall-wiring/36-01-SUMMARY.md`
</output>
