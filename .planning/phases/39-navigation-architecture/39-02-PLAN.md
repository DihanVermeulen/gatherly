---
phase: 39-navigation-architecture
plan: 02
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - apps/gatherly-mobile/app/(event)/[id]/_layout.tsx
  - apps/gatherly-mobile/app/(event)/[id]/edit-event.tsx
  - apps/gatherly-mobile/app/(event)/[id]/modules-config.tsx
  - apps/gatherly-mobile/app/(event)/[id]/polls.tsx
  - apps/gatherly-mobile/app/(event)/[id]/rsvp.tsx
  - apps/gatherly-mobile/app/(event)/[id]/potluck.tsx
  - apps/gatherly-mobile/app/(event)/[id]/potluck-setup.tsx
  - apps/gatherly-mobile/app/(event)/[id]/my-wishlist.tsx
  - apps/gatherly-mobile/app/(event)/[id]/view-wishlists.tsx
  - apps/gatherly-mobile/app/(event)/[id]/pricing.tsx
  - apps/gatherly-mobile/app/(event)/[id]/checkout.tsx
  - apps/gatherly-mobile/app/(event)/[id]/payment-success.tsx
  - apps/gatherly-mobile/app/_layout.tsx
  - apps/gatherly-mobile/app/(event)/[id]/(event-tabs)/hub.tsx
  - apps/gatherly-mobile/app/(event)/[id]/(event-tabs)/gift-exchange.tsx
  - apps/gatherly-mobile/app/(event)/[id]/(event-tabs)/manage.tsx
autonomous: true

must_haves:
  truths:
    - "The event-level tab bar persists when navigating to edit-event, modules-config, potluck-setup, and other event-context screens"
    - "All event-context screens correctly read the event ID from the route"
    - "Navigation between event-context screens uses relative paths within the (event) group"
    - "Old root-level event screen files are removed and their Stack.Screen entries are cleaned from root _layout.tsx"
  artifacts:
    - path: "apps/gatherly-mobile/app/(event)/[id]/edit-event.tsx"
      provides: "Edit event screen inside event context"
      min_lines: 100
    - path: "apps/gatherly-mobile/app/(event)/[id]/modules-config.tsx"
      provides: "Module config screen inside event context"
      min_lines: 50
    - path: "apps/gatherly-mobile/app/(event)/[id]/my-wishlist.tsx"
      provides: "My wishlist screen inside event context"
      min_lines: 50
    - path: "apps/gatherly-mobile/app/(event)/[id]/_layout.tsx"
      provides: "Stack with all event-context screens registered"
      contains: "edit-event|modules-config|potluck|polls|rsvp"
  key_links:
    - from: "apps/gatherly-mobile/app/(event)/[id]/(event-tabs)/hub.tsx"
      to: "/(event)/EVENT_ID/edit-event"
      via: "router.push for manage event button"
      pattern: "router\\.push.*edit-event"
    - from: "apps/gatherly-mobile/app/(event)/[id]/(event-tabs)/manage.tsx"
      to: "/(event)/EVENT_ID/edit-event"
      via: "router.push for edit event card"
      pattern: "router\\.push.*edit-event"
    - from: "apps/gatherly-mobile/app/(event)/[id]/edit-event.tsx"
      to: "/(event)/EVENT_ID/modules-config"
      via: "router.push for module config button"
      pattern: "router\\.push.*modules-config"
---

<objective>
Move all event-context screens into the `(event)/[id]/` route group so they live within the event navigation context, preserving the event tab bar. Update all cross-screen navigation paths and clean up the root layout.

Purpose: With screens inside the event context, the event tab bar persists during navigation to edit-event, modules-config, potluck, etc. (per the user's locked decision). This eliminates the tab bar disappearing when navigating deeper into event management.

Output: All event-context screens relocated, all router.push calls updated, root layout cleaned of moved screen entries.
</objective>

<execution_context>
@C:\Users\dihan\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\dihan\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/39-navigation-architecture/39-CONTEXT.md
@.planning/phases/39-navigation-architecture/39-RESEARCH.md
@.planning/phases/39-navigation-architecture/39-01-SUMMARY.md

Key source files (existing, to be moved):
@apps/gatherly-mobile/app/edit-event.tsx
@apps/gatherly-mobile/app/modules-config.tsx
@apps/gatherly-mobile/app/polls.tsx
@apps/gatherly-mobile/app/rsvp.tsx
@apps/gatherly-mobile/app/potluck.tsx
@apps/gatherly-mobile/app/potluck-setup.tsx
@apps/gatherly-mobile/app/my-wishlist.tsx
@apps/gatherly-mobile/app/view-wishlists.tsx
@apps/gatherly-mobile/app/pricing.tsx
@apps/gatherly-mobile/app/checkout.tsx
@apps/gatherly-mobile/app/payment-success.tsx
@apps/gatherly-mobile/app/_layout.tsx
@apps/gatherly-mobile/app/(event)/[id]/_layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move event-context screens into (event)/[id]/ and register in layout</name>
  <files>
    apps/gatherly-mobile/app/(event)/[id]/edit-event.tsx
    apps/gatherly-mobile/app/(event)/[id]/modules-config.tsx
    apps/gatherly-mobile/app/(event)/[id]/polls.tsx
    apps/gatherly-mobile/app/(event)/[id]/rsvp.tsx
    apps/gatherly-mobile/app/(event)/[id]/potluck.tsx
    apps/gatherly-mobile/app/(event)/[id]/potluck-setup.tsx
    apps/gatherly-mobile/app/(event)/[id]/my-wishlist.tsx
    apps/gatherly-mobile/app/(event)/[id]/view-wishlists.tsx
    apps/gatherly-mobile/app/(event)/[id]/pricing.tsx
    apps/gatherly-mobile/app/(event)/[id]/checkout.tsx
    apps/gatherly-mobile/app/(event)/[id]/payment-success.tsx
    apps/gatherly-mobile/app/(event)/[id]/_layout.tsx
  </files>
  <action>
**Move screen files** from `app/` into `app/(event)/[id]/`:

Use `git mv` for each file:
```
git mv app/edit-event.tsx app/(event)/[id]/edit-event.tsx
git mv app/modules-config.tsx app/(event)/[id]/modules-config.tsx
git mv app/polls.tsx app/(event)/[id]/polls.tsx
git mv app/rsvp.tsx app/(event)/[id]/rsvp.tsx
git mv app/potluck.tsx app/(event)/[id]/potluck.tsx
git mv app/potluck-setup.tsx app/(event)/[id]/potluck-setup.tsx
git mv app/my-wishlist.tsx app/(event)/[id]/my-wishlist.tsx
git mv app/view-wishlists.tsx app/(event)/[id]/view-wishlists.tsx
git mv app/pricing.tsx app/(event)/[id]/pricing.tsx
git mv app/checkout.tsx app/(event)/[id]/checkout.tsx
git mv app/payment-success.tsx app/(event)/[id]/payment-success.tsx
```

Run these from the `apps/gatherly-mobile/` directory.

**Update `(event)/[id]/_layout.tsx`** to register all moved screens as Stack.Screen entries:
```typescript
<Stack screenOptions={{ headerShown: false }}>
  <Stack.Screen name="(event-tabs)" />
  <Stack.Screen name="edit-event" />
  <Stack.Screen name="modules-config" />
  <Stack.Screen name="polls" />
  <Stack.Screen name="rsvp" />
  <Stack.Screen name="potluck" />
  <Stack.Screen name="potluck-setup" />
  <Stack.Screen name="my-wishlist" />
  <Stack.Screen name="view-wishlists" />
  <Stack.Screen name="pricing" />
  <Stack.Screen name="checkout" />
  <Stack.Screen name="payment-success" />
</Stack>
```

**Fix import paths** in all moved files:
- The `@/` alias should still work for `components/` and `contexts/` since it resolves to the project root
- But relative imports like `./contexts/AuthContext` or `./api/events` will break because the files moved deeper
- Search each moved file for imports starting with `./` or `../` and update them:
  - `./contexts/` → `@/app/contexts/` or `../../../contexts/` (use the `@/app/` pattern to be consistent with project conventions — check which pattern the file uses)
  - `./api/` → `@/app/api/` or `../../../api/`
  - `./utils/` → `@/app/utils/`
- Check `tsconfig.json` paths to confirm `@/` resolves correctly from the new location. The project uses `@/` aliased to the project root (e.g., `components/` or `app/`). Verify and fix if needed.

**CRITICAL: Each moved file currently gets its `id` from `useLocalSearchParams<{ id: string }>()`**. Since `[id]` is now in the route path `(event)/[id]/`, `useLocalSearchParams` will automatically provide `id`. This means the `?id=` query param pattern is no longer needed — the `id` comes from the URL path segment. Verify each file reads `id` correctly.

Some files also read additional params (e.g., `edit-wishlist-item` reads `eventId`). For screens that receive additional query params, ensure those still work.
  </action>
  <verify>
- All 11 screen files exist in `app/(event)/[id]/`: `ls apps/gatherly-mobile/app/\(event\)/\[id\]/`
- None of the 11 files remain at `app/` root: `ls apps/gatherly-mobile/app/edit-event.tsx` should fail
- `(event)/[id]/_layout.tsx` registers all 11 screens plus `(event-tabs)`
- No broken imports: `cd apps/gatherly-mobile && npx tsc --noEmit 2>&1 | grep -i "cannot find" | head -20`
  </verify>
  <done>All 11 event-context screens live inside (event)/[id]/ and are registered in the event layout Stack; import paths are fixed; id param reads from route segment</done>
</task>

<task type="auto">
  <name>Task 2: Update all navigation paths + clean root layout</name>
  <files>
    apps/gatherly-mobile/app/_layout.tsx
    apps/gatherly-mobile/app/(event)/[id]/(event-tabs)/hub.tsx
    apps/gatherly-mobile/app/(event)/[id]/(event-tabs)/gift-exchange.tsx
    apps/gatherly-mobile/app/(event)/[id]/(event-tabs)/manage.tsx
    apps/gatherly-mobile/app/(event)/[id]/edit-event.tsx
    apps/gatherly-mobile/app/(event)/[id]/modules-config.tsx
    apps/gatherly-mobile/app/(event)/[id]/potluck.tsx
    apps/gatherly-mobile/app/(event)/[id]/potluck-setup.tsx
    apps/gatherly-mobile/app/(event)/[id]/view-wishlists.tsx
    apps/gatherly-mobile/app/(event)/[id]/my-wishlist.tsx
    apps/gatherly-mobile/app/(event)/[id]/pricing.tsx
  </files>
  <action>
**1. Update `router.push` calls in ALL moved and new files.**

Since all event-context screens now live under `(event)/[id]/`, navigation between them no longer needs query params for `id` — the `id` is already in the URL path. Use paths relative to the event context:

**In hub.tsx (event-details content):**
- `router.push('/view-wishlists?id=...')` → `router.push('/(event)/${id}/view-wishlists' as any)` (or gift-exchange tab navigation depending on Plan 01 decisions)
- `router.push('/potluck-setup?id=...')` → `router.push('/(event)/${id}/potluck-setup' as any)`
- `router.push('/potluck?id=...')` → `router.push('/(event)/${id}/potluck' as any)`
- `router.push('/polls?id=...')` → `router.push('/(event)/${id}/polls' as any)`
- `router.push('/rsvp?id=...')` → `router.push('/(event)/${id}/rsvp' as any)`
- `router.push('/edit-event?id=...')` → `router.push('/(event)/${id}/edit-event' as any)`

**In gift-exchange.tsx:**
- `router.push('/my-wishlist?id=...')` → `router.push('/(event)/${id}/my-wishlist' as any)`
- `router.push('/view-wishlists?id=...')` → `router.push('/(event)/${id}/view-wishlists' as any)`

**In manage.tsx:**
- `router.push('/edit-event?id=...')` → `router.push('/(event)/${id}/edit-event' as any)`
- `router.push('/modules-config?id=...')` → `router.push('/(event)/${id}/modules-config' as any)`

**In edit-event.tsx:**
- `router.push('/edit-event-details?id=...')` → keep as-is for now (Plan 03 converts to modal sheet)
- `router.push('/manage-exclusions?id=...')` → keep as-is for now (Plan 03 converts to modal sheet)
- `router.push('/modules-config?id=...')` → `router.push('/(event)/${id}/modules-config' as any)`

**In modules-config.tsx:**
- Any navigation back or to other screens, update accordingly

**In potluck.tsx:**
- `router.push('/potluck-setup?id=...')` → `router.push('/(event)/${id}/potluck-setup' as any)`

**In potluck-setup.tsx:**
- `router.push('/potluck?id=...')` → `router.push('/(event)/${id}/potluck' as any)`

**In view-wishlists.tsx:**
- `router.push('/my-wishlist?id=...')` → `router.push('/(event)/${id}/my-wishlist' as any)`

**In pricing.tsx:**
- `router.push('/checkout?eventId=...')` → `router.push('/(event)/${id}/checkout' as any)` — Note: checkout may use `eventId` param, adapt accordingly. If checkout reads `eventId` from params, it now gets `id` from the route segment instead. Update checkout.tsx to read `id` instead of `eventId`, or pass it as a query param if needed.

**In my-wishlist.tsx:**
- `router.push('/edit-wishlist-item?id=...&eventId=...')` → keep as-is for now (Plan 03 converts to modal sheet). Note: `edit-wishlist-item.tsx` and `manage-exclusions.tsx` and `edit-event-details.tsx` remain at root level since they are being converted to modal sheets in Plan 03.

**2. Clean root `_layout.tsx`:**
- Remove the following `Stack.Screen` entries from the authenticated `Stack.Protected` guard (they are now registered in `(event)/[id]/_layout.tsx`):
  - `edit-event`
  - `event-details`
  - `modules-config`
  - `my-wishlist`
  - `view-wishlists`
  - `polls`
  - `rsvp`
  - `potluck`
  - `potluck-setup`
  - `pricing`
  - `checkout`
  - `payment-success`
- Keep these entries in root layout (NOT moved):
  - `(tabs)` — global tabs
  - `(event)` — event context group (added in Plan 01)
  - `edit-event-details` — stays at root until Plan 03 converts to sheet
  - `manage-exclusions` — stays at root until Plan 03 converts to sheet
  - `edit-wishlist-item` — stays at root until Plan 03 converts to sheet
  - `modal` — generic modal
  - `onboarding/profile-setup`, `onboarding/preferences` — onboarding
  - `welcome`, `sign-in`, `register` — auth screens
  - `join`, `magic-link/[token]` — public routes

**3. Delete `event-details.tsx` from root:**
- The old `app/event-details.tsx` is replaced by `hub.tsx`. Delete it: `rm app/event-details.tsx`
- Verify no other files import from or reference `event-details` (grep first).
  </action>
  <verify>
- Grep for old navigation patterns: `grep -r "router.push.*'/edit-event\?id" apps/gatherly-mobile/app/\(event\)/` should return 0 results (all updated)
- Grep for old patterns with query params: `grep -r "router.push.*'/(edit-event|modules-config|polls|rsvp|potluck|my-wishlist|view-wishlists)\?id=" apps/gatherly-mobile/app/` should return 0 results for moved screens
- Root `_layout.tsx` does NOT contain Stack.Screen for `edit-event`, `event-details`, `modules-config`, etc.
- `event-details.tsx` no longer exists at root: `ls apps/gatherly-mobile/app/event-details.tsx` should fail
- App launches and basic navigation works: tap event → hub loads → tap Gifts tab → gift exchange screen → tap Manage tab → manage screen → tap Edit Event → edit-event screen loads with event tab bar still visible
  </verify>
  <done>All event-context navigation uses (event)/[id]/ paths; root layout is clean with only non-event screens; event tab bar persists on all event-context screens; old event-details.tsx removed</done>
</task>

</tasks>

<verification>
1. Launch app, tap event card → event hub with 3-tab bar
2. Tap Gifts tab → gift exchange screen, tap My Wishlist → my-wishlist loads (event tab bar still visible)
3. Tap Manage tab → manage screen, tap Edit Event → edit-event loads (event tab bar still visible)
4. From edit-event, tap Module Config → modules-config loads (event tab bar still visible)
5. From hub, tap a module card (potluck, polls, rsvp) → correct screen loads (event tab bar still visible)
6. Back navigation works at every level (back from edit-event returns to manage or hub, back from hub returns to events list)
7. Global tab bar visible on events list and profile; hidden inside event context
8. Join flow navigates to event hub correctly
</verification>

<success_criteria>
- All 11 event-context screens live under (event)/[id]/ with correct Stack registration
- All router.push calls use the new (event)/[id]/ path pattern (no more ?id= query params for moved screens)
- Event tab bar persists on all event-context screens (edit-event, modules-config, potluck, polls, etc.)
- Root layout only contains non-event screens plus the (event) group entry
- Old event-details.tsx is deleted
- No broken navigation paths in the app
</success_criteria>

<output>
After completion, create `.planning/phases/39-navigation-architecture/39-02-SUMMARY.md`
</output>
