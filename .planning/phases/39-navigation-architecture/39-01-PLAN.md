---
phase: 39-navigation-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/gatherly-mobile/app/_layout.tsx
  - apps/gatherly-mobile/app/(event)/[id]/_layout.tsx
  - apps/gatherly-mobile/app/(event)/[id]/(event-tabs)/_layout.tsx
  - apps/gatherly-mobile/app/(event)/[id]/(event-tabs)/hub.tsx
  - apps/gatherly-mobile/app/(event)/[id]/(event-tabs)/gift-exchange.tsx
  - apps/gatherly-mobile/app/(event)/[id]/(event-tabs)/manage.tsx
  - apps/gatherly-mobile/app/(tabs)/index.tsx
  - apps/gatherly-mobile/app/join.tsx
autonomous: true

must_haves:
  truths:
    - "Tapping an event card from the events list navigates to the event hub with a 3-tab event bar (Hub, Gifts, Manage)"
    - "The global tab bar (Events, Profile) is NOT visible when inside the event context"
    - "The event-level tab bar IS visible on all 3 event tab screens"
    - "Back gesture/button from the event hub returns to the events list"
    - "BottomSheetModalProvider is available throughout the app for sheet modals"
  artifacts:
    - path: "apps/gatherly-mobile/app/(event)/[id]/_layout.tsx"
      provides: "Stack wrapper for event context"
      contains: "Stack.Screen.*event-tabs"
    - path: "apps/gatherly-mobile/app/(event)/[id]/(event-tabs)/_layout.tsx"
      provides: "Event-level tab navigator with Hub, Gifts, Manage tabs"
      contains: "Tabs.Screen.*hub"
    - path: "apps/gatherly-mobile/app/(event)/[id]/(event-tabs)/hub.tsx"
      provides: "Event hub screen (adapted from event-details.tsx)"
      min_lines: 50
    - path: "apps/gatherly-mobile/app/(event)/[id]/(event-tabs)/gift-exchange.tsx"
      provides: "Gift exchange tab placeholder with navigation to my-wishlist and view-wishlists"
      min_lines: 20
    - path: "apps/gatherly-mobile/app/(event)/[id]/(event-tabs)/manage.tsx"
      provides: "Organizer manage tab with navigation to edit-event, modules-config"
      min_lines: 20
  key_links:
    - from: "apps/gatherly-mobile/app/(tabs)/index.tsx"
      to: "/(event)/EVENT_ID/(event-tabs)/hub"
      via: "router.push"
      pattern: "router\\.push.*\\(event\\)"
    - from: "apps/gatherly-mobile/app/_layout.tsx"
      to: "BottomSheetModalProvider"
      via: "wraps Stack inside GestureHandlerRootView"
      pattern: "BottomSheetModalProvider"
---

<objective>
Create the event-contextual navigation architecture: nested route groups for event tabs that replace the global tab bar when inside an event, plus the BottomSheetModalProvider infrastructure for modal sheets.

Purpose: This is the structural backbone of the navigation overhaul. Once this plan completes, the app has two navigation contexts (global tabs and event tabs) and the entry point routes to the new event hub.

Output: Working event route group with 3-tab layout (Hub, Gifts, Manage), BottomSheetModalProvider in root layout, updated navigation from events list into the new route structure.
</objective>

<execution_context>
@C:\Users\dihan\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\dihan\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-navigation-architecture/39-CONTEXT.md
@.planning/phases/39-navigation-architecture/39-RESEARCH.md

Key source files:
@apps/gatherly-mobile/app/_layout.tsx
@apps/gatherly-mobile/app/(tabs)/_layout.tsx
@apps/gatherly-mobile/app/(tabs)/index.tsx
@apps/gatherly-mobile/app/event-details.tsx
@apps/gatherly-mobile/app/edit-event.tsx
@apps/gatherly-mobile/app/contexts/AuthContext.tsx
@apps/gatherly-mobile/app/contexts/EventsContext.tsx
@apps/gatherly-mobile/components/ui/bottomsheet/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root layout infrastructure + event route group layouts</name>
  <files>
    apps/gatherly-mobile/app/_layout.tsx
    apps/gatherly-mobile/app/(event)/[id]/_layout.tsx
    apps/gatherly-mobile/app/(event)/[id]/(event-tabs)/_layout.tsx
  </files>
  <action>
**1. Modify `app/_layout.tsx`:**
- Import `BottomSheetModalProvider` from `@gorhom/bottom-sheet`
- Wrap the `<Stack>` with `<BottomSheetModalProvider>` inside `GestureHandlerRootView` (after `<View>` and `<SafeAreaView>` but wrapping `<ThemeProvider>` and its `<Stack>`)
- Add `<Stack.Screen name="(event)" options={{ headerShown: false }} />` inside the authenticated `Stack.Protected` guard, BEFORE the `(tabs)` screen entry
- Keep ALL existing Stack.Screen entries for now (they will be removed in Plan 02 when screens move). Do NOT remove `event-details`, `edit-event`, `modules-config`, etc. yet — that would break the app before the migration completes.

**2. Create `app/(event)/[id]/_layout.tsx`:**
- Create directories: `app/(event)/` and `app/(event)/[id]/`
- This is a Stack layout that wraps the event context
- Register these Stack.Screen entries with `headerShown: false`:
  - `(event-tabs)` — the tab group (the initial/default route)
- For now only register the tab group. Other event-context screens (edit-event, modules-config, etc.) will be registered here in Plan 02 when they move.
- Export default function `EventLayout`

**3. Create `app/(event)/[id]/(event-tabs)/_layout.tsx`:**
- Create directory: `app/(event)/[id]/(event-tabs)/`
- Import `Tabs` from `expo-router`
- Import `Home`, `Gift`, `Settings` from `lucide-react-native`
- Import `useSession` from `@/app/contexts/AuthContext` to determine organizer status
- Use `useLocalSearchParams` to get `id`, then look up the event from EventsContext to check if current user is organizer (compare `event.organizerId` to `user.id` — same pattern as event-details.tsx uses)
- Configure 3 tabs:
  - `hub` — title "Hub", icon `Home`, always visible
  - `gift-exchange` — title "Gifts", icon `Gift`, always visible
  - `manage` — title "Manage", icon `Settings`, use `href: isOrganizer ? undefined : null` to hide for non-organizers
- Tab bar styling: match the global tab bar pattern — `tabBarActiveTintColor: '#0d9488'` (teal), `tabBarInactiveTintColor: '#94a3b8'`, white background, `borderTopColor: '#e2e8f0'`
- Set `screenOptions={{ headerShown: false }}`

**IMPORTANT:** The `isOrganizer` check pattern from existing code is: the user object has no `participantId` field (full account user) AND the event's `organizerId === user.id`. Check `event-details.tsx` for the exact pattern used (`user?.participantId === undefined` means full account, then check organizerId).
  </action>
  <verify>
- Directory structure exists: `ls apps/gatherly-mobile/app/\(event\)/\[id\]/\(event-tabs\)/`
- No TypeScript errors in the 3 files: `cd apps/gatherly-mobile && npx tsc --noEmit --pretty 2>&1 | head -50` (some pre-existing errors OK, but no NEW errors in the created files)
- `_layout.tsx` contains `BottomSheetModalProvider`
  </verify>
  <done>Root layout wraps Stack with BottomSheetModalProvider; event route group has Stack layout at [id] level and Tabs layout at (event-tabs) level with Hub/Gifts/Manage tabs</done>
</task>

<task type="auto">
  <name>Task 2: Create event tab screens + wire entry navigation</name>
  <files>
    apps/gatherly-mobile/app/(event)/[id]/(event-tabs)/hub.tsx
    apps/gatherly-mobile/app/(event)/[id]/(event-tabs)/gift-exchange.tsx
    apps/gatherly-mobile/app/(event)/[id]/(event-tabs)/manage.tsx
    apps/gatherly-mobile/app/(tabs)/index.tsx
    apps/gatherly-mobile/app/join.tsx
  </files>
  <action>
**1. Create `hub.tsx`** — adapt from `event-details.tsx`:
- Copy the FULL content of `event-details.tsx` into `hub.tsx`
- Change the component name to `EventHubScreen`
- The `useLocalSearchParams<{ id: string }>()` will still work because `[id]` is an ancestor dynamic segment — Expo Router makes it available to child routes
- Update module card navigation: instead of `router.push('/view-wishlists?id=...')`, navigate to the gift-exchange tab. For gift_exchange module taps, use `router.push('/(event)/${id}/(event-tabs)/gift-exchange' as any)` (or use relative navigation if cleaner). For other modules (potluck, polls, rsvp), keep the existing `router.push('/potluck?id=...')` etc. for now — these still work from the root Stack until Plan 02 moves them.
- Update the "Manage Event" button: instead of `router.push('/edit-event?id=...')`, navigate to the manage tab using the same pattern, or keep as-is since edit-event still lives at root for now.
- Update `onBack` in AppHeader: `router.back()` — this pops the entire (event) group back to the events list. This is correct.
- Keep ALL existing functionality (hero banner, module cards, date badge, etc.)

**2. Create `gift-exchange.tsx`** — a functional screen (not just a stub):
- Import `useLocalSearchParams` to get `id`
- Import `useRouter` from `expo-router`
- Import `useEvents` and `useSession` from contexts
- Show the event name as header (use AppHeader with title, NO back button — this is a tab screen)
- Display two main action buttons/cards:
  1. "My Wishlist" — navigates to `/my-wishlist?id=${id}` (still at root for now)
  2. "View All Wishlists" — navigates to `/view-wishlists?id=${id}` (still at root for now)
- If gift_exchange module is not active, show a message "Gift Exchange is not enabled for this event"
- Use GlueStack UI components (Text, Pressable) with the project's teal color scheme
- Style as a clean card-based layout with icons (Gift icon for each card)

**3. Create `manage.tsx`** — organizer management panel:
- Import `useLocalSearchParams` to get `id`
- Import `useRouter` from `expo-router`
- Show AppHeader with title "Manage Event" (NO back button — tab screen)
- Display action cards/buttons for organizer tasks:
  1. "Edit Event" — navigates to `/edit-event?id=${id}` (still at root for now)
  2. "Module Config" — navigates to `/modules-config?id=${id}` (still at root for now)
- Use GlueStack UI components, teal color scheme, card-based layout with icons (Settings, Pencil icons)

**4. Update `(tabs)/index.tsx`** — change event card navigation:
- Find the `router.push('/event-details?id=...')` call (line ~220)
- Replace with: `router.push('/(event)/${item.id}/(event-tabs)/hub' as any)`
- Use template literal with the event's ID

**5. Update `join.tsx`** — change post-join navigation:
- Find the `router.push('/event-details?id=...')` call (line ~98)
- Replace with: `router.push('/(event)/${joinedEventId}/(event-tabs)/hub' as any)`

**Do NOT delete `event-details.tsx` yet** — it stays as a fallback until all navigation paths are migrated. It will be removed in Plan 02.
  </action>
  <verify>
- All 3 tab screen files exist and have content
- `(tabs)/index.tsx` contains `(event)` in its router.push call
- `join.tsx` contains `(event)` in its router.push call
- Start the app: `cd apps/gatherly-mobile && npx expo start` — verify no crash on launch, tap an event card, confirm event hub loads with 3-tab bar visible at bottom
  </verify>
  <done>Event hub shows event details with 3-tab bar (Hub, Gifts, Manage); tapping an event card from the events list navigates to the event hub; join flow navigates to the event hub; Manage tab hidden for non-organizers; back navigation from hub returns to events list</done>
</task>

</tasks>

<verification>
1. Launch the app (`npx expo start`) and navigate to the events list
2. Tap an event card — should navigate to the event hub with the 3-tab event bar
3. The global tab bar (Events, Profile) should NOT be visible
4. Tap between Hub, Gifts, Manage tabs — all 3 should render their screens
5. On the Hub tab, back gesture/button returns to the events list
6. On the events list, global tab bar (Events, Profile) is visible again
7. If logged in as a participant (not organizer), the Manage tab should be hidden
</verification>

<success_criteria>
- Event route group (`(event)/[id]/(event-tabs)/`) fully functional with 3 tabs
- Global tab bar hidden when inside event context, visible when outside
- Event tab bar visible on all 3 tab screens
- BottomSheetModalProvider wrapping the app for modal sheets in later plans
- Navigation from events list and join flow uses the new route
- Back navigation works correctly from event hub to events list
</success_criteria>

<output>
After completion, create `.planning/phases/39-navigation-architecture/39-01-SUMMARY.md`
</output>
