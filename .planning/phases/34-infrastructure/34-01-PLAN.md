---
phase: 34-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/gatherly-mobile/app/api/events.ts
  - apps/api/src/routes/modules.ts
  - apps/api/src/routes/events.ts
autonomous: true

must_haves:
  truths:
    - "TEvent.planTier type is 'free' | 'premium' — the string 'standard' does not appear in the mobile codebase"
    - "RSVP, polls, and potluck endpoints respond without 403 for free-tier events — only white_elephant is in PREMIUM_MODULES"
    - "PATCH /api/events/:id/upgrade sets plan_tier = 'premium' and returns the full updated event"
    - "Repeated PATCH /api/events/:id/upgrade calls are idempotent — a second call returns 200 with the updated event, not an error"
  artifacts:
    - path: "apps/gatherly-mobile/app/api/events.ts"
      provides: "TEvent type definition"
      contains: "planTier?: 'free' | 'premium'"
    - path: "apps/api/src/routes/modules.ts"
      provides: "PREMIUM_MODULES constant"
      contains: "PREMIUM_MODULES = [\"white_elephant\"]"
    - path: "apps/api/src/routes/events.ts"
      provides: "upgrade route handler"
      contains: "router.patch.*/:id/upgrade"
  key_links:
    - from: "apps/api/src/routes/events.ts upgrade handler"
      to: "fetchEventById"
      via: "direct call after UPDATE — fetchEventById is file-scoped, no import needed"
      pattern: "fetchEventById\\(id\\)"
    - from: "apps/api/src/routes/modules.ts"
      to: "PREMIUM_MODULES array"
      via: "PREMIUM_MODULES.includes(moduleType) gate"
      pattern: "PREMIUM_MODULES\\.includes"
---

<objective>
Fix the planTier type union in the mobile codebase, reduce PREMIUM_MODULES to white_elephant only, and add the upgrade route to the API.

Purpose: These three changes are the contractual foundation that all paywall screens in Phase 35 depend on. The type fix ensures TypeScript consumers compile correctly; reducing PREMIUM_MODULES to ["white_elephant"] allows free-tier events to enable polls and potluck (their usage is then capped by the trial limits in Plan 02, not by an outright gate); the upgrade route is the only write path that promotes an event to premium.

Output: One mobile type change, one PREMIUM_MODULES reduction, one new PATCH route in events.ts.
</objective>

<execution_context>
@C:\Users\dihan\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\dihan\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix planTier type union in TEvent (mobile)</name>
  <files>apps/gatherly-mobile/app/api/events.ts</files>
  <action>
    On line 44, change the planTier field from:
      planTier?: 'free' | 'standard';
    to:
      planTier?: 'free' | 'premium';

    No other changes to this file. The comment "// Phase 25 additions" above the field can remain.
    All consumers use `=== "free"` comparisons and will continue to work without further changes.
  </action>
  <verify>grep -n "standard" apps/gatherly-mobile/app/api/events.ts — must return no matches</verify>
  <done>The string 'standard' does not appear anywhere in apps/gatherly-mobile/app/api/events.ts</done>
</task>

<task type="auto">
  <name>Task 2: Reduce PREMIUM_MODULES to white_elephant only and add upgrade route</name>
  <files>apps/api/src/routes/modules.ts, apps/api/src/routes/events.ts</files>
  <action>
    In apps/api/src/routes/modules.ts, line 9: change:
      const PREMIUM_MODULES = ["polls", "potluck", "rsvp", "white_elephant"];
    to:
      const PREMIUM_MODULES = ["white_elephant"];

    Rationale: polls and potluck are moved out of the hard gate so free events can enable them.
    Their usage is capped at the creation endpoints (Plan 02) via trial_limit_reached, not at the
    module-enable step. rsvp was already earmarked for removal. white_elephant remains premium-only.

    In apps/api/src/routes/events.ts, add the following route AFTER the DELETE /api/events/:id route block (which ends at approximately line 429 with the closing `);`):

    ```typescript
    // PATCH /api/events/:id/upgrade — promote event to premium tier (idempotent)
    router.patch(
      "/:id/upgrade",
      authenticateJWT,
      requireOrganizer,
      asyncHandler(async (req: Request, res: Response) => {
        const { id } = req.params;
        const result = await query(
          "UPDATE events SET plan_tier = 'premium' WHERE id = $1 AND organizer_id = $2 RETURNING id",
          [id, (req as any).user.userId],
        );
        if (result.rowCount === 0) {
          return res.status(403).json({ error: "Forbidden" });
        }
        const updatedEvent = await fetchEventById(id);
        res.json(updatedEvent);
      }),
    );
    ```

    fetchEventById is already defined as a file-scoped function in events.ts — no import is needed.
    asyncHandler, authenticateJWT, requireOrganizer are already imported at the top of events.ts.

    Idempotency: when plan_tier is already 'premium', the UPDATE still matches the WHERE clause
    (id = $1 AND organizer_id = $2 both hold), rowCount is 1, and the handler returns 200 with the
    event. A second call is indistinguishable from the first — no 403, no 500.
  </action>
  <verify>
    1. grep -n "PREMIUM_MODULES" apps/api/src/routes/modules.ts — must show ["white_elephant"] only; polls, potluck, rsvp must not appear in the array
    2. grep -n "upgrade" apps/api/src/routes/events.ts — must show the new PATCH route
    3. cd apps/api && npx tsc --noEmit — must pass with no errors
    4. Idempotency: confirm that PATCH /api/events/:id/upgrade called a second time (when plan_tier is already 'premium') returns 200 — the UPDATE WHERE clause still matches so rowCount = 1, no error branch fires
  </verify>
  <done>
    PREMIUM_MODULES contains exactly ["white_elephant"].
    PATCH /api/events/:id/upgrade route exists in events.ts, returns 200 on both first and second call, and TypeScript compiles cleanly.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
- grep -rn "standard" apps/gatherly-mobile/app/api/events.ts — zero matches
- grep -n "PREMIUM_MODULES" apps/api/src/routes/modules.ts — shows ["white_elephant"] only
- grep -n "polls\|potluck\|rsvp" apps/api/src/routes/modules.ts — must NOT appear inside PREMIUM_MODULES array
- grep -n "upgrade" apps/api/src/routes/events.ts — shows PATCH route handler
- cd apps/api && npx tsc --noEmit — exits 0
</verification>

<success_criteria>
1. TEvent.planTier is typed as 'free' | 'premium' in the mobile codebase
2. PREMIUM_MODULES is ["white_elephant"] — polls, potluck, and rsvp are absent so free-tier events can enable those modules (usage capped by Plan 02 trial limits)
3. PATCH /api/events/:id/upgrade exists, sets plan_tier = 'premium', returns updated event via fetchEventById
4. A second PATCH /api/events/:id/upgrade call on an already-premium event returns 200 (idempotent — UPDATE WHERE clause still matches)
5. TypeScript compiles without errors after all changes
</success_criteria>

<output>
After completion, create `.planning/phases/34-infrastructure/34-01-SUMMARY.md`
</output>
