---
phase: 35-paywall-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/gatherly-mobile/components/PaywallBanner.tsx
  - apps/gatherly-mobile/constants/upgrades.ts
  - apps/gatherly-mobile/app/api/plans.ts
  - apps/gatherly-mobile/app/_layout.tsx
autonomous: true

must_haves:
  truths:
    - "PaywallBanner renders contextual headline based on feature prop — participant_cap, potluck_trial, polls_trial, and module_locked each produce distinct copy"
    - "Magic-link participant sees 'Ask your organiser to upgrade this event' with NO CTA button"
    - "Organizer sees 'Upgrade to Premium' CTA button that triggers a confirmation Alert before opening external URL"
    - "Banner uses amber/gold visual style — not teal, not the default brand color"
    - "plansApi.upgrade(eventId) calls PATCH /api/events/:id/upgrade and returns TEvent"
    - "pricing screen is registered in _layout.tsx under Stack.Protected with headerShown: false"
  artifacts:
    - path: "apps/gatherly-mobile/components/PaywallBanner.tsx"
      provides: "Shared paywall banner component"
      exports: ["PaywallBanner", "PaywallFeature"]
      min_lines: 50
    - path: "apps/gatherly-mobile/constants/upgrades.ts"
      provides: "UPGRADE_REQUEST_URL constant"
      exports: ["UPGRADE_REQUEST_URL"]
    - path: "apps/gatherly-mobile/app/api/plans.ts"
      provides: "plansApi client wrapping PATCH /upgrade"
      exports: ["plansApi"]
    - path: "apps/gatherly-mobile/app/_layout.tsx"
      provides: "pricing screen route registration"
      contains: "name=\"pricing\""
  key_links:
    - from: "components/PaywallBanner.tsx"
      to: "constants/upgrades.ts"
      via: "import UPGRADE_REQUEST_URL"
      pattern: "import.*UPGRADE_REQUEST_URL.*from.*constants/upgrades"
    - from: "components/PaywallBanner.tsx"
      to: "expo-web-browser"
      via: "WebBrowser.openBrowserAsync"
      pattern: "openBrowserAsync"
    - from: "app/api/plans.ts"
      to: "PATCH /api/events/:id/upgrade"
      via: "apiClient.patch"
      pattern: "apiClient\\.patch.*upgrade"
---

<objective>
Build the shared PaywallBanner component, the plansApi client, the upgrade URL constant, and register the pricing screen route — establishing all the canonical upgrade UX primitives that Phase 36 will wire into every locked screen.

Purpose: No individual screen should ever invent its own upgrade flow. This plan creates the single source of truth for upgrade UX.
Output: PaywallBanner.tsx, upgrades.ts, plans.ts, updated _layout.tsx
</objective>

<execution_context>
@C:\Users\dihan\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\dihan\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-paywall-components/35-CONTEXT.md
@.planning/phases/35-paywall-components/35-RESEARCH.md
@apps/gatherly-mobile/app/_layout.tsx
@apps/gatherly-mobile/app/api/events.ts
@apps/gatherly-mobile/app/api/client.ts
@apps/gatherly-mobile/constants/Colors.ts
@apps/gatherly-mobile/components/OfflineBanner.tsx
@apps/gatherly-mobile/components/AppHeader.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create constants, plansApi client, and PaywallBanner component</name>
  <files>
    apps/gatherly-mobile/constants/upgrades.ts
    apps/gatherly-mobile/app/api/plans.ts
    apps/gatherly-mobile/components/PaywallBanner.tsx
  </files>
  <action>
    **1. Create `constants/upgrades.ts`:**
    - Export `UPGRADE_REQUEST_URL` as a single string constant
    - Value: `"https://form.typeform.com/to/PLACEHOLDER"` (placeholder — replaced at launch)
    - This is the only place the URL lives in the codebase

    **2. Create `app/api/plans.ts`:**
    - Import `apiClient` from `./client` and `TEvent` from `./events`
    - Export `plansApi` object with one method:
      - `upgrade(eventId: string): Promise<TEvent>` — calls `apiClient.patch<TEvent>(\`/api/events/${eventId}/upgrade\`)` and returns `response.data`
    - Follow the exact pattern of existing API files (modules.ts, events.ts)

    **3. Create `components/PaywallBanner.tsx`:**
    - Export the `PaywallFeature` type:
      ```
      type PaywallFeature =
        | 'participant_cap'
        | 'potluck_trial'
        | 'polls_trial'
        | { type: 'module_locked'; moduleName: string };
      ```
    - Export the `PaywallBanner` component with props:
      ```
      { feature: PaywallFeature; isParticipant: boolean }
      ```
    - `getHeadline(feature, isParticipant)` function that returns contextual copy:
      - isParticipant=true: "Ask your organiser to upgrade this event"
      - participant_cap: "Free events are limited to 20 guests"
      - potluck_trial: "Upgrade for unlimited potluck categories"
      - polls_trial: "Upgrade for unlimited polls"
      - module_locked: "Unlock {moduleName} with Premium"
      - fallback: "Upgrade to Premium"
    - Visual style — amber/gold inline styles (NOT teal):
      - Card background: `#fffbeb` (amber-50)
      - Card border: 1px `#f59e0b` (amber-500)
      - Border radius: 16
      - Text color: `#92400e` (amber-900)
      - CTA button background: `#d97706` (amber-600), white text, border radius 10
    - Layout: View with margin 16, marginBottom 24, padding 16
      - Row: Lock icon (from lucide-react-native, size 18, color #92400e) + headline Text
      - Conditional: if NOT isParticipant, render a Button with "Upgrade to Premium" text
      - If isParticipant: only show the headline (no CTA button)
    - CTA behavior: `Alert.alert("Request Premium Access", "You'll be taken to an external form to request Premium access.", [Cancel, Continue])` — Continue calls `WebBrowser.openBrowserAsync(UPGRADE_REQUEST_URL)`
    - Do NOT make the banner dismissable (no X button, no close handler)
    - Do NOT use a modal or bottom sheet — this is an inline card rendered by the parent screen
    - Use inline styles for all amber colors (safe from NativeWind purge issues)
    - Import Lock from lucide-react-native, Text from @/components/ui/text, Button+ButtonText from @/components/ui/button
  </action>
  <verify>
    - `npx tsc --noEmit` in gatherly-mobile compiles without errors
    - PaywallBanner.tsx exports both `PaywallBanner` and `PaywallFeature`
    - plans.ts exports `plansApi` with `upgrade` method
    - upgrades.ts exports `UPGRADE_REQUEST_URL`
  </verify>
  <done>
    - PaywallBanner renders 5 distinct headlines for 5 feature triggers
    - Organizer sees "Upgrade to Premium" CTA; participant sees no CTA
    - CTA triggers Alert confirmation then opens external URL
    - plansApi.upgrade() wraps PATCH /api/events/:id/upgrade
    - UPGRADE_REQUEST_URL is a single constant in constants/upgrades.ts
  </done>
</task>

<task type="auto">
  <name>Task 2: Register pricing screen route in _layout.tsx</name>
  <files>
    apps/gatherly-mobile/app/_layout.tsx
  </files>
  <action>
    Add a new `Stack.Screen` entry inside `Stack.Protected guard={!!session}` block in `_layout.tsx`:

    ```
    <Stack.Screen
      name="pricing"
      options={{ headerShown: false }}
    />
    ```

    Place it after the `potluck-setup` entry (last in the authenticated block, before the closing `</Stack.Protected>` tag). This registers the route so `app/pricing.tsx` (created in Plan 35-02) will be accessible via `router.push('/pricing?eventId=X')`.

    Do NOT create `app/pricing.tsx` in this plan — that is Plan 35-02's scope.
  </action>
  <verify>
    - `_layout.tsx` contains `name="pricing"` inside the authenticated `Stack.Protected` block
    - No duplicate screen registrations
    - `npx tsc --noEmit` passes (the screen file not existing yet is fine — Expo Router handles this gracefully)
  </verify>
  <done>
    - pricing route registered in _layout.tsx under the authenticated guard
    - headerShown is false (AppHeader will be rendered by the pricing screen itself)
  </done>
</task>

</tasks>

<verification>
1. `cd apps/gatherly-mobile && npx tsc --noEmit` — no type errors from new files
2. `grep -r "UPGRADE_REQUEST_URL" apps/gatherly-mobile/` — appears in constants/upgrades.ts and components/PaywallBanner.tsx
3. `grep "plansApi" apps/gatherly-mobile/app/api/plans.ts` — export exists
4. `grep 'name="pricing"' apps/gatherly-mobile/app/_layout.tsx` — route registered
5. PaywallBanner.tsx does NOT contain any teal hex values (#0d9488, #0f766e)
</verification>

<success_criteria>
- PaywallBanner component exists with exported PaywallFeature type and contextual headline logic
- Amber/gold visual style (not teal) with Lock icon
- Organizer CTA triggers Alert -> WebBrowser.openBrowserAsync flow
- Participant variant shows "Ask your organiser" with no CTA button
- plansApi wraps PATCH /upgrade endpoint
- UPGRADE_REQUEST_URL constant is the single source for the external form URL
- pricing route registered in _layout.tsx authenticated block
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/35-paywall-components/35-01-SUMMARY.md`
</output>
