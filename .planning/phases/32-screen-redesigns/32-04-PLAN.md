---
phase: 32-screen-redesigns
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/gatherly-mobile/app/event-details.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Secret Assignment card only renders when gift_exchange module is active"
    - "Bottom action bar (View Wishlists / Add My Gifts) is removed from event-details"
    - "Tapping an inactive, non-comingSoon module card (e.g. Potluck) shows a toast"
  artifacts:
    - path: "apps/gatherly-mobile/app/event-details.tsx"
      provides: "Gated Secret Assignment, removed bottom bar, working inactive-module toast"
  key_links:
    - from: "event-details.tsx handleModuleTap"
      to: "toast.show()"
      via: "inactive non-comingSoon branch before switch"
      pattern: "if \\(!isActive && !entry\\.comingSoon\\)"
---

<objective>
Fix three interrelated issues in event-details.tsx:
1. The Secret Assignment card renders unconditionally — gate it behind activeModuleTypes.has('gift_exchange').
2. The bottom action bar (View Wishlists / Add My Gifts) is always-visible — remove it entirely (buttons accessible through Gift Exchange module card).
3. The Potluck module card fires no toast when tapped because the Pressable is disabled when the module is inactive — fix the disabled logic and add an early-return toast branch.

Purpose: Event Hub should only show gift-exchange content when that module is active, and inactive-but-not-coming-soon modules should give user feedback on tap.
Output: event-details.tsx with three targeted fixes applied.
</objective>

<execution_context>
@C:\Users\dihan\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\dihan\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/32-screen-redesigns/32-01-SUMMARY.md
@apps/gatherly-mobile/app/event-details.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate Secret Assignment card + remove bottom action bar</name>
  <files>apps/gatherly-mobile/app/event-details.tsx</files>
  <action>
    1. Find the Secret Assignment card block (starts around line 404 with comment `{/* ── Secret Assignment card ──`). Wrap the entire outer `<View>` in a conditional: `{activeModuleTypes.has('gift_exchange') && ( ... )}`. The closing brace/paren goes after the closing `</View>` tag of that card block (around line 444).

    2. Find the bottom absolute-positioned action bar (starts around line 575–580, the `<View style={{ position: "absolute", bottom: 0, ...` containing the View Wishlists and Add My Gifts buttons). Remove the entire `<View>` block including both Button children. Also remove any `paddingBottom` spacer that was added specifically to compensate for this bar (check for a View with paddingBottom: 80 or similar near the end of the ScrollView content). Do NOT remove the outer SafeAreaView closing tags.

    3. After removing the bottom bar, verify the `Gift` import from lucide-react-native is still used elsewhere in the file (it is, in the Gift Exchange module status line). Keep the import.
  </action>
  <verify>Search event-details.tsx for "Your Secret Assignment" — confirm it is inside a conditional block containing `activeModuleTypes.has('gift_exchange')`. Search for "View Wishlists" — confirm it no longer appears in event-details.tsx.</verify>
  <done>Secret Assignment card conditionally rendered; bottom bar JSX fully removed.</done>
</task>

<task type="auto">
  <name>Task 2: Fix inactive-module toast — Potluck tappable when inactive</name>
  <files>apps/gatherly-mobile/app/event-details.tsx</files>
  <action>
    Two changes in event-details.tsx:

    **A. Fix the Pressable disabled prop (around line 503):**
    Change:
    ```
    disabled={!isTappable}
    ```
    To:
    ```
    disabled={isComingSoon}
    ```
    This allows the Pressable to fire for inactive-but-not-coming-soon modules (like Potluck).

    **B. Add inactive-module toast in handleModuleTap (around line 206):**
    The current early-return at line 209 is:
    ```typescript
    if (!isActive) return;
    ```
    Replace it with:
    ```typescript
    if (!isActive) {
      toast.show({
        placement: "bottom",
        duration: 3000,
        render: ({ id: toastId }) => (
          <Toast nativeID={`toast-${toastId}`} action="info" variant="solid">
            <ToastTitle>Enable this module in Module Config to use it</ToastTitle>
          </Toast>
        ),
      });
      return;
    }
    ```

    The `isTappable` variable used for `onPress` guard on line 502 (`onPress={() => isTappable && handleModuleTap(entry)}`) should also be updated to `isActive && !isComingSoon` so the handler fires for inactive modules. Actually since disabled is now `isComingSoon` only, the onPress will fire regardless of isActive — remove the `isTappable &&` guard: change to `onPress={() => handleModuleTap(entry)}`.
  </action>
  <verify>In event-details.tsx: confirm `disabled={isComingSoon}` exists in the module Pressable; confirm `handleModuleTap` has an `if (!isActive)` branch that calls `toast.show()`; confirm no `!isTappable` in disabled prop.</verify>
  <done>Tapping any inactive non-comingSoon module card shows a "Enable this module in Module Config" toast. Coming Soon modules remain non-interactive.</done>
</task>

</tasks>

<verification>
- Secret Assignment card only visible when gift_exchange is in activeModuleTypes
- No "View Wishlists" / "Add My Gifts" bottom bar rendered in event-details.tsx
- Module Pressable disabled only when isComingSoon (not when merely inactive)
- handleModuleTap shows toast for inactive non-coming-soon modules before switch
</verification>

<success_criteria>
- event-details.tsx compiles with no TypeScript errors (pnpm check-types from repo root)
- Secret Assignment block wrapped in activeModuleTypes.has('gift_exchange') guard
- Bottom bar with View Wishlists + Add My Gifts buttons fully removed
- Potluck card (inactive) fires toast on tap
</success_criteria>

<output>
After completion, create `.planning/phases/32-screen-redesigns/32-04-SUMMARY.md`
</output>
