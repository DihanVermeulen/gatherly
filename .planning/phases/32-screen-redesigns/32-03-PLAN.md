---
phase: 32-screen-redesigns
plan: 03
type: execute
wave: 1
depends_on: ["32-01", "32-02"]
files_modified:
  - apps/gatherly-mobile/app/event-details.tsx
  - apps/gatherly-mobile/app/edit-event.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Event Details screen shows the event cover photo hero when a cover photo is set"
    - "Event Details screen shows 'Organized by [name]' sourced from organizer profile"
    - "Manage Event screen shows cover photo thumbnail when a cover photo is set"
  artifacts:
    - path: "apps/gatherly-mobile/app/event-details.tsx"
      provides: "useEffect calling eventsApi.getById to fetch coverPhotoUrl + organizerName"
      contains: "eventsApi.getById"
    - path: "apps/gatherly-mobile/app/edit-event.tsx"
      provides: "useEffect calling eventsApi.getById to fetch coverPhotoUrl for thumbnail"
      contains: "eventsApi.getById"
  key_links:
    - from: "apps/gatherly-mobile/app/event-details.tsx"
      to: "eventsApi.getById"
      via: "useEffect on mount, stores coverPhotoUrl + organizerName in local state"
      pattern: "eventsApi\\.getById\\(id\\)"
    - from: "apps/gatherly-mobile/app/edit-event.tsx"
      to: "eventsApi.getById"
      via: "useEffect on mount, stores coverPhotoUrl in local state"
      pattern: "eventsApi\\.getById\\(id\\)"
    - from: "event-details.tsx hero Image"
      to: "local state detailCoverPhotoUrl"
      via: "source={{ uri: detailCoverPhotoUrl }}"
      pattern: "detailCoverPhotoUrl"
---

<objective>
Close 3 verification gaps where coverPhotoUrl and organizerName are always undefined because the list endpoint (GET /api/events) does not return these fields, but both screens read them exclusively from EventsContext which only calls getAll().

Purpose: The cover photo hero, "Organized by" line, and manage-event thumbnail never render actual data. This plan wires each screen to fetch the single-event detail endpoint on mount, matching the existing pattern used for modulesApi.getModules.

Output: Both event-details.tsx and edit-event.tsx fetch detail fields from eventsApi.getById(id) and use local state for coverPhotoUrl / organizerName.
</objective>

<execution_context>
@C:\Users\dihan\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\dihan\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-screen-redesigns/32-VERIFICATION.md
@apps/gatherly-mobile/app/api/events.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire event-details.tsx to fetch detail fields on mount</name>
  <files>apps/gatherly-mobile/app/event-details.tsx</files>
  <action>
In EventDetailsScreen, add two pieces of local state:

```typescript
const [detailCoverPhotoUrl, setDetailCoverPhotoUrl] = useState<string | null>(null);
const [detailOrganizerName, setDetailOrganizerName] = useState<string | null>(null);
```

Add a useEffect (right after the existing modulesApi useEffect at line ~160-162) that fetches the single-event detail:

```typescript
useEffect(() => {
  eventsApi.getById(id).then((detail) => {
    setDetailCoverPhotoUrl(detail.coverPhotoUrl ?? null);
    setDetailOrganizerName(detail.organizerName ?? null);
  }).catch(() => {});
}, [id]);
```

Then update the two render locations to use local state instead of context:

1. Hero cover photo (line ~252): Change `event.coverPhotoUrl` to `detailCoverPhotoUrl` in the ternary condition and the Image source uri.

2. Organized by line (line ~324): Change `event.organizerName` to `detailOrganizerName` in the condition and the rendered text.

Do NOT change any other part of the file. The eventsApi import already exists (it is re-exported from `./api/events`; if not imported, add it alongside the existing modulesApi import).

Note: eventsApi is already imported in the file via `TEventModule` from `./api/events` but the `eventsApi` named export may not be imported yet. Check the imports at the top of the file; if `eventsApi` is not imported, add it to the existing import from `./api/events`.
  </action>
  <verify>
1. Search the file for `eventsApi.getById` -- must appear in a useEffect
2. Search for `detailCoverPhotoUrl` -- must appear in useState declaration and in the hero ternary
3. Search for `detailOrganizerName` -- must appear in useState declaration and in the organized-by conditional
4. Confirm `event.coverPhotoUrl` and `event.organizerName` are no longer referenced in the render JSX (only local state vars used)
5. Run `cd apps/gatherly-mobile && npx tsc --noEmit --pretty` -- no type errors in event-details.tsx
  </verify>
  <done>
- The hero Image branch uses detailCoverPhotoUrl from local state (populated by getById)
- The "Organized by" line uses detailOrganizerName from local state
- No type errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire edit-event.tsx to fetch coverPhotoUrl on mount</name>
  <files>apps/gatherly-mobile/app/edit-event.tsx</files>
  <action>
In the edit-event screen component, add local state for the cover photo URL:

```typescript
const [detailCoverPhotoUrl, setDetailCoverPhotoUrl] = useState<string | null>(null);
```

Add a useEffect that fetches the single-event detail (place it near the other useEffects in the file):

```typescript
useEffect(() => {
  eventsApi.getById(id).then((detail) => {
    setDetailCoverPhotoUrl(detail.coverPhotoUrl ?? null);
  }).catch(() => {});
}, [id]);
```

The `eventsApi` import already exists at line 32. No new imports needed.

Then update the cover photo thumbnail (line ~371): Change `event.coverPhotoUrl` to `detailCoverPhotoUrl` in both the ternary condition and the Image source uri.

Do NOT change any other part of the file.
  </action>
  <verify>
1. Search the file for `eventsApi.getById` -- must appear in a useEffect
2. Search for `detailCoverPhotoUrl` -- must appear in useState and in the thumbnail ternary (around line 371)
3. Confirm `event.coverPhotoUrl` is no longer referenced for the thumbnail render
4. Run `cd apps/gatherly-mobile && npx tsc --noEmit --pretty` -- no type errors in edit-event.tsx
  </verify>
  <done>
- The Event Details card thumbnail uses detailCoverPhotoUrl from local state (populated by getById)
- No type errors
  </done>
</task>

</tasks>

<verification>
All three verification gaps should be closed:

1. **Cover photo hero** (event-details.tsx): detailCoverPhotoUrl is fetched from getById on mount. When a cover photo exists, the Image branch renders instead of the teal gradient.

2. **Organized by line** (event-details.tsx): detailOrganizerName is fetched from getById on mount. When the organizer has a profile name, "Organized by [name]" renders below the hero.

3. **Cover photo thumbnail** (edit-event.tsx): detailCoverPhotoUrl is fetched from getById on mount. When a cover photo exists, the thumbnail Image renders instead of the initial-letter fallback.

Type check: `cd apps/gatherly-mobile && npx tsc --noEmit --pretty` passes with no new errors.
</verification>

<success_criteria>
- event-details.tsx calls eventsApi.getById(id) in a useEffect and stores coverPhotoUrl + organizerName in local state
- edit-event.tsx calls eventsApi.getById(id) in a useEffect and stores coverPhotoUrl in local state
- Hero, organized-by line, and thumbnail all read from local state, not from EventsContext
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/32-screen-redesigns/32-03-SUMMARY.md`
</output>
