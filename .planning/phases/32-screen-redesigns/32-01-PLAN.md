---
phase: 32-screen-redesigns
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/routes/events.ts
  - apps/gatherly-mobile/app/api/events.ts
  - apps/gatherly-mobile/app/event-details.tsx
autonomous: true

must_haves:
  truths:
    - "Event Details screen shows a full-bleed cover photo hero (or teal gradient fallback) with event name, date, and location overlaid"
    - "An UPCOMING/TODAY/PAST badge appears on the hero based on event date"
    - "Organized by [name] line appears below the hero, sourced from the organizer's profile"
    - "Module cards are grouped under ACTIVITY, COLLABORATION, MEMORIES category headers"
    - "All modules shown including unimplemented ones (Photo Gallery) as greyed-out locked cards"
    - "Gift Exchange card shows assignment status line (Assignments generated / Setup needed)"
    - "Tapping an active module card navigates to its content screen where one exists (gift_exchange, polls, rsvp); potluck shows a toast since its screen ships in Phase 33"
  artifacts:
    - path: "apps/api/src/routes/events.ts"
      provides: "organizerName in GET /events/:id response, gift_exchange auto-insert removed from POST /events"
    - path: "apps/gatherly-mobile/app/api/events.ts"
      provides: "TEvent type with coverPhotoUrl, location, allowGuestInvites, isPublic, organizerName fields"
    - path: "apps/gatherly-mobile/app/event-details.tsx"
      provides: "Redesigned Event Hub screen with full-bleed hero and module cards"
      min_lines: 300
  key_links:
    - from: "apps/gatherly-mobile/app/event-details.tsx"
      to: "/api/events/:id"
      via: "eventsApi.getById fetching organizerName, coverPhotoUrl, location"
      pattern: "organizerName|coverPhotoUrl|location"
    - from: "apps/gatherly-mobile/app/event-details.tsx"
      to: "modulesApi.getModules"
      via: "useEffect fetch on mount"
      pattern: "modulesApi\\.getModules"
---

<objective>
Redesign the Event Details screen into an Event Hub with a full-bleed cover photo hero, organizer line, and categorized module cards. Also update the backend to return organizer name and remove the gift_exchange auto-insert on event creation.

Purpose: Transform the event details view from a basic info screen into a visually rich Event Hub matching the Details.png template, with module cards as the primary navigation mechanism.
Output: Updated API returning organizerName, updated TEvent type, fully rewritten event-details.tsx
</objective>

<execution_context>
@C:\Users\dihan\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\dihan\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-screen-redesigns/32-CONTEXT.md
@.planning/phases/32-screen-redesigns/32-RESEARCH.md
@apps/gatherly-mobile/screen-templates/Details.png
@apps/gatherly-mobile/app/event-details.tsx
@apps/gatherly-mobile/app/api/events.ts
@apps/gatherly-mobile/app/api/modules.ts
@apps/api/src/routes/events.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend API changes + TEvent type update</name>
  <files>
    apps/api/src/routes/events.ts
    apps/gatherly-mobile/app/api/events.ts
  </files>
  <action>
  **1. Add organizerName to GET /api/events/:id response** (apps/api/src/routes/events.ts, ~line 111):

  After the `SELECT * FROM events WHERE id = $1` query (line 111), add a separate query to fetch the organizer's name:
  ```sql
  SELECT u.name FROM users u WHERE u.id = $1
  ```
  Pass `event.organizer_id` as the parameter. Store result as `organizerName` (may be null if no organizer_id or user not found).

  Add `organizerName: organizerResult.rows[0]?.name || null` to the response object at ~line 194.

  **2. Remove gift_exchange auto-insert** from POST /api/events (lines 241-245):

  Delete the 4-line block:
  ```
  // Auto-insert gift_exchange module for new events
  await query(
    "INSERT INTO event_modules ...",
    [event.id],
  );
  ```

  **3. Update TEvent type** in apps/gatherly-mobile/app/api/events.ts:

  Add these fields to the `TEvent` type (after the Phase 25 additions block):
  ```typescript
  // Phase 32 additions
  coverPhotoUrl?: string | null;
  location?: string | null;
  allowGuestInvites?: boolean;
  isPublic?: boolean;
  organizerName?: string | null;
  ```

  Do NOT change eventsApi.getById or eventsApi.update — they already pass through all fields from the API response via `response.data`.
  </action>
  <verify>
  - `curl -s http://localhost:5001/api/events/1 -H "Authorization: Bearer $TOKEN" | jq '.organizerName'` returns a name string or null
  - `grep -n "gift_exchange" apps/api/src/routes/events.ts` shows NO auto-insert block (only other references remain)
  - `grep "organizerName" apps/gatherly-mobile/app/api/events.ts` shows the field in TEvent
  </verify>
  <done>GET /api/events/:id returns organizerName field; gift_exchange is no longer auto-inserted on event create; TEvent type includes all Phase 32 fields</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite event-details.tsx as Event Hub</name>
  <files>
    apps/gatherly-mobile/app/event-details.tsx
  </files>
  <action>
  **Install expo-linear-gradient first:**
  ```bash
  cd apps/gatherly-mobile && npx expo install expo-linear-gradient
  ```

  **Full rewrite of event-details.tsx** to match the Details.png template. The screen structure:

  **A. Full-bleed hero (top ~35% of screen):**
  - Use `Image` from react-native for the cover photo (source `{ uri: event.coverPhotoUrl }`) that extends edge-to-edge and behind the status bar
  - If no coverPhotoUrl: use `LinearGradient` from `expo-linear-gradient` with colors `['#14b8a6', '#0f766e', '#134e4a']` (teal gradient)
  - Overlay a semi-transparent gradient from bottom using `LinearGradient` with `colors={['transparent', 'rgba(0,0,0,0.6)']}` for text legibility
  - Back arrow (ArrowLeft) top-left, positioned absolutely over the hero, white color
  - Event name as bold white text near the bottom of the hero
  - Date + location line below the name in white/semi-transparent, formatted: "Jul 15, 2024 - Santa Monica, CA"
  - **Date-based badge** top-right of hero: compute from `event.eventDate`:
    - Same day = "TODAY" (amber badge)
    - Future = "UPCOMING" (teal badge)
    - Past = "PAST" (gray badge)
    - No date = no badge

  **B. "Organized by [Name]" line:**
  - Immediately below the hero
  - Fetch `organizerName` from the event data (already in TEvent from Task 1)
  - Gray text, left-aligned with padding

  **C. "Event Hub" header with "Manage All" link (organizer only):**
  - "Event Hub" as section heading
  - "Manage All" tappable text on the right that navigates to `/edit-event?id=${id}` — only visible if user is organizer (user.participantId === undefined)

  **D. Module cards with category grouping:**

  Define a MODULE_CATALOG constant (NOT derived from API — this is the full catalog of all possible modules):
  ```typescript
  type ModuleCatalogEntry = {
    type: string;
    label: string;
    description: string;
    icon: React.ReactNode;
    category: 'ACTIVITY' | 'COLLABORATION' | 'MEMORIES';
    comingSoon?: boolean;
  };
  ```

  Categories and modules:
  - **ACTIVITY**: gift_exchange (icon: Gift), white_elephant (icon: Gift, comingSoon: true)
  - **COLLABORATION**: potluck (icon: Utensils), polls (icon: BarChart2), rsvp (icon: CheckSquare), expense_splitter (icon: DollarSign — import from lucide-react-native, comingSoon: true)
  - **MEMORIES**: photo_gallery (icon: Camera — import from lucide-react-native, comingSoon: true)

  For each category, render a category header label (e.g., "ACTIVITY") as small uppercase gray text, then the module cards below it.

  Each module card:
  - Rounded card with icon (teal bg circle), module name, status/description line, right chevron or lock icon
  - **Active modules** (in `activeModules` from API): full color, tappable, show status line
    - Gift Exchange status: if `event.assignments` is not null -> "Assignments generated", else "Setup needed"
    - Potluck: "Coordinate food and drink signups" (static for now)
    - Polls: show "Let guests vote on event details"
    - RSVP: show "Collect attendance confirmations"
  - **Inactive modules** (not in activeModules, not comingSoon): dimmed/gray, not tappable
  - **Coming Soon modules** (comingSoon flag): gray icon, "Coming soon" badge text, lock icon, not tappable

  **Navigation on module card tap:**
  - gift_exchange -> `router.push('/view-wishlists?id=${id}')`
  - potluck -> show toast "Potluck screen coming soon" (no screen exists yet, Phase 33 will add it)
  - polls -> `router.push('/polls?id=${id}')`
  - rsvp -> `router.push('/rsvp?id=${id}')`
  - white_elephant, photo_gallery, expense_splitter -> not tappable (coming soon / inactive)

  **E. Keep the existing bottom action bar** (View Wishlists + Add My Gifts buttons).

  **F. Keep the existing "Your Secret Assignment" card** between organizer line and Event Hub heading.

  **G. Keep the "Create account" banner** for participant-only sessions.

  **Remove:** The old hero block (colored square with initial letter), the old stats row, the old Participants section, the old Event Modules section. These are replaced by the new layout above.

  **Keep:** Wishlist progress card, countdown banner — place them between the assignment card and the Event Hub section.
  </action>
  <verify>
  - `npx tsc --noEmit` in apps/gatherly-mobile passes (no type errors)
  - The app renders the event details screen without crashes
  - Cover photo hero shows when coverPhotoUrl is set; gradient fallback when null
  - Module cards display with category headers
  - Tapping gift_exchange navigates to wishlists; tapping potluck shows toast; tapping polls/rsvp navigates correctly
  </verify>
  <done>Event Details screen is fully redesigned as an Event Hub with full-bleed hero (photo or gradient), date badge, organizer name, categorized module cards with proper navigation (including potluck toast fallback), and retained assignment/wishlist/countdown sections</done>
</task>

</tasks>

<verification>
- GET /api/events/:id includes `organizerName` in response
- POST /api/events no longer auto-inserts gift_exchange module
- TEvent type on mobile includes coverPhotoUrl, location, allowGuestInvites, isPublic, organizerName
- Event Hub screen renders full-bleed hero with cover photo or teal gradient fallback
- Date-based badge (UPCOMING/TODAY/PAST) shows correctly
- Module cards grouped by category with Coming Soon badges on unimplemented modules
- Tapping Gift Exchange card navigates to wishlists
- Tapping Potluck card shows toast (no screen yet)
</verification>

<success_criteria>
The Event Details screen matches the Details.png template layout: full-bleed hero with photo/gradient, overlaid event info, organizer line, and categorized module card grid as primary navigation. Backend returns organizerName and no longer auto-inserts gift_exchange.
</success_criteria>

<output>
After completion, create `.planning/phases/32-screen-redesigns/32-01-SUMMARY.md`
</output>
