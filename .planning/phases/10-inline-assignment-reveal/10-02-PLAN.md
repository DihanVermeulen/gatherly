---
phase: 10-inline-assignment-reveal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/routes/magicLink.ts
  - apps/api/src/routes/invites.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "A magic link can be redeemed multiple times within its 7-day validity window"
    - "Magic link tokens expire after 7 days, not 24 hours"
    - "First-time redemption still creates participant and accepts invite"
    - "Returning participant still gets fresh JWT tokens on re-redemption"
  artifacts:
    - path: "apps/api/src/routes/magicLink.ts"
      provides: "Non-destructive token lookup (SELECT instead of DELETE)"
      contains: "SELECT invite_id FROM magic_link_tokens WHERE token_hash"
    - path: "apps/api/src/routes/invites.ts"
      provides: "7-day token expiry on both invite creation and invite acceptance flows"
      contains: "7 * 24 * 60 * 60 * 1000"
  key_links:
    - from: "apps/api/src/routes/magicLink.ts"
      to: "magic_link_tokens table"
      via: "SELECT query that reads token without deleting it"
      pattern: "SELECT invite_id FROM magic_link_tokens"
---

<objective>
Fix magic link tokens to be reusable within a 7-day validity window.

Purpose: UAT revealed that magic links are single-use (token deleted on first redemption) with only 24-hour expiry. Users expect to click the same magic link multiple times over 7 days to access their event. This plan fixes the backend to support that behavior.

Output: Modified magicLink.ts (SELECT instead of DELETE) and invites.ts (7-day expiry instead of 24h).
</objective>

<execution_context>
@C:\Users\dihan\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\dihan\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-inline-assignment-reveal/10-01-SUMMARY.md
@apps/api/src/routes/magicLink.ts
@apps/api/src/routes/invites.ts
@apps/api/src/db/schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change magic link redemption from single-use DELETE to reusable SELECT</name>
  <files>apps/api/src/routes/magicLink.ts</files>
  <action>
    In `apps/api/src/routes/magicLink.ts`, modify the token consumption query at lines 54-58.

    **Replace:**
    ```typescript
    const consumeResult = await query(
      `DELETE FROM magic_link_tokens
       WHERE token_hash = $1 AND expires_at > NOW()
       RETURNING invite_id`,
      [tokenHash],
    );
    ```

    **With:**
    ```typescript
    const lookupResult = await query(
      `SELECT invite_id FROM magic_link_tokens
       WHERE token_hash = $1 AND expires_at > NOW()`,
      [tokenHash],
    );
    ```

    Then update the variable name on the check and usage below:
    - Line 61: Change `consumeResult.rows.length === 0` to `lookupResult.rows.length === 0`
    - Line 65: Change `consumeResult.rows[0].invite_id` to `lookupResult.rows[0].invite_id`

    Also update the JSDoc comment block above the route (lines 24-33):
    - Change "Single-use: atomic DELETE + RETURNING ensures each token is consumed exactly once" to "Reusable: token persists in DB and can be redeemed multiple times within 7-day expiry window"
    - Change "Expiry enforced in SQL: tokens older than 24h automatically rejected" to "Expiry enforced in SQL: tokens older than 7 days automatically rejected"

    Do NOT change anything else in the file. The first-time participant creation logic (lines 98-136) and returning participant logic (lines 85-97) remain unchanged — they already handle both cases correctly.
  </action>
  <verify>
    - `cd apps/api && npx tsc --noEmit` passes (no type errors)
    - Grep for "DELETE FROM magic_link_tokens" in magicLink.ts returns zero matches
    - Grep for "SELECT invite_id FROM magic_link_tokens" in magicLink.ts returns one match
  </verify>
  <done>
    Magic link redemption endpoint uses SELECT (non-destructive) instead of DELETE. Token persists in the database after redemption, allowing the same magic link to be used multiple times until it expires.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend magic link token expiry from 24 hours to 7 days</name>
  <files>apps/api/src/routes/invites.ts</files>
  <action>
    In `apps/api/src/routes/invites.ts`, there are two locations where magic link token expiry is set to 24 hours. Change both to 7 days.

    **Location 1 — Line 85 (inside POST /events/:eventId/invites handler):**

    Replace:
    ```typescript
    const tokenExpiresAt = new Date(Date.now() + 24 * 60 * 60 * 1000); // 24 hours
    ```
    With:
    ```typescript
    const tokenExpiresAt = new Date(Date.now() + 7 * 24 * 60 * 60 * 1000); // 7 days
    ```

    **Location 2 — Line 306 (inside POST /invites/:code/accept handler):**

    Replace:
    ```typescript
    const tokenExpiresAt = new Date(Date.now() + 24 * 60 * 60 * 1000);
    ```
    With:
    ```typescript
    const tokenExpiresAt = new Date(Date.now() + 7 * 24 * 60 * 60 * 1000); // 7 days
    ```

    Do NOT change anything else. The invite expiration (30-day default on lines 56-63) is separate from the magic link token expiration and should not be modified.
  </action>
  <verify>
    - `cd apps/api && npx tsc --noEmit` passes (no type errors)
    - Grep for "24 \* 60 \* 60 \* 1000" in invites.ts returns zero matches
    - Grep for "7 \* 24 \* 60 \* 60 \* 1000" in invites.ts returns two matches
  </verify>
  <done>
    Both magic link token creation paths (invite creation and invite acceptance) generate tokens with 7-day expiry instead of 24-hour expiry.
  </done>
</task>

</tasks>

<verification>
1. TypeScript compilation: `cd apps/api && npx tsc --noEmit` passes
2. No DELETE in magicLink.ts token lookup: `grep -c "DELETE FROM magic_link_tokens" apps/api/src/routes/magicLink.ts` returns 0
3. SELECT used instead: `grep -c "SELECT invite_id FROM magic_link_tokens" apps/api/src/routes/magicLink.ts` returns 1
4. Both expiry values updated: `grep -c "7 \* 24 \* 60 \* 60 \* 1000" apps/api/src/routes/invites.ts` returns 2
5. No 24h expiry remaining: `grep -c "24 \* 60 \* 60 \* 1000" apps/api/src/routes/invites.ts` returns 0
</verification>

<success_criteria>
- Magic link tokens are not deleted on redemption (SELECT, not DELETE)
- Magic link tokens expire after 7 days (both creation paths)
- Existing redemption flow (first-time and returning participant) unchanged
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/10-inline-assignment-reveal/10-02-SUMMARY.md`
</output>
