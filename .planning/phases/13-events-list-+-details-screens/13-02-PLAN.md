---
phase: 13-events-list-+-details-screens
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - apps/gatherly-mobile/app/event-details.tsx
autonomous: true

must_haves:
  truths:
    - "User can tap an event card and see the event details screen"
    - "Details screen shows event name, status badge, member count, and gift count"
    - "User can tap 'View My Assignment' to reveal their secret assignment inline"
    - "User can tap 'Hide Assignment' to hide the revealed assignment"
    - "Details screen shows a list of all participants with avatar and role badge"
    - "When no assignments exist, the assignment card shows 'not yet generated' state"
    - "'View All Gifts' and 'Add My Gifts' buttons are visible (stubbed for future phases)"
  artifacts:
    - path: "apps/gatherly-mobile/app/event-details.tsx"
      provides: "Event details screen matching Details.png template"
      min_lines: 120
  key_links:
    - from: "apps/gatherly-mobile/app/event-details.tsx"
      to: "useEvents"
      via: "reads event by ID from context"
      pattern: "events\\.find.*id"
    - from: "apps/gatherly-mobile/app/event-details.tsx"
      to: "useSession"
      via: "reads user for assignment lookup"
      pattern: "useSession|user\\?"
    - from: "apps/gatherly-mobile/app/event-details.tsx"
      to: "assignments"
      via: "in-memory lookup by user name"
      pattern: "assignments\\?\\[.*\\]"
---

<objective>
Create the Event Details screen matching Details.png template with participant list, inline assignment reveal, and stubbed gift action buttons.

Purpose: Completes EVNT-04 — users can view event details including their secret assignment without needing to decode secret codes.
Output: A fully styled event-details.tsx screen accessible by navigating from the Events list.
</objective>

<execution_context>
@C:\Users\dihan\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\dihan\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-events-list-+-details-screens/13-RESEARCH.md
@apps/gatherly-mobile/app/_layout.tsx
@apps/gatherly-mobile/app/edit-event.tsx
@apps/gatherly-mobile/app/contexts/EventsContext.tsx
@apps/gatherly-mobile/app/contexts/AuthContext.tsx
@apps/gatherly-mobile/app/api/events.ts
@apps/gatherly-mobile/screen-templates/Details.png
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create event-details.tsx matching Details.png</name>
  <files>apps/gatherly-mobile/app/event-details.tsx</files>
  <action>
Create `apps/gatherly-mobile/app/event-details.tsx` as a push screen. Use the Details.png template as the visual guide. Reference the code example in 13-RESEARCH.md as a starting scaffold but implement the full styled version.

**Imports:**
- `useLocalSearchParams, useRouter` from `expo-router`
- `useEvents` from `./contexts/EventsContext`
- `useSession` from `./contexts/AuthContext`
- `useState` from `react`
- `View, ScrollView` from `react-native`
- GlueStack components: `Text`, `Button/ButtonText`, `Badge/BadgeText`, `Avatar/AvatarFallbackText`, `Pressable`
- Icons: `ArrowLeft`, `MoreVertical`, `Users`, `Gift`, `EyeOff`, `ChevronRight` from `lucide-react-native`

**Screen structure (matching Details.png exactly):**

1. **Header bar:** Back arrow (left) via `router.back()`. Three-dot menu icon (right) — for now a simple `Pressable` with `MoreVertical` icon (no menu action needed in Phase 13, can be wired in Phase 14 for edit/delete).

2. **Hero area:** A teal/mint gradient-like block (use `bg-teal-500` or similar, ~h-48) with a semi-transparent overlay. Show event name large and white-centered on this block. Below the hero, show a status badge: "ACTIVE" (green) if `event.assignments !== null`, "PLANNING" (blue) otherwise. Use GlueStack `Badge`.

3. **Stats row:** An HStack or flex-row showing: `{people.length} Members` with Users icon, and `{Object.keys(event.gifts ?? {}).length} Gifts` with Gift icon. Separate with a vertical divider or pipe character. Style: small text, muted color.

4. **"Your Secret Assignment" card:** Teal/mint background (bg-teal-50 or bg-teal-100), rounded-2xl, padding. Contains:
   - A lock/secret icon (use `EyeOff` from lucide)
   - Heading: "Your Secret Assignment" (bold)
   - Subtext: When not revealed: "Shhh! It's a secret. View who you are buying for." When revealed: "You are buying for: {names joined by comma}"
   - Button: "View My Assignment" (dark bg, white text) / "Hide Assignment" when revealed
   - Toggle state with `useState<boolean>(false)`
   - Assignment lookup: `const myAssignment = event.assignments?.[user?.name ?? ''] ?? []`
   - When `event.assignments === null`: show "Assignments haven't been generated yet." with no button
   - When assignments exist but user has no entry (name mismatch): show "No assignment found for your account."

5. **Participants section:**
   - Section header row: "Participants" (bold, left) + "Invite Members" text (right, muted/disabled — no navigation, this is Phase 17+)
   - FlatList or map over `event.people` array
   - Each row: `Avatar` with `AvatarFallbackText` (shows initials), name text (bold), role Badge on right, chevron-right icon
   - Role logic: if `participant name === user?.name && user?.role === 'organizer'` then show "ORGANIZER" badge (teal/green). Otherwise show "PARTICIPANT" badge (gray/slate).
   - Divider line between rows (thin border-b)

6. **Bottom action bar:** Absolute positioned at bottom, white bg, border-top, flex-row with gap:
   - "View All Gifts" — outline button, flex-1. On press: show a Toast or console.log "Coming soon" (no gifts screen exists yet). Do NOT navigate to a non-existent route.
   - "+ Add My Gifts" — filled primary button, flex-1. On press: same "Coming soon" stub.

7. **SafeAreaView / padding:** Use `contentContainerStyle={{ paddingBottom: 120 }}` on ScrollView to account for bottom bar. The screen does NOT show the tab bar (it's a Stack push screen with `headerShown: false`).

**Edge cases:**
- `event` not found (bad ID): Show centered "Event not found" text with back button
- `event.people` is empty: Show "No participants yet" in participants section
- `event.gifts` is undefined: Default to `{}` for count

**Style notes from Details.png:**
- The hero area has a photo background in the template — use a solid teal/mint color instead (no event images in data model)
- Status badge overlaps the bottom of the hero area slightly
- Event name is large (text-2xl or text-3xl), bold, dark
- The assignment card has rounded corners, distinct background, clear CTA button
- Participant rows are clean with good spacing
- Bottom bar has equal-width buttons
  </action>
  <verify>
1. Navigate from Events list by tapping a card — event-details screen loads with correct event data
2. Event name, status badge, member count, gift count all display correctly
3. "View My Assignment" button toggles to show/hide receiver names
4. When no assignments exist, card shows "not yet generated" message with no button
5. Participant list shows all event.people with avatar initials and role badges
6. Back arrow returns to Events list
7. "View All Gifts" and "Add My Gifts" buttons are visible and show "Coming soon" feedback
8. No TypeScript errors
  </verify>
  <done>
Event details screen renders matching Details.png layout: hero with event name, status badge, stats row, assignment reveal card with toggle, participant list with avatars and role badges, bottom action bar with stubbed gift buttons. All edge cases handled (no event, no assignments, no participants).
  </done>
</task>

</tasks>

<verification>
1. Full flow works: Events list → tap card → Details screen loads with correct data
2. Assignment reveal: tap "View My Assignment" → shows names → tap "Hide Assignment" → hides
3. No-assignment state: card shows informational message, no reveal button
4. Participant list: all names shown with correct initials and role badges
5. Back navigation: arrow returns to Events list
6. Bottom buttons visible and tappable (stubbed)
7. Screen matches Details.png visual structure
8. No TypeScript errors
</verification>

<success_criteria>
- event-details.tsx exists and is navigable from Events list
- Screen visually matches Details.png template (hero, badge, stats, assignment card, participants, bottom bar)
- Assignment reveal works with toggle state
- Participant role badges derived from user.role
- Gift buttons stubbed with "Coming soon" feedback
- Back arrow navigates back to Events list
</success_criteria>

<output>
After completion, create `.planning/phases/13-events-list-+-details-screens/13-02-SUMMARY.md`
</output>
