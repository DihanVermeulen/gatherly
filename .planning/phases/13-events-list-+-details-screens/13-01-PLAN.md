---
phase: 13-events-list-+-details-screens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/gatherly-mobile/app/_layout.tsx
  - apps/gatherly-mobile/app/(tabs)/index.tsx
  - apps/gatherly-mobile/app/contexts/EventsContext.tsx
  - apps/gatherly-mobile/app/api/events.ts
  - apps/gatherly-mobile/components/CreateEvent.tsx
autonomous: true

must_haves:
  truths:
    - "Events tab loads without crashing (EventsProvider mounted)"
    - "User sees a list of events with name, participant count, and status"
    - "User can search events by name and the list filters"
    - "User can create a new event and it appears in the list"
    - "User can delete an event and it disappears from the list"
    - "Tapping an event card navigates to event-details screen"
  artifacts:
    - path: "apps/gatherly-mobile/app/_layout.tsx"
      provides: "EventsProvider wrap + event-details Stack.Screen registration"
      contains: "EventsProvider"
    - path: "apps/gatherly-mobile/app/(tabs)/index.tsx"
      provides: "Events list screen matching Events.png template"
      min_lines: 100
    - path: "apps/gatherly-mobile/app/api/events.ts"
      provides: "Correct type exports (Event alias for TEvent, WishlistItem alias for TWishlistItem)"
      exports: ["TEvent", "Event", "TWishlistItem", "WishlistItem", "eventsApi"]
    - path: "apps/gatherly-mobile/components/CreateEvent.tsx"
      provides: "Create event with API call when useApi is true"
      contains: "eventsApi.create"
  key_links:
    - from: "apps/gatherly-mobile/app/_layout.tsx"
      to: "EventsProvider"
      via: "wraps Stack content"
      pattern: "EventsProvider"
    - from: "apps/gatherly-mobile/app/(tabs)/index.tsx"
      to: "/event-details"
      via: "router.push on card tap"
      pattern: "router\\.push.*event-details"
    - from: "apps/gatherly-mobile/components/CreateEvent.tsx"
      to: "eventsApi.create"
      via: "API call when useApi is true"
      pattern: "eventsApi\\.create"
    - from: "apps/gatherly-mobile/app/(tabs)/index.tsx"
      to: "dispatch DELETE_EVENT"
      via: "handleDeleteConfirmed dispatches with stored eventToDeleteId"
      pattern: "DELETE_EVENT.*eventToDeleteId"
---

<objective>
Complete the Events list screen to match Events.png template, fix all broken behaviors (delete, navigation, search, create API), and mount EventsProvider in the app layout.

Purpose: This is the foundation for Phase 13 — without EventsProvider mounted, no screen using useEvents() will work. Without the bug fixes, create/delete/navigation are all broken.
Output: Working Events list screen with functional create, delete, search, and card-tap navigation to event-details.
</objective>

<execution_context>
@C:\Users\dihan\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\dihan\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-events-list-+-details-screens/13-RESEARCH.md
@apps/gatherly-mobile/app/_layout.tsx
@apps/gatherly-mobile/app/(tabs)/index.tsx
@apps/gatherly-mobile/app/contexts/EventsContext.tsx
@apps/gatherly-mobile/app/api/events.ts
@apps/gatherly-mobile/components/CreateEvent.tsx
@apps/gatherly-mobile/screen-templates/Events.png
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount EventsProvider + fix type exports + register event-details screen</name>
  <files>
    apps/gatherly-mobile/app/_layout.tsx
    apps/gatherly-mobile/app/api/events.ts
  </files>
  <action>
**_layout.tsx changes:**
1. Import `EventsProvider` from `./contexts/EventsContext`
2. Wrap the content inside `GluestackUIProvider` with `<EventsProvider>...</EventsProvider>` — place it INSIDE GluestackUIProvider but OUTSIDE GestureHandlerRootView (or inside, as long as it wraps the Stack)
3. Add `<Stack.Screen name="event-details" options={{ headerShown: false }} />` inside the `Stack.Protected guard={!!session}` block, after the existing `edit-event` screen registration

**events.ts type export fixes:**
The file exports `TEvent` and `TWishlistItem` but EventsContext.tsx imports `Event` and `WishlistItem`. Add type aliases at the bottom of events.ts:
```typescript
// Type aliases for backward compatibility
export type Event = TEvent;
export type WishlistItem = TWishlistItem;
```
This ensures both `TEvent`/`TWishlistItem` (explicit) and `Event`/`WishlistItem` (legacy imports) resolve correctly.
  </action>
  <verify>
Run `npx tsc --noEmit` from apps/gatherly-mobile to confirm no type errors. Check that EventsProvider import resolves and Event/WishlistItem types are exported.
  </verify>
  <done>
EventsProvider wraps the app's Stack content. event-details screen is registered in the authenticated Stack.Protected block. Event and WishlistItem type aliases are exported from events.ts — no import errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix bugs + rework Events list screen to match Events.png + fix CreateEvent API call</name>
  <files>
    apps/gatherly-mobile/app/(tabs)/index.tsx
    apps/gatherly-mobile/components/CreateEvent.tsx
  </files>
  <action>
**CreateEvent.tsx — add API call:**
1. Import `eventsApi` from `@/app/api/events` and destructure `useApi` from `useEvents()` (change `const { dispatch } = useEvents()` to `const { dispatch, useApi } = useEvents()`)
2. Make `handleCreate` async. When `useApi` is true, call `const created = await eventsApi.create(eventName.trim())` then dispatch `ADD_EVENT` with the returned event object. When `useApi` is false, keep the existing local dispatch with `Date.now().toString()` id.
3. Wrap the API call in try/catch — on error, log it and optionally show an alert. The `eventsApi.create()` endpoint requires organizer role; if a 403 comes back, it means the user isn't an organizer.

**index.tsx — fix delete bug:**
1. Add state: `const [eventToDeleteId, setEventToDeleteId] = useState<string | null>(null)`
2. Fix `confirmDelete`: set `setEventToDeleteId(id)` then `setIsDeleteAlertOpen(true)`
3. Add `handleDeleteConfirmed` async function: if `eventToDeleteId` is null, return. If `useApi`, call `await eventsApi.delete(eventToDeleteId)`. Then dispatch `{ type: 'DELETE_EVENT', payload: eventToDeleteId }`. Finally `setIsDeleteAlertOpen(false)` and `setEventToDeleteId(null)`.
4. Wire the AlertDialog confirm button to call `handleDeleteConfirmed`. Add a Cancel button that calls `setIsDeleteAlertOpen(false)`.
5. Destructure `useApi` from `useEvents()` and import `eventsApi` from the API module.

**index.tsx — fix navigation:**
1. Change `router.push("/edit-event", {})` to `router.push(`/edit-event?id=${item.id}`)` on the Manage button
2. Wrap the entire card `<Pressable>` with an `onPress` handler: `router.push(`/event-details?id=${item.id}`)`

**index.tsx — wire search filter:**
1. Add state: `const [searchQuery, setSearchQuery] = useState('')`
2. Add state for status filter: `const [statusFilter, setStatusFilter] = useState<'all' | 'planning' | 'active'>('all')`
3. Add derived state using `useMemo`:
```typescript
const filteredEvents = useMemo(() =>
  events.filter(e => {
    const matchesSearch = searchQuery.trim() === '' ||
      e.name.toLowerCase().includes(searchQuery.toLowerCase());
    const matchesStatus = statusFilter === 'all' ||
      (statusFilter === 'active' && e.assignments !== null) ||
      (statusFilter === 'planning' && e.assignments === null);
    return matchesSearch && matchesStatus;
  }),
  [events, searchQuery, statusFilter]
);
```
4. Wire the TextInput's `value` to `searchQuery` and `onChangeText` to `setSearchQuery`
5. Render `filteredEvents` in FlatList instead of `events`

**index.tsx — rework UI to match Events.png template:**
The Events.png shows: header with title + avatar icon, search bar, horizontal filter pills (category), section heading, event cards with hero area + name + stats + status pill + actions, FAB button.

Adapt for gatherly:
- Header: "My Events" title (keep existing) + user avatar or bell icon on right
- Search bar: already exists, just wire it (done above)
- Filter pills: horizontal ScrollView with "All", "Planning", "Active" pills. Highlight selected pill with filled bg (e.g., bg-primary-500 text-white), others outline. Use `Pressable` for each pill.
- Event cards: Redesign to match Events.png card pattern:
  - Top area: solid color block (teal/mint like the app theme, ~h-32) with event name initial large + party icon overlay. Use a `View` with `className="h-32 rounded-t-2xl bg-teal-500 items-center justify-center"` containing the first letter of event name in large white text.
  - Below hero: event name in bold, date row with calendar icon, stats row (participants count + gift count)
  - Status pill: "Active" (green) if assignments exist, "Planning" (blue) if not
  - Action row: "View" button (navigates to details) + trash icon button (delete)
- Remove the "Manage" and "Share" buttons from cards — replace with simpler "View" that navigates to details, plus trash icon
- Keep the FAB for create event
- Keep the BottomSheet for CreateEvent component
- Keep the AlertDialog for delete confirmation
- Keep the empty state

Remove the `useNavigation` import from `@react-navigation/native` — it is unused after the rework.
  </action>
  <verify>
1. Start the Expo dev server and load the Events tab — it should render without crashing
2. Verify: Search bar filters events by name as you type
3. Verify: Filter pills switch between All/Planning/Active
4. Verify: Tapping an event card navigates (may show blank screen if event-details.tsx doesn't exist yet — that's fine for this task, Plan 02 creates it)
5. Verify: Tapping delete icon shows AlertDialog, confirming deletes the event
6. Verify: Creating an event via bottom sheet adds it to the list
  </verify>
  <done>
Events list screen matches Events.png layout pattern: header, search, filter pills, styled event cards with hero area + stats + status. Delete works correctly (stores event ID, dispatches on confirm). Navigation passes event ID as query param. Search filters events by name. CreateEvent calls eventsApi.create() when API is available.
  </done>
</task>

</tasks>

<verification>
1. Events tab loads without crash (EventsProvider mounted)
2. Event cards display name, participant count, status badge
3. Search bar filters events in real-time
4. Filter pills filter by Planning/Active status
5. Create event via FAB + bottom sheet works (calls API when available)
6. Delete event via trash icon + confirm dialog works
7. Card tap navigates to `/event-details?id=X` (screen may not exist yet — navigation intent is correct)
8. No TypeScript errors (`npx tsc --noEmit`)
</verification>

<success_criteria>
- EventsProvider is mounted in _layout.tsx wrapping the Stack content
- event-details screen is registered in Stack.Protected
- Events list renders with styled cards matching Events.png layout
- All three bugs fixed: delete stores ID, navigation passes ID, search filters
- CreateEvent calls eventsApi.create() when useApi is true
- Event/WishlistItem type aliases exported from events.ts
</success_criteria>

<output>
After completion, create `.planning/phases/13-events-list-+-details-screens/13-01-SUMMARY.md`
</output>
